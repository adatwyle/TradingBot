# Coding a Profitable Stochastic Moving Average EA for MT5 (mql5 Tutorial)

- URL : https://www.youtube.com/watch?v=YrzfNo8iKlw
- Date de publication : -
- Vues : 30 000
- Duree : 37:36
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is Né and welcome back for another video on this channel. And today I will give you another programming tutorial and we will work with the stock cost stick and the moving average indicator. So before I explain how the strategy will work, make sure to smash the like button and also subscribe to the channel so you don't miss anything about automated trading in general on how to program for the MetaTrader 5. Also it's free, so you have nothing to lose. Okay, so let's talk about the strategy here. I wanted to use this oscillator as a main trigger for trades. And my first idea is to wait until the price or until the sarcastic indicator jumps above

[00:45] the 20 line here or below the 80 line. And of course, when it jumps above the 20 line, we will be entering a buy trend. So for example here we would enter a buy trend, maybe can make this even a little bit bigger. And yeah, for example, this would be another buy signal. This would also be a buy signal. This would be a buy signal somewhere here. And this would be a sale signal. Then we would have another buy signal. So you can see where this is going. like a classical stochastic strategy, but I want to combine it with a indicator. And this is the moving average. So we will make it completely flexible.

[01:32] So you can choose any moving average with any period or any method. And this moving average will work as a filter. So we will be able to choose like the overall trend direction with this moving average and then choose the stochastic indicator for the for the entries of the trades. So this is the idea and we will make this. So you can choose like the time frame for both indicators. So this is also like a multi time frame program. So for example, you could say I want to have the moving average on the daily chart and I want to have the stochastic on the one hour chart. OK, so this is the plan. If you like it, yeah, go ahead and watch the video.

[02:11] So we will now write the code for this program. I will keep everything short and simple so you can follow it quickly. Also, if you're interested in more programming videos, check out the link below this video and you will find a link to a complete course where you learn everything for MetaTrader5 programming, which is also suitable for absolute beginners. So let's jump into the programming part. When you are in your MetaTrader5, you will have to click on Tools, MetaQuartz Language Editor. This will open the program that you can then use to write automated trading strategies for MetaTrader5. And on the left side you would see a navigator.

[02:48] If you do not see it, click on View Navigator. And then you can click on New in the upper left corner of the Meta Editor. You then have to select Expert Advisor Template, click Next, Provide a Name. We can say Sochastik Moving Average Program. It does really matter what name you choose. Then you click on Next, Next and Finish. And this creates a brand new Expert Advisor for you. advisor are the kind of programs in Metatrader 5 programming that can open, modify and close positions and can also place orders of course. So what I'm doing here is I delete all these gray lines. You can just go ahead and delete all the gray lines because they are just comments. Also I

[03:32] deleted the first three lines with the properties because they are not really needed. They do do not have any influence on the program or on the functionality of the program. Then I like to rearrange my brackets like this. You can do this, you do not have to do this. This also doesn't really change the functionality but this is like the style that I used to program in. Okay, so when we want to work with indicators, like the first thing we have to do is to get the indicator data, right? And in MetaTrailer 5 programming you do this by using indicator handles. So we can go here like outside of any of these three

[04:13] functions and we can declare variables for the indicators. So we will create two handle variables. The first one will be the handle for the stochastic indicator. The second one will be the handle for the moving average indicator. Just do it like this and then if you click on compile this will create the program for you. And once you did this, you can go back to your Metatrader. And if you have a look at the experts in your Metatrader and the navigator, you will already see the program. So we can search for stochastic moving average program and we can drag and drop it on a chart. And if we click on OK, you can see in the upper left upper right corner, the program

[04:55] is now already active in the chart. But it is not doing anything. And this is where these three functions come in handy because these functions are triggered when there is a specific event in a chart where a expert advisor is attached to. For example, we have this on init function and again we do not have to write this function that is already part of the expert advisor template. So we have this function and whenever a program is initialized, this function is triggered. So So if we write something like print and then in round brackets in it or in team or whatever you want to write, it will print this word in the experts journal as soon as the on-init

[05:41] function is triggered and is executed because it is in the body of this on-init function which is wrapped by these curly brackets here. So this print function is a predefined function so we can use it right away. And if you have a look at the program now, you can see there it is writing int. And if I change the time frame, it's writing int again. And if I remove the program and if I activate it, it is writing int again. So you can see whenever the program is initialized and it is initialized if you activate it, or if you change the time frame, then it is initialized on the new time frame, then we'll print this message.

[06:22] And this works, of course, pretty much the same for the dInit function, but here it says d in t. So if I change the time frame or before I change the time frame, it would be dInitialized on the last time frame and then initialized on the new time frame. You can read it in the expert journal. Yeah, also this dInitialization function is called when the program is removed, as you can see here. So these are functions that you can use. And there is one more function that is executed more frequently, which is the on tick function. This function is triggered with every single tick. So when I attach the program to the chart, I will not end the print statement because

[07:02] it's always the same. And if there is any price movement, it will automatically trigger or fire the on tick function. And everything in between of these curly brackets will be executed. So what we have to do now is first of all, we have to define or indicate us. So we already created these two variables. are always declared like this. You have a type, a data type. In this case, it is integer, which means that it can be any number, any numeric number, pretty much. So one, two, three, four, five, whatever. And this is just the name of the variable. This could be anything. This could be your dog's name doesn't really matter. This if you compile this, there's

[07:41] no error, everything works. But it always makes sense to choose a name that already describes the function of the variable and then we end the line with a semicolon and then in the onInit function we can change the value inside of this variable. For example if we write handle stock we could write something like this and we could store the value two inside of this variable because it is an integer variable. So this is okay. What we could not do is store a value like this in this integer variable. This would give us a warning because this is because of this decimal point, this would be a double type number and not a integer type number. So this

[08:21] is why data types are important. So we do not want to store just any number in this variable, but what we want to do is we want to return the we want to store the return value of a specific function. And this function is the small letter I and then capital letter S and then stochastic. So I stochastic function, you have to make sure that you write it exactly like this because otherwise it will not work. So this is a function and if we have a look at the reference which you can open if you move your cursor inside of this name pretty much and then you can go to mql5 reference and it will open the reference

[09:01] and you can read everything about this function. This is a predefined function. So it's already part of the mql5 framework and it will calculate or give us a handle for the stochastic oscillator indicator. So this returns an integer value. You can read about this here, which is the handle for an indicator. So a handle in MQA5 programming is used to describe one specific indicator. So we can describe this indicator in the function parameters and then it will combine all of these parameters pretty much and return one handle that then describes this specific indicator if you want to say so. So we first of all have to provide all of these

[09:51] parameters. So in round brackets directly after the function name you can provide the parameters. So first of all we will have to provide the symbol that we want to have this indicator calculated for. And this is the chart symbol. So we can use underscore symbol because this is a predefined variable which always holds the value of the current chart symbol. So in our case it would be euro US dollar. Then the second parameter is the time frame. And I said before I want to make this variable. So we will create a input variable for this. So we can say input timeframes and then we say something like

[10:32] stochastic timeframe timeframe like this and we can say for example period H1 which would be the one hour timeframe. And this is now a input variable. We declare input variables at the very top of our program usually and they are pretty much like normal variables, they have this input written before the variable data type. And this means that the user is able to change the value. We will see this in a second. Also the data type for this variable is enum timeframes. And if we have a look at the reference, this is a enumeration. So you can see you can put any of these values that are listed under ID. And yeah, we

[11:16] can choose any time frame pretty much. So the next parameters are the k, the d and the slowing. So we can say something like stochastic k. I think it's five by default stochastic d. I think it's three and stochastic slowing is three. So I'm just pretty much doing the same every single time. I create a new input variable, which is of type integer in this case. And I I choose a name for this variable. Again, this could be anything. So I could write it like this, would be totally okay. So, and then I put a default value, which is done by writing this equal sign and then put a default value and end the line with a semicolon.

[11:58] And now can use these variables stock k, stock d, stock slowing. And then it requests a, wait a second, i-stochastic, it requests a MA method. I think I would just hard code this. I think it is, let me look at the default value here. I think it is the simple moving average. And then we have the price field, which is low high. So we will hard code this. You could also create input variables for this. but we would say mode, simple moving average and stochastic low high. And this is it. These are all the parameters for the I stochastic function. So if we compile this after we close the parameter list

[12:48] with this round bracket and the semicolon, then you can see there are no errors. So everything is working and we now have this handle. This handle by the way is just like any number, but for the PC, this is one specific number which is connected to this specific indicator. So you can see the number is just 10. But yeah, if we have a look at the inputs here, you can see all of these things that we declared as input variables can be changed here in the input list of the EA. So what can we do with this handle now? We can use this handle to create, to receive data from this specific stochastic indicator.

[13:26] So there's another function, which is the copy buffer function. And we will put this in our on tick function. This is really important that you put it here because we want to calculate this indicator on every single tick. So what we do here is we provide all the parameters for this copy buffer function. Again, this function is predefined. We can use this straight away. So we say handle stochastic. So we provide the indicator handle. This is the indicator where we want to receive the data for. Then we have the buffer number. There are some indicators that have multiple lines. For example, the stochastic also has multiple lines.

[14:01] So what we can do here is we can have a look at the data window and you can see there is the stochastic line and the signal line. And the stochastic line is the green one I think and the signal line is the red one. So I think we will use the stochastic line here. So the stochastic line is the main line, I think. So we can choose a main line here. And we could also type zero. This is exactly the same, by the way. And then we can choose a starting position, which will be one for us, and a count, which will be two. I will explain in a second what this does. But in the end, we will just have to provide one more parameter.

[14:56] And this is an array of type double. And double, we already talked about the double data type before. You remember when I said, for example, you cannot store this in a integer variable because it has the decimal point. This is where double variables come into play because double variables can hold values that have a decimal point. So we can say, for example, we create this double variable named stock. And we will provide this as a last parameter. And this doesn't work, unfortunately, because this does not request a normal variable but you can see it requests an array. And you know this because there are these,

[15:39] how do you say like these brackets, these squared brackets. So this is why we have to put them here. So we have to say that this is an array and an array variable cannot only hold one value but it can hold multiple values. So the array can hold multiple values and this copy buffer function will increase the size of the array. So we can put both of our two values because we have the count of two inside of this array. And yeah, this is how it's done. So what we can do now is we can have a look at these values. So we can read the array at index zero and we can read the array at index one. These are the two index indices that we can read because we put two values

[16:26] We put two values inside of this array. So we have two indices. We cannot read stochastic at index two Because this would only be possible if we would have put it if we would have put three values inside of this array But you can see now it it always prints like the last two values for this stochastic for like the main line Or it should it should do this and I have to switch on the h1 time frame of course, because this is where I calculate the stochastic right now. And if we have a look like this value should be printed and it is printed, it is the second value printed and this value should be the first one printed and it is also printed. So I'm comparing the value in the data

[17:10] window with the value printed in the expert journal. So you can see this is like the easy way in MQL5 to receive data from pretty much any indicator. You just create a handle. Usually you make it a global variable like this. Then you initialize the handle variable in the onInit function and you use the i, i moving average, iBand, forBullingerBand, i whatever function for this and you put like the value in the handle variable and you can then use this handle variable in the copy buffer function to receive values for this specific indicator and you store these values in an array. So this is what you always do.

[17:50] So and because of this, we can do the same thing for the moving average. So we call the function, which is IMA for moving average. Then we provide the symbol again, we will create a MA timeframe variable. We create a MA period variable. We will have the shift default zero, I think. Then we can create MA method variable and the applied price. I can hard code this as price close. So we will use the close price of the bars. And if we compile this, this will throw an error, of course, because I first have to create these input variables. So we say enum timeframes again for the MA timeframe. And here again, I can just choose a higher timeframe.

[18:35] For example, the D1 chart, and this will automatically make or program a multi-timeframe, a multi-timeframe program pretty much. Then we have the input for the MA periods or MA period, which could be 100. We have E-NAM, MR method for the MA method. And here we can choose the simple moving average by default. For example, and if we compile it like this, all the errors are gone. And now we can pretty much do the same. What we did for the stochastic here with the moving average, we say copy buffer, now we provide the handle MA here. We choose the main line again. We say one. I think we only need one value and we store this value in the MA double array.

[19:19] Even if you only need, if you only receive one value, you still have to provide an array because this is just how the copy buffer function is designed. Okay. So now we have values. Oh, and I can print the MA value of course for you. So you can see this is indeed working. So you can see this should be the moving average value on the D1 chart. Let me check it's a 100 simple. Yeah, so this should be this moving average value. And I think it is correct. It is rounded of course, like here in the data window, but you can see this is correct. Okay, since we have all the necessary data already now, what we can do is we can check at the start

[20:00] of a new bar if we have a signal. And whenever we want to do something only once per bar, the easiest way to do so is to create another global variable, which could be named total bars, for example. And then we can say something like in the ontic function, we create one more variable. This is also another variable, which will store the return value of the iBars function. This iBars function, again, you can read about all the predefined functions in the reference, and we return the number of bars in a specific chart, pretty much. So we have to provide the chart symbol and the chart timeframe. And we will use the stochastic timeframe for this.

[20:41] Now we can check if the totalBARs value is unequal the BARs value. And if this is true, we want to process the code inside of the body of this if statement. Bars, totalBARs. So please do not be confused by this if statement. A if statement is a so-called controlled structure. So this control structure is used to control the flow of your program pretty much. So when the antique function is executed, everything in the antique function will be processed in the order that it is written here. But sometimes we do not want everything to be processed. And this is where control structures come in handy

[21:25] because we can use the control structures to have an influence on the flow of a program. So for example, if the antique function is executed, this will be executed on every single tick. So we will receive the number of bars for the current chart. Then we will check and this is always a check of a condition using a if statement. We will check if the total bars so the value in this variable if it is different than the value in this bars variable. And this is why we can do using this unequal operator which is always this exclamation mark and then one equal sign. So if these These values are different.

[22:06] Only then we enter the body of this if statement. So then we will process everything that is written here. If this condition is not true, we would simply skip the body of the if statement and we will process everything that is written here. Hope this was enough to make clear how an if statement works. So what do we do in the body of the if statement? We first of all update the total bars variable and we put the value from the bars variable inside of it. This makes sure that we only process the code here once per bar, only if there is a new bar in the chart, because then the value in the bars variable is greater than the

[22:46] value in the total bars variable. So then we will calculate all of these indicators and now we can search for entry signals. So what we can do here is, first of all I want to create two more variables for the stochastic here. So we can say something like double stochastic upper level, which is 80, for example, and input double stochastic lower level, level which could be 20, like this. Yes. So then we can check here, like in this if statement, we will put another if statement and we check if stochastic at index one is no smaller than stochastic upper level. And the previous value of the stochastic should be greater than the stochastic upper level. This would be a short

[23:34] signal. And we check the same thing pretty much or the like the opposite case. And we can combine combine multiple if statements using an if structure just add this if directly after the closing bracket for the body of the first if statement then we can say something like this if stochastic is greater than stochastic lower level and stochastic at index zero is smaller than stochastic lower level this would be a bias signal. So this is cell and the next one is bi. So if you write it like this we can now check, we can also check like the moving average. So what we want to do is we want to check if the current price is above or below the moving

[24:25] average. So first we will need the current price and you can get the current price using the symbol info double function in mql5 programming. This function double has two parameters. The symbol you want to receive the value for and then a identifier for what specific value you want to receive. In this case, what's happening here? So in this case we want to receive like the bit value. So I can make this a little bit bigger so you can see the whole on tick function. Okay, so since we have the bit value now for the current market, we can check if bit is greater than the moving average value. Oh no, for a sales signal, we have to check if bit is smaller than

[25:09] the moving average value. And in this case, we want to open our sales signal. And here we can check if bit is greater than the moving average value. In this case, we open our buy signal like this, right? So now all we have to do now is we have to open the positions. And the And the easiest way to do so in MetaTrader 5 programming is to use a class. And this is the Ctrade class. So what you have to write in the very top of your program is include like this with a small hashtag sign. And then in these spiky brackets like smaller and creator sign on your keyboard, you have to write trade slash trade dot mqh.

[25:48] Because this will include the Ctrade class into your program. This is a path pretty much to a file which lays in the include folder. So if you have a look at the navigator, you will find this include folder and you will find the trade folder and the trade.mkh file. So this is the file that we are now including into our program. And if you compile, you will see like in your toolbox, there's a lot of stuff happening because we are compiling a lot of files now since we included this. But this a little bit too complex. Again, if you want to learn about this, check out the link in the description and you will learn everything

[26:26] in like with like super detailed explanations. I'm not like rushing through the MQL5 or meter trader five masterclass complete course, but in this course I explain everything like completely detailed and you will have a complete video for variables, for data structures, for everything. But here on YouTube, I kind of have to shorten things a little bit. But what the C-Trad class enables us to do now is we can say Ctrade, trade like this, which we create another global variable, which is of type Ctrade. So this makes it an object variable. And the name is trade. So we can now use this trade variable.

[27:05] We can say here trade.sell, because this trade variable can be used to open, manage, or close positions. So we can now, for example, say we want to sell 0.1 lots. Also we could buy 0.1 lots. if we have a buy signal. So if you compile this, you can see there are no errors and we will be able to run this program now already. So you can see, I mean it is running in the chart but I do not want to wait for a signal. So what I will do is I will open the strategy tester. So you can click on view strategy tester and you can close the toolbox and And then we click on visualize. Then you go to stochastic moving average program for a expert here.

[27:52] You choose any market you want to test. For example, euro, US dollar, you choose a timeframe, one hour, you choose your period. I would just test the current year because I just want to see like if the program works and then make sure to take this like visual mode button here. And you can also change all the inputs here, but for now I would just use the default inputs and hit start button in the lower right corner of this strategy tester. So you can see this will open another window where it will simulate the historical data and it will search for like trading signals. So what we can see here is if we make this like a little slower, you can see whenever

[28:36] there's a sales signal, for example here or here, we will open a position. these are sell positions because we are currently below the 100 periods daily moving average. And you can see these signals were great already because we are in a strong downtrend. Now the price is changing directions. But yeah, you can see all signals are working. So now I want to wait pretty much for a buy signal. And let's see if we can see some buy signals. Of course, we will only open sell signals. Yeah, there you can see like as soon as the price is above the moving average We only saw this once now, but you you can see like if the price above the moving average

[29:18] We also see buy trades so I think our code is working But you can see this is completely messy because we never really closed the positions So what we can do is here we can provide a TP and a stop loss and we could say something like the TP and SL are based on are based on a specific amount of points. This would be okay. And I think this is like the easiest way for now. So we will just do it like this. So we can say, for example, we have a input variable. Wait, let me put it at the very bottom. We have several input variables for the trading parameters. Oh, also if you have a lot of input variables, it makes sense to create groups.

[30:11] So we can say something like group indicator inputs. And if you compile this, and if we create another group like trading inputs, you can see this will create some separators. And I will show it to you in a second. But first of all, I will create some variables here. For example, a variable for the lot size. I want to create a variable for the TP distance, TP distance, which could be something like this. It would be 100 points and the SA distance, also something like this. Or maybe we can choose a little bit wider as well. And you can just play around with this since they are input variables.

[30:56] And you can see like, if we have a look at the inputs, you can see they are now separated. There are trading inputs and indicator inputs. And this is what this group does. So you can see we can now change these values. But of course they do not really make any difference because we didn't use them yet. So what we can do here is we can say we want to sell and then we use our lots variable to say how many lots we want to sell or buy. But you can see these functions also have a lot of, a lot more different parameters. So first of all, we can provide the symbol. It's usually the chart symbol because you mostly want to trade the current chart

[31:33] where the expert advisor is running, but you could choose any other symbol. Then we want to copy this line and we want to receive the ask price also because we will use the bit and ask price for the calculation of the positions here. Because we want to open the sale position at the current bit price, then we want to calculate the stop loss which is bit plus s a distance and then we want to calculate the tp which is bit minus tp distance right we could have a order comment but I will not yeah put it here then we can do the same thing pretty much for the um by positions like this. If we compile this, this is not throwing any errors. And you can see we now

[32:24] provide a entry price for the orders, which is the current market price only. And then we can calculate the stop loss and the TP because these are the next parameters using our TP distance and SA distance inputs. So if we do it like this, this would open the positions again, just like they did before, but in this case, we also see like they have ATP and ASL. So you can see all of our positions are now closed in the TP or SL. And you can see this like really cool. Yeah, you can see like the cell position, it has ATP, it has ASL and we just wait until one of them is reached. So we can see like right now we are not making any profits because like I think

[33:10] the commissions are just too high or we just weren't able to make profits here. But, um, yeah, you can do a lot of testing with this program. And you can see, um, this is like the big benefit of automated trading because you can test like a lot of data, like really quickly. And you can see there are periods where this program is, is going like really well, and there are periods where it's not going that well. But, um, you can do a lot of optimizations now with this program. And I think I will do some more backtests. And you can see this would already be profitable if you just stick to the plan in 2022.

[33:47] Because you can see we have like a good profit factor. We made profit and everything. And this seems to work just fine for the current year. But of course you would have to do a lot more backtesting. So what else can I show to you here? Yeah, this is how the program is designed now. So you can see like just 60 lines of code. Once you get used to the programming, this will be really easy for you to write. I mean, it took me like 30 minutes now with all the, with all the explanations that I put into this video. If I would write this program without any explanations, I would write it in like 10 or 15 minutes.

[34:24] And I'm able to back test this completely unique strategy. I mean any strategy for as long as I want. I could back test this for 10 years. And this is like the big benefit that you get from automated trading. This is just crazy. I mean, like the benefits are so, so there are so many benefits of automated trading. And I cannot stress this enough in my videos. And I really just want people to understand that this can change your trading like completely. If you never, if you never were able to figure out if your strategies were profitable And if you were never able to create profitable strategies,

[35:07] this might change once you have a look at automated trading and programming especially. So yeah, check out the free tutorials on this channel. And if you want to learn it in a more structured way, check out the link below this video. But yeah, I think there's like one more last thing I want to show you. You can create another Boolean input variable. You can say, for example, is moving average filter active. You can call it like this or however you like. And if you put it to false, for example, you can then add some more code here for the filter where we check the filter. We can check if the MA filter active variable is false

[35:49] like this, like putting this exclamation mark in front of the name of the function or if this condition is true. So this is a way to like decide if you even want to use the filter because if I run the program again now, you will see that it will open positions no matter if like the price is above or below the moving average filter because I created this variable where the user can decide to like not care about the about the moving average filter. So you can see it should open by positions now. Also if we are below the moving average filter. So yeah, this changes pretty much like the complete strategy.

[36:40] And you can see now, like there are a lot more trades and it looks like the strategy is more profitable with this daily trend filter, at least in the tested period. So yeah, this is how you can quickly adjust your programs and how you can add like more code to it, add more filters, whatever you like. So this is like the benefit of automated trading. So make sure to copy the code if you want to use the program for yourself and if you want to do some testing. So this was like the short video on how to create a stochastic moving average program for MeteorTrader 5. Hope you liked it. Again, if you like the videos, give it a thumbs up, please, and subscribe.

[37:25] Maybe tell your friends or your fellow traders about these videos so they can also benefit from the automated trading. So this is it for this video. Thanks for watching. Have a great time and good trades. Bye bye.
