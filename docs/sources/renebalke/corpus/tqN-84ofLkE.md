# Easy Tripple Moving Average Trading Strategy | Simple mql5 Expert Advisor Coding Lesson

- URL : https://www.youtube.com/watch?v=tqN-84ofLkE
- Date de publication : -
- Vues : 29 000
- Duree : 54:47
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is René. Welcome back to another video on this channel. And today I will show you how to write a simple but effective Skyping strategy expert advisor in MetaTrader 5. So I already prepared something here in the chart and I will show you exactly what I mean. So I added three on normal moving averages here in the chart, you can see this is a 10 period, a simple moving average. The blue one is a 50 period, simple moving average. And the red one is a 100 period, simple moving average. And the strategy is really easy. So once these three moving averages are aligned like this, so the fastest is on top, then

[00:50] Then comes the middle one and then the slowest. This would indicate an uptrend or a buy or long trend. And whenever the price drops below the fastest moving average line, closes below this line And then with a new candle opens above the... Oh wait, so we have a close below the moving average and then a close above the moving average. So with the start of this bar we would buy it. And we put our SA, let's say somewhere here at the 50 periods moving average and the take profit somewhere up here. So we could have a factor for this, maybe 0.5 or 1.0. It doesn't really matter. We can make this a variable.

[01:51] So again, you would see this would be like the first signal. Then here we would have another signal because there is a bar closing below the fast moving average. And then we we have a close above the far moving average. So with this bar, we would enter a trade same here, here and here. Um, but I think we would design it like we only have a maximum of one open trades and then we will wait until this trade is closed before we open more positions. So, um, yeah, this is the general idea and we could also increase this. Yeah, we could also like have another signal where we trade with the middle and the slowest

[02:35] moving average. And we say if the middle moving average is above the slow moving average, and then we do the same pretty much. So if we have a close below the bar and then a new close above the middle moving average, then we enter a trade and put the stop loss at the very slowest moving average. So this is like the general idea. I will write the code together with you now. I would recommend that you type everything as I do because this is like the best way for you to learn something. I will explain one or two things, maybe not everything, because this is a YouTube tutorial. I do not want to go too much, but if you're interested in MetaTrader 5

[03:22] programming, check out the link below this video and you will find a complete course on MetaTrader programming which is absolutely suitable for beginners. So whenever we try, whenever we want to write a program, we will have to open the MetaQuest language editor. So you can click on tools and MetaQuest language editor in your MetaTrader 5. This will open the meta editor which is like the tool that we use to write our programs and on the left side you can see the navigator with all the programs and if you do not see it you can click on view navigator and there we can create a new expert advisor so if you click on experts and then

[04:06] on new in the upper left corner expert advisor should be automatically pre-selected and then then you click on next, you add a name for this. We can say, Sculping strategy or moving average MA Sculping strategy, then you click on next, next and finish. So this will open a program for you. This is like the raw structure for a meta trader expert advisor. And the first lines like these gray lines, they are just comments. they have absolutely no impact on the code and do not change the functionality. So we can just remove all of these gray lines here and it will make our code a little bit shorter

[04:51] and it's easier to have a overview in my opinion. Also, I like to rearrange these curly brackets a little bit. So this like the style that I use and what I'm used to. So now we have three lines here at the top. These are just properties. So if we compile the program now, click on compile here, this will take this .mq file file and turn it into a EX5 executable file. So once you compile this program, you can go back to your Metatrader, you can then go here to your navigator in your Metatrader. If you do not see it, click on view navigator. And there you will find the program already. So somewhere here in the expert advisors folder,

[05:38] you should see MsGalping strategy or whatever name you chose. So I can just drag and drop it on any chart and you can now see the name. This is like the version, the version ID. This is the copyright text and there below this is the link. So these are the properties here. So as you can see, they are just like a information for the user and we do not really need it. So if we compile it again And if we activate the program again, these informations will no longer be there. So yeah, but the program is still the same. I can click on okay and it is already running here on the chart, but it doesn't do anything of course.

[06:21] So we will have to add some code. So what we have here now is we have three basic essential functions that any expert advisor have pretty much. This is the onDIC function, we have the onDNet function and the onIndian function. I exchanged on tick and on init. This is on init, of course, this is on tick. And these functions are always built like, we see it here. So the first word here, so this function, so let's talk about this like real quick. The first word or abbreviation is a data type. This is the return data type. So this function returns something which is a integer value And the short form for this is int,

[07:08] which is just any number pretty much like this. These are all integer values. Then we have the name of the function and we have the parameters. In this case, we do not have any parameters, but still you need to add these round brackets. And then in between of these curly brackets, you can see them highlighted here. You have the body of this function. So whenever this function is called, the body will be executed. And here we see this function returns something. We know this because of this int data type here. So we return if the initialization of the program was successful. And this on init function is called

[07:48] whenever the program is executed. So if it is attached to any chart or when the timeframe is changed, for example. Then we have the onDnet, the build or the structure of a function is always the same. We have the return value void in this case. case means that this function does not return anything. Then we have the name, which is on D in it, and we have the parameter. This is the reason for the D initialization. So whenever this function is called, the meter trader will provide the reason for the D initialization of the program. This might be that the meter trader is closed, the chart is closed.

[08:24] The EA is removed from the chart. There are several reasons and the code for this reason is stored in this variable. If you're asking yourself, where are these information coming from this is provided by the Metatrader. So all of these three functions are called by the Metatrader because there are system functions and they are in this violet color. So we know that these functions are called by the Metatrader. So whenever any expert advisor is attached to a chart and this chart is for example closed, the onDNet function for this expert advisor will automatically be executed. Same with the onTick function.

[09:01] The onTick function does not return anything and it has no parameters, but it is still one of the most important functions for any expert advisor because this function will be called whenever there is a tick in the chart. So whenever there is like this more or less possible price movement, the tick function for the attached expert advisor will be called. So since we cover the basics now, let's go ahead. First of all, we will have to calculate the moving average values. And whenever we want to do so, we will first of all have to define the moving average that we are talking about. So we will have to create a handle for this moving average.

[09:40] We can say handle MA fast or handle fast MA. This is just an integer variable and we can declare it like this. So we provide the data type, which is int integer. Again I said it already. are just any numbers. Like these are all possible integer values. But since we just declared this variable, there's no value or the default value zero stored inside of this variable. So we've write the data type, then we've write a name for this variable, and this could be anything, but it always makes sense to choose a name that pretty much describes what value should be stored inside of this variable. And we end the line with a semicolon.

[10:25] So now we created a variable. This variable can be used to store data inside or to receive data from this variable. So we can store data inside of this variable like this. So right now we stored the value 2 inside of this variable. Could be done, doesn't really make sense for our program. So what we want to do here is we want to create a, or we want to create, that was correct I think. We want to create a handle for a moving average indicator. So whenever you work with indicators in Metatrader 5, you first of all have to build like a description for this specific indicator. So indicators can, they can have inputs or different parameters like the periods, the

[11:13] method, the applied to price or the shift value. These are the parameters for this indicator. there are parameters like the time frame. So if we switch to a different time frame, the parameters or the moving average will look different. And if we switch to a different symbol, the moving average would look different again. So we would have to take all of these information and like store it somewhere in the memory of your PC. So we will have to like create a description for this specific moving average. And we can do so using the IMA function. So it's a small letter I and then a capital M and A. And this is a predefined function, which is part of the MQL5

[12:01] framework. So we can use it right away. And if you write IMA like this, and open the parameter list here pretty much, which you can do by writing this opening round bracket, you would see all the things we have to provide. And as I said to you before, a indicator is defined pretty much by the parameters it has and the time frame and the symbol. And this is exactly what is requested here now. So you can see first the symbol name is requested, then the time frame, then the moving average period, the shift value, the method and the applied price. And this This is exactly like symbol, period, and all the parameters of this specific indicator.

[12:45] So what we do here is, we can say, for example, euro US dollar like this. This would be like the symbol. And you always write text information in these quotation marks, so the PC knows it's a text and not a variable, for example. And this could be done. This would work and we would use the moving average or calculated for the euro-US dollar always. But since we drag and drop our program on a chart, we can just use the chart symbol because there is a variable which is underscore symbol. In this variable, we did not create it on our own. It is predefined in the MQL5 framework and it will always hold the value for the chart

[13:38] where the expert advisor is attached to. So in this case, this variable would hold the value Euro US dollar as a tax value. So we can just use this instead of Euro US dollar. And when we then run the program on the US dollar Japanese yen chart, for example, it would also work without any problems. The next one is the time frame. And we could choose a fixed time frame like the M1 or the M5 chart, but we could also say we want period current, which is a predefined identifier and it will always use the current chart time frame of the chart where the expert advisor is attached to. So now we would use the M1 chart.

[14:25] And if I change the time frame, we would automatically use the M5 chart. So this is a way of easily providing the current chart time frame. Then we have the period, the shift, the method and the applied price. And here we just go with the values that are used for these indicators. So we have 10, then we have the shift, zero, we have mode, SMA, which is the simple moving average, and we have the applied price, which is price close. If you're asking yourself, how do I know all of these identifiers here? For example, price close. This is a part of a enumeration. So this is predefined in the MQL5 framework.

[15:05] And if you are questioning yourself, where do I find all of these information? You should have a look at the reference or the documentation. So if you move your cursor inside of this IMA name, function name and press F1 on your keyboard it will open the reference for you. Also you can click on tools and no on help and mqualify reference and it will open the IMA function documentation. You can also just go here on the left side click on index and right IMA and you will also find the function. And here you can read pretty much everything about this function. For example, you can see here all of these parameters are described and you can see what

[15:55] values you can put in each of these parameters pretty much. So you can see for the symbol, you can use the name of the security you want to trade or you could provide Nile, which means the current chart symbol. This is pretty much the same as writing underscore symbol. So I could also go ahead and write Nile. It would be like completely the same result. And then you have the period. And here you can click, for example, on this enum timeframes. It is a link. And here you can see all the possible values. Same for the M.A. method. Also, you can see all the values and their description here. So this is like a process you can use or follow if you want to know what parameters

[16:41] are possible and what they do. So now we created this, now we use this IMA function to define a specific moving average. So all of these information they are used to define one specific moving average. And this moving average is unique pretty much. So it's like a specific moving average. And this IMA function it returns a handle. You can read this here. And the handle is pretty much a, this is a concept in metadata 5 programming. So a handle for a moving average means pretty much that it is just a reference to some point in the memory of your PC where this specific description for a indicator is stored.

[17:39] So if we now use this value that is inside of this handle fastma variable and if it is interpreted by the PC or the Metatrader SA handle, he will know at what specific spot in the memory of the PC, he will have to look for this specific moving average description. So this is why we can also go ahead and create two more handles here with the exact same process. So let me just copy this line two times. So paste, paste and paste. And now we will not only initialize the fast MA but also the middle MA and the slow MA. And now all we have to do is we have to change these values here because this is like the

[18:36] periods. The rest can stay the same because we all we always want to use the simple moving average and we want to use the close prices of the bars to calculate it. So now we have three handles stored in these three variables. And since these three variables are declared on a global scale, so not inside of any function but on a global scale we can use them in any function. So we can go all the way down here to our antique function and we can now say we want to receive the indicator values. So now since we have the description for these three specific indicators, the moving averages, we now want

[19:18] to receive the values for them. So we will have to store these values somewhere in some variables. So for example we can say we want to have a value stored in this fast ma variable. The data type should be double here, which could be any number pretty much that has a decimal point. And we will have to use a double value or data type and not the integer because we want to have prices and prices usually have like decimal points and digits after the decimal point, right? So yeah we will use a double data type and now we will use the copy buffer function. This is another function which is part of the MQL5 framework. You can

[20:07] read about it in the documentation but it's a little bit confusing if you never worked with programming and data series. So I will explain this in my words, I think. So the copy buffer function can be used to receive data from a specific indicator that is already explained to the PC pretty much and stored in a handle. So you can see here the first requested parameter is a handle. So we can just say we want our handle for the fast moving average as a first parameter. So this function now automatically knows what specific indicator we want to receive the data from. And this is why working with the handle is so convenient.

[20:55] And now we will have to provide the buffer number. This is pretty much just what specific data series you want to receive from the indicator. So if you are thinking about a bollinger bands indicator, for example, which has like these multiple lines, you You will have to define if you want the upper line, the lower line or the middle line. But in this case we only have one line. And this one line is the first buffer which has the index zero or it's the main line. So you can write either zero or main line, whatever you like more. Then we will have to provide a starting position. And this is a really cool concept because when you look at a chart you will see

[21:39] different bars. you can see here in the data window on the left side, for example, if I use my cursor here for every bar, this moving average has a different value, right? So what you can do here is you can say I want the value of this bar, for example. So all of these bars have a index and the last bar always has the index zero. So this bar would have index 0, 1, 2, 3. So by providing a index, you can specify a specific bar. And if there would be a new bar, the new bar would have index 0 and this would be index 1. So if we want to have, for example, the value of the last two bars, we will have

[22:29] to say we will start at index one, this bar. And this is what we will do here. So we will start at bar one, this is our starting position. And now we have a count and the count is two because starting from this position here, we want to have two values, this value and this value. And these two values shall be stored in an array. And this array is the last parameter and this is our fastma variable here. So now if we compile this there's an error and this error says that there's something wrong here. And indeed there is something wrong because we set here that we We want two values. This is why we set two for the count parameter.

[23:24] But in a variable, we can only store one value, right? Because if we store one value in this variable and then another value, the first value will be overwritten pretty much. So this is why we cannot just provide a single normal variable, but we will have to provide an array. And this is why this parameter name says double array and then there are these squared brackets after the double array variable name. So we will have to make this an array and you can do this by writing these squared brackets after the variable name. So if we compile now the error is gone. So what does this array mean? So we use this array to fix the problem that we cannot store multiple values in a variable.

[24:14] But since we have an array now, which says pretty much that we want to store multiple values in this variable. So we are using an array and this array has an indexing mechanism pretty much. So what we can do is we can store two values in this array and we can now use indexes indices, I don't know how to pronounce it, to receive the data. So what we can do here is we can say print or we can say comment, which is even better. And then we write, so a comment is in another function and it will write a chart comment in your chart. We will see this in a second. And as a parameter, we can provide the text.

[25:01] This is a text, for example, that should be printed. So if we close this and write at a semicolon and compile this, you can see now in our chart in the upper left corner, there will be written, this is a text. So I want to write the moving average values here. So instead of writing, this is a text. I will say something like fast ma and then I will say or something like yes fast ma at index zero of this array. So fast ma at index zero colon and then I can write a comma and then I can write a comma to add another text that should be printed as a chart comment. I can now say double to string, or let's just say fast MA at index zero.

[26:00] And if we compile this, you will now see that we print a moving average value of the fast moving average in the upper left corner of the chart. So let's have a look here. So we now should have these two values here stored in the array. And this value for the current bar, not for the last bar, but for the previous bar should be the value that is printed in the upper left corner. And if we compare these values here in the data window and in the upper left corner of the chart, this is indeed correct. Now you see there is a new bar created. So now it is this value for this bar. And yeah, this works.

[26:37] So if we now also want to have the value for this bar, we will also have to print index one of our array. So we can say, we want to print even more. So like fast MA1, and we say fast MA at index one like this. So if we compile it, it should work. And now we will have these, both of these values here, printed in the upper left corner of the chart. This looks really ugly because there are a lot of digits after the decimal point, and we can fix this using the double to string function. This function will take a double value, like the value stored in this array and will turn them into string text values

[27:19] and we can define the amount of digits after the decimal point. So in this case it's five for five digits of this moving average or we can just say we will want to have the amount of digits after the decimal point for the current chart symbol which is stored in a system variable called underscore digits like this. So if we do the same here for the second value underscore digits like this, this should look just fine. And we can also maybe rearrange this a little bit so it doesn't look, it doesn't go too far to the right. So now it looks a little bit, looks more beautiful in the chart here. And maybe we can even add like something here

[28:04] like this. Okay, so now we did this for the fast moving average and what we can do here is we can do this for the middle MA2 and for the slow MA, like this. So we just write the copy buffer function three times. Now, this is like the most important thing. We choose a different handle because this is the way of telling the PC what specific moving average we want to calculate here. and then we store the values in the matching array. And this is all we have to do. So now we can just go ahead, add some more lines for our comment here, and say for example, middle MA at index zero is middle MA array at index zero, and, oh wait.

[28:57] Oh wait. Yeah, we can jump to a new line here by writing this backslash n. And now we can also write this line one more time, middle m a. I'm just printing the values in the upper left corner of the chart. This is always a good idea, always something I like to do when I work with indicators in an EA. So I know that I didn't mess up anything and that I calculated the indicators correctly. So always print the indicator values in the chart so you can compare it to the visual indicators that you see. So once we did this, we can compile and we will see all of the values in the upper left corner of the chart.

[29:52] And yeah, I think this should be correct. I do not want to compare it, but this should work. You can compare it on your own PC. So once we did this, we can already start building like the conditions for our trade, right? So what we do here is this is like really easy. We now have to compare the close price for the bars, for the last bar and the previous bar with the moving average radius. So first of all, we will have to get the close prices. So we will need double variables for this. We can say close at index one, for example, and we can say this will be the return value of the I close function. This is another function,

[30:40] which will return the close price of a specified bar in a specific chart. So we will have to provide the symbol which is underscore symbol for the chart symbol then we say period current for the current chart and then we will have to provide a index as a shift value and this is again the same procedure as with the moving averages we start at the very right side this is index zero and since we want this bar we will provide index one and we also want this bar so we will also do the same with index two so we just copy this line and write close two is equal to i closed for a simple period current and for shift value two. So now we

[31:18] have these two values we could also of course print them here so make sure to modify your comment and we can say something like close one and close two like this and we just print these values here. We do not need the index now because this is not an array this is a single a variable, a normal variable where we can just store one value inside. So now we also see the close prices for the last two bars here in the chart comment. And now we can like just compare these values to figure out if we have a bicycle. And this is like really easy now. What we can do here is we can say, first of all, we want to do all of these calculations

[32:09] only once per bar, right? we do not have to do this with every single tick, like the on tick function would do naturally. So we can do something like figure out if there's a new bar in the chart. And this is, that's an easy way to do this. So we create another global variable which is named bus total for example. And then we will say in, in, uh, yeah, we should initialize it in the onInit function. So we can say, barstotal is equal to Ibarse, underscore symbol, period current. So this Ibarse function, so this barstotal variable is just another global variable of type integer. So we can store values inside that doesn't have a decimal,

[32:57] they do not have a decimal point. And we can initialize it. So put a value inside of this variable for the first time here in this unending function. So just write the variable name equal sign and then we will put the return value of this I bus function inside of the variable. And this function is really easy. It just returns the number of bars of the chart. So we will have to specify the chart using this underscore symbol and the current chart time frame of the chart where the program is running. So we will get the amount of bars that are currently in this chart. It's a big number like 10,568.

[33:36] And now we want to check if the next bar is just coming. So in the antique function we create another variable which is called bars. And we will store the same value inside. So pretty much the return value of this IEBars function. So just copy it or just write it again. And what we can do now is we can check if bar's total is smaller than this bar's value. So what we did here is we use an if statement. And if statement is built like this. You have this word, if, which just says that you want to use an if statement now. Then in round brackets directly following the if, you will have a condition.

[34:26] And this is usually a comparison of values, for example, which you can do using a comparison operator like this, smaller or greater or equal, something like this. In this case, we want to check if the value stored in this bus total variable is smaller than the value stored in this bus variable. And now there's one important thing. Since this bus variable will be calculated or updated in the on tick function, and this bus total variable is not, this is just initialized once, once in the onInnit function, and then the value will stay the same until we change it. So we can check if there are no more bars in this chart

[35:14] than there were before. So in this case, if this is true, then we will update the value in this bus total variable, And then this will not be true unless there's a new bar. So one more bar. So this is a way to make sure that everything inside of this if statement is only executed once with the start of a new bar. So what I do here is I take all of our code, I cut it and I paste it inside of the body of the if statement. Then I select everything again and press the tabulator tabulator button on my keyboard to move it a little bit to the right because now you can see everything is beautifully aligned inside of the body of this if

[35:59] statement so we can see what is part of this if statement so you you will have to create like this beautiful structure so it's easier to read like the code is easier to read and understand I mean I could also do it like this there's no problem this doesn't cause any errors or so but this is ugly and not easy to read. So let's move it a little bit to the right so it looks beautiful and crispy clean. So now we do this once per bar. And since we have all the values now and we already learned how to work with if statements, we can now use these values and the knowledge to build a by signal structure here. So what we can do here is we can check if the fast moving average

[36:49] value for the last bar, which is the value stored in this array at index one, if this is greater than the middle MA at index one, and we can combine multiple conditions using this double and sign and we check if the middle MA is greater than the slow MA because this is the sign for us that we have a by trend. And what we want to do now is we want to add another if statement and you can do this as often as you want. So inside of the body of this if statement we will put another one. So we will check another condition after this is true and only if If everything is true, we will get to the body of the inner if statement.

[37:33] So we will check if the close price of the last bar is above the value in the fast m a array at the last bar. we want to check if the previous close is below the previous value in the fast moving average array. So only if these things are all true, we will print something for example by signal, usual trade, something like this. And we can now take this complete block and just copy it, paste it here and we just turn around some of these comparison operators and if we turn everything around this is a cell signal. So in this case we are checking for a cell signal. So if we compile this now and go to the metadata and go to view strategy tester then go to overview visualize select

[38:45] the expert you want to test. In this case, we will test the MA Skyping strategy. And then you can select like any symbol you want to test. I will just go with URU or SOLAR, any time frame you want to test. Let's go for M5. And then you can just go and click start in the lower right corner. And this will open a back test for you. So this is a simulation with historical data of this program. This is pretty much just like live trading, on historical data. So you can check if your program is working. So you can see we already have several by-signals and we see them here in the journal. So let's check this out. So the last one was at 8 o'clock here and this is indeed a

[39:33] by-signal. Everything is aligned. There was a close below the fast moving average and this was a close above the fast moving average. So we have the buy signal here would be a bad signal would be a loss but still correct signal. The signal before this was the same day for 25 so let's also check this out for 25 should be this signal and it is true there is another signal previous signal was at 355 should be this one yes also true so you can see this a program seems to work. So let's check the same thing for like sales signals. And I think there's a sales signal coming up right now. If we close below the fast moving average

[40:25] here, this would be a sales signal because everything is aligned and nope, it is not giving us the sales signal information. No, there is the information 1050. I think This is too late, isn't it? Oh, no, it's not. We did not close below the fast moving average. We just did it here. Okay, no, signal is correct. Everything is working smoothly as intended. And this set signal would be great. I think we would hit the take profit. But since we are just writing something in the journal not actually trading, we should fix this as a next step. So the easiest way to trade automatically in META Trader 5

[41:13] is to use a predefined class for the position opening. And you should go to the very top of your program and write this hashtag include and then a smaller sign, then trade slash trade dot mqh, a greater sign, and compile. This will include an additional file to your program. And this file is a so-called class or this file contains a class. And this is kind of complex stuff. If you never had a look at programming, again, if you want to learn like programming, like everything like with even more explanation of the background, you should check out the link in the description and you will find the complete course.

[42:04] But in this case, you just have to know that you can always write this in a program that you are writing, and then you can create object variables of the class C trade. And this is what I want to do here. So I want to write C trade. I will give a name to this variable, which is just trade in this case. You could choose anything like dog cat doesn't matter. It's just a name for a variable. So this also works as you can see here, but trade makes more sense to me. And this is now a so-called object variable. So this variable can be used to open positions. So we can go to the part of our code where we want to open a by position.

[42:45] And here we now say trade, then there's a point directly following the trade. And now you can see a lot of recommended functions. And the first one is the by function. And we can use this function and compile and we'll have to provide a parameter which is the lot size that we want to trade. And we can say something like 0.1 lots for example. And if we compile this, this works. Now we go to the part of the code where we want to open a sale position. So here we trade, say, trade.sell 0.1, 0.1, close the brackets and put a semicolon on compile. And if we run the exact same back test again now in the mid-rater five,

[43:38] you can see whenever there's a buy signal, there's also a buy trade. And if there's a sale signal, there's also a sale trade. So this is like really cool, I think. Yes. So some of these sale rates would have worked. Some of them wouldn't, but you can see we are trading. We're opening a lot of trades, but we're not closing these trades. So that's another thing we will have to work on. So if you can see, if you put another comma after the lot size, the volume for this buy and sell position, you can see there are some more optional parameters and they do not have to be filled because they have default values,

[44:24] but we can fill them. So we can say we want to trade the current chart symbol and now we can add a price, a stop loss and ATP value and this is great because these are the values that we will now provide. So what we want to do here is we want to have the price first which is for a market buy position usually the ask price of the current chart. So we can say symbol info double. This is a function which can give you properties of pretty much any symbol in your metadata five. You will have to provide the symbol first, which is underscore symbol because we want to have the chart symbol in this case and in 99% of all cases,

[45:07] if you're working with expert advisors. And then we can say symbol underscore ask to say that we want to have the ask price stored in this variable that we just created. Now, as a next step, we will, But first of all, before we do anything else, we will round the value. And you can do this by saying ask should now be equal to normalize double, ask and then comma underscore digits. We know this underscore digits already because we use it here. So this is just the amount of digits after the decimal point for a for the current chart symbol. And this normalized double function, it will round a number to a specific accuracy.

[45:50] So we will take our value and if there are more than five digits, we will cut everything after five digits. So this is what the normalized double function does. And what we do next is we want to calculate the stop loss. And this is really easy because the stop loss should be the middle ma value at index one. Again, we should round this. So there are no problems with any unwanted numbers after the decimal point. And what we do next is we calculate the take profit. So take profit and now we need the distance from our entry to the SL. This is really easy because we will just add this to the ask price.

[46:32] So we say ask plus ask minus SL and now we have ATP factor, which could be something like 1.0 or 1.5 or 0.5, whatever you like. And then we should of course around this because with multiplications and everything that could always be some unwanted numbers after the decimal point. Since we did all this now we have these three beautiful variables that have the values that hold the values for our entry. So this is the entry ask then we have the SL, SL and TP. And these are just the variables that we just created. So we are providing these values to the buy function to tell this function pretty much

[47:22] where the SL and TP values should be. So now let's go ahead and copy this block and print it here before we open a sale position. So I want to print this here or paste it here and now we will make some adjustments. So first of all we do not open sale positions at the ask price but we will use the bid price instead. You cannot only change the variable name. You will also have to change like the identifier here because this will now give us the bid price. And if I compile this, this will not work because as you can see ask is an undeclared identifier. This is because we changed the name of this variable and a variable always only lives

[48:06] in the scope where it is created. And since this variable or since this ask variable is created in the scope of the body of this if statement, it only lives here. If the program leaves the body of this if statement, this ask variable will always be deleted. So here this ask variable is not known anymore because this is now called bit. So if I write bit, this is, this is okay again because this variable is known. But still there is a problem because I use R here and here again. So we will have to change the calculation, so we will have to write bit minus and now we need the distance from the SL to the open price and we multiply this with the TP multiplier.

[48:58] And this is it. Now we only have to add some more information for our cell function which is the symbol, the open price, the SL and the TP. And if we compile this, this should be working just fine. So let's run the program one more time. And we should see whenever there is a signal, you can see these traits now FATP and ASL and the SL for this position, for example, you can see the position is opened here there was a close below the fast moving average the SL for no this was last position I think so the current position was opened here I think wait this closed below yeah so here the position was open the SL is placed at the

[50:04] moving average of the last bar and the TP is 0.5 times the distance from the entry point to the SL. And yeah, now we just wait if we hit TP or SL level and then we can also wait for more signals. Oh, you can see there were three signals. Yeah, I mean it's like you will have to think to figure out if you want to open multiple positions like here. Or if you want to say like, I only want one open position. And if there was one open position, I do not want to open more positions. I think both is valid. You can do everything what you want pretty much. But in this case, yeah, we open multiple positions.

[50:50] But of course you can always make adjustments like this to your code. If you do not want to open multiple positions like here. In this case, everything worked out and we had multiple take profits, but this could also go wrong of course. And yeah you can just test around with this concept and you can see like right now we are making profit but the equity is below the start but yeah you can just play around with it and check if you find some profitable settings here there were some some wins. Okay, so and whenever you want to change something on the system you can change for example this TP factor you can say I want 1.0 instead of 0.5 and this will change the system of course.

[51:43] And yeah also if you want to have only one open position you can say that like before you check the position opening you can say if positions total is equal zero like this and then you would just have to wrap everything here inside of this if statement like this do not forget this closing bracket which is a curly bracket and if you compile this and run the program again we will no longer see multiple positions open at the same time but only one position so at this point here for example, where we had three positions before, we now only see one position. Yeah, so you can see this is how you can make some adjustments through a program.

[52:33] And this is how you can write this like easy skyping strategy. It doesn't seem to work like for the first few days that I tested here, but you should test it in a longer setting or maybe change even the stop loss or take profit calculations here. And also I mentioned before you could do the same thing if you hit the middle line and in place the SL for example at the slow moving average. So this would also be a possible modification of the program and maybe with the things you learned in this video you can do it on your own. You can just copy this whole block pretty much and then make some adjustments to the conditions for opening the positions and then

[53:23] some adjustments especially at the SA calculation because in this case you want the SA at the slow moving average. So yeah this is how you can build a really easy Skyping strategy and you can use this program in any market in any time frame and this is because everything is so is programmed like like it is also you can change of course these values here and it will just give you some different moving average moving averages that you are working with also if you want to learn how to make these programs safe for live trading and how to like yeah write everything that you want check out the link in the description you will find the complete course. You will learn a lot about

[54:09] MetaTrader programming. It's absolutely suitable for complete beginners. You do not need any knowledge upfront and you will learn everything like variable status structures, operators, everything like step by step and really detailed and well explained. So I hope you like this little programming tutorial. Let me know what you think in the comments below as always. And in the end, nothing left to say but thanks for watching and I hope that you can use this and good trades for you. Yeah, I will see you next time and then have a great time. Bye.
