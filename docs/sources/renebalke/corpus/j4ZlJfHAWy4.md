# How To Use The MT5 Strategy Optimizer (EA Optimization Explained)

- URL : https://www.youtube.com/watch?v=j4ZlJfHAWy4
- Date de publication : -
- Vues : 31 000
- Duree : 19:40
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey there, Seney. Welcome to another video on this channel. And in this video, I want to talk about the optimization in the MIDI Trader 5. I already made another video about the Tester, which is just a normal and single back test, which I can show to you real quick. So wait, let me take one more step here. So for a normal test, you just open the Tester, which you can find clicking on view in the upper left corner and then strategy tester or control r on your keyboard then you can choose one of these methods. We will have a look at the optimization later but first of all we will have a look at a single back test then you can choose

[00:43] to have a visualized back test. This is great because I can show you the strategy I'm using for this back test. Then you can make several settings for example you can choose the expert advisor that you want to test. An expert advisor is just an automated program in MitterTrader5. You can choose the symbol you want to test, the time frame that the main chart shall be and you can choose a date. You can choose for example the last year or a custom period. I chose a custom period and filled in the dates for the 1st of January of 2021 and the 31st of December of 2021, so the complete year 2021. Then we have a modeling method, which we can have

[01:41] I explained everything about this in the complete video about testing in M2.5. You can search for it on the channel. And then we have optimization disabled for this backtest. And then we have several inputs. And this is a really simple program. It just trades the crossing of two moving averages. So it is a very well-known strategy. And I can show it to you here real quick. So if I start the test, you can see there is a visualized back test starting at the first of January of 2021. And you can see there is a fast line, which is the lower line in this case, and a slow moving average, which is the upper line.

[02:27] So the fast moving average has 20 periods. The slow moving average has 40 periods. And if these two lines cross, we will see it here. So if the fast moving average reaches above the slow moving average, then we enter a buy position. And this is what the program does. And we have ATP enter SL and then we simply wait until we hit one of these levels. And I can speed it up as I want using this thing here at the top. And yeah, this is pretty much just a simple back test. So this was important to show you the strategy that I'm using here. And then you can have a look of course at the graph, so at the performance.

[03:16] And if this back test is finished, so if the complete year is processed by the program, then we will have an overview over the resets. So you can see at one glance if this was profitable or not. And in this case, you can see this was not profitable. We made a small loss. So the next step would be to think about this strategy and especially about the input parameters. So you might question yourself, what happens if I increase the take profit distance? For example, if I choose 200 points instead of 100 points, and then you could adjust this single parameter and you could start the test again. And then everything would be the same pretty much.

[04:10] The test is simulated for the year, for the period that you chose. And you can see the graph again. Everything is pretty much the same. you would see in this case, the result is, yeah, there's not a big difference. It is a little bit worse than before. So you could go ahead and change the parameters again. And you can see this is a pretty time consuming method of optimizing a strategy in MetaTrader 5. So this is why the MetaTrader 5 has a built-in optimizer. So you can use this optimizer by opening the strategy tester and this is still open. So we just go to overview here and then we can choose complete optimization.

[05:00] And this opens the same form that we used before for a single back test. And the only difference is that we have this optimization mode pre-selected. So if we change this back to disable and take the visual mode here, this would be exactly the same that we did before. So the optimization is just pretty much, yeah, one of these. So you can choose a slow, complete algorithm. You can choose the fast, genetic-based algorithm and all symbols selected in MarketWatch. So for this video I will explain the slow complete algorithm because this is the method that I use for my optimization. And then you see this drop down menu also

[05:56] changes because now you can select what key figure pretty much you want to optimize. So the pre-selected key figure is the profit factor but you can change it to one of these. But I would always go with a profit factor because it pretty much describes the profitability of your program better than, for example, just the balance maximum. Because the profit factor pretty much considers the winning trades and the losing trades and sets them into a relation. And yeah, this is why I like to go with a profit factor. And the rest is pretty much all the same that we had it before with a normal test. So you can choose the system

[06:42] that you want to test. Of course, I would choose the same system. You will have the possibility to choose a symbol and a date and yeah, even the modeling method here and the initial the initial deposit and the leverage. So this is all the same. But there is a small difference when it comes to the input. The inputs, because we do not do a single test, but we do a complete optimization. So what I can do here is I can tick all the parameters that I want to optimize. And you can see as soon as I tick the checkbox in front of the name of this parameter, this value column here is no longer available.

[07:29] So if I take this, I can no longer change this 100 value. But what I know I'm able to change is the start, step, and the stop value. And what I can do here is, I can say, for example, I want to start at 100 for the TP points, And I want to have a step of 50 and I want to end at, for example, 500. And I can do the same with the stop loss. For example, 100 as a start, 50 as a step and 500 as a stop. And what this does is it makes several backtests now because the first backtest will have 0.1 for the lots, it will have the current time frame, it will have the simple moving average, the close price and 20 and 40 periods for the moving averages.

[08:24] And these parameters do not change for all the testing because we did not take the checkbox in front of them. But we did take the checkbox for the TP points and the SL points. And this is why the testing will optimize these two parameters. So the first test will have 100 for the TP points and 100 for the SL points. And then the second test will have 150 for the TP points and 100 for the SL points. So we increase the starting value of these TP points input by the step value. And then the test after the second test it will have 200 for the TP points because we increase the value by 50 again and then we will have 250 and this goes on until we

[09:13] hit 500 and as soon as we hit 500 it will then drop back to 100 and then we will increase the SL points by 50 because we always chose, we also chose to optimize this parameter. So you can see all the parameters that are ticked before the input variable name, these parameters will be optimized. And you can see the optimizer, it will combine all the possible combinations. So we will test 100 and 150, we will test 150 and 100, we will test 200 and 100, we We will test 200 and 150, we will test 300 and 100 and 300 and 150. So all the possible combinations will be tested. And this is why it takes nine steps from 100 with a step value of 50 to reach 500.

[10:16] This is why we have nine steps here displayed at the end of this row. And same goes for the SL points. But since we optimize all the possible combinations, we will have nine multiplied by nine possibilities, which is 81. And if we would choose to optimize another parameter, for example, we can choose to optimize the moving average method. And you can see we have four different options here. So we will optimize from simple to linear weighted. And these are four different inputs. So we have to multiply 81 by four. So we already have 324 possible combinations. And you can see this is getting bigger exponentially.

[11:09] So if I, for example, also choose this parameter, we will have 2,268 possible combinations. And this is a lot. So it takes a lot of, a lot more time if you choose several parameters and if you have a lot of steps for every single parameter. So this is why, yeah, it pretty much depends how much power your PC or laptop has. And sometimes it can take a lot of time for optimization to finish. But let me demonstrate this optimization using only these two parameters here. And I can simply click on Start in the lower right corner. And this will start the optimization. And we will not see a visual display of the optimization

[12:05] because all of these runs are done in the background. And you can see there's a new tab that just appeared. And this is the optimization results tab. And as soon as the first optimization run is finished, we will see the result in this optimization results tab. And you can also see the agents, which is pretty much just the power that your PC has. And you can see how many tasks already passed. So we should already see some optimization results here, since we see some of these tasks passed already. And what we can also see is in the optimization resides, we can see that the resides are listed by the profit factor,

[12:55] because this is what we chose in the settings. We chose that we want to optimize the maximum profit factor. Yeah, and you can see there are several other things that we can see here. For example, we can see the profit factor. We can see the overall profit, the amount of trades, the expected pay off per every single trade, and also the drawdown. And then at the right side, and this is pretty much the most important part of this of this window here, we see the TP points and the SL points that were used to achieve this reset. So at the top you can see the most profitable settings right now for this strategy, for

[13:45] the symbol for the year 2021. And this means that you could have chosen these settings to achieve this reset in the previous year. And we can see it for all the different combinations and this is what is great about optimization because we do not have to do every single back test on our own. We can just set up a optimization and we could have for example 2000 runs and we could just set up the optimization. We can click on start and we can leave the PC for three hours and the PC does all the calculations for us and we do not have to be active in these three hours. And yeah, this is a great way of optimizing a strategy.

[14:28] And you can, yeah, what does this say? This pretty much just says that these settings would be, would have been the best for the previous year for the symbol. But, and this is really important, this does not mean that these settings have to be the best for the next year. So even if you do a optimization, you should always be aware of this fact. So there is a term called over-optimizing a strategy. This is a big trap that you should not fall for if you're optimizing because these settings, they do not have to be the best in every market condition. And you can see if you compare these optimization results at the top, you can see these settings

[15:15] are pretty much completely different. So you can see the settings can be completely different, but the results can be really, really good. So it doesn't have to be like a fundamental good setting for this strategy. It could only mean that in this specific year, for example, these settings work really good, but in the next year they might fail like really hard. So what I always like to do is I like to look at the results and I search for cluster areas pretty much. So I search for things that are really similar. So for example, you could say that, for example, if we have a look at the really bad resites

[16:03] here, so these are the results that made a loss. And you can see the settings for the TP points and for most of these tests for the SL points, they are like really low. So this might tell us that it doesn't really make sense to choose a TP of only 100 points. And if we have a look at the good resides, we can see that pretty much most of these resides have a really high input for the TP points. So it would be a general thought here to choose a bigger input for the TP points end or there is eight points. Um, yeah. So this is like a way of optimizing your strategy. Just choose one or two parameters, um, optimize them, uh, them, and then think

[16:55] about it. Uh, does it make sense to you? Is it maybe just a coincidence that worked so well in the, in the, in this single year and then you can go on from there. So you could choose, um, another year and test it or you could change or optimize even more of these parameters. But what I want to show to you is if you have this optimization, you can just double click the settings pretty much. And I think if you double click it, it automatically starts a back test with these inputs from this specific optimization result. And then you can have a look at the resides here. And yeah, for example, we could also change the modeling to every tick based on real ticks,

[17:44] which is a even better modeling method, but which of course also takes more time. So yeah, this will take more time. But yeah, you can you can just take away from this video how to do a optimization. And yeah, you can do this with every expert advisor pretty much that has inputs. And yeah, this is a great way of optimizing your strategy. I would always suggest using the optimizer, especially if you just start working with a new strategy, you can just set up a optimization with, for example, 2000 optimization runs. And you can just have a look at it in the end, search for cluster regions pretty

[18:29] much. have a very good understanding for your strategy and you will see what can work and what maybe doesn't really work. So this is a good way to start optimizing a strategy and then after optimizing I would always recommend having a look at the performance again using is it in a single back test like, uh, like I just did here. And then you will have a look at the single back test and see if this back test verifies the results. And, um, you can have a look at the graph then. So at the performance graph, and you can see if it was only like one event, one month, which boosted the performance, or is it like a consistent,

[19:21] uh, profitable system? So hope you learned something about optimization in MetaTrader 5. Let me know what you think about it. And yeah, I hope you will see each other in the next video on this channel, which is about automated trading in MetaTrader 4 and 5. So see you next time. Bye.
