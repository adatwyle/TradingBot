# I Coded This Scalping Strategy From Scratch — Here's Every Line

- URL : https://www.youtube.com/watch?v=vFlFmWKx6Zs
- Date de publication : -
- Vues : 50 000
- Duree : 104:28
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is Rene. It's been a while since I published my last video on this channel, but the time has come and I will record another programming tutorial for you guys today and publish it on this channel in English and I have another channel in German where I published more videos in the past but for my English viewers, I want to do another programming tutorial and this time it will be a a tutorial for a strategy that I found on YouTube and it is quite famous. Maybe you already saw this video, it is from Forex Signals TV and it has over one million, nearly two million views and it says in the title, Simple and Profitable Forex Skyping

[00:54] Strategy. And the person who introduces this strategy also explains it in detail. So it will be easy for us to listen to his explanations and convert it into a fully working expert advisor for the Metatrader 5. If you are interested, feel free to watch the whole tutorial. I will try to explain everything very slowly and in detail so it will be easy to follow for you even if you are not an experienced programmer or if you are an absolute beginner it should be fine. So to start off I will briefly explain the strategy or I will summarize it. If you want the whole explanation from Forex Signals TV you can of course watch this

[01:51] video I think I will link it in the description below if I don't forget it so you can watch the whole thing and for my brief summary I will explain it showing you and a trading example in the euro-euros dollar on the 1st of December 2020 at about 15, 40 o'clock. So the strategy is based on two timeframes. And you can see it here in this picture. It's the five minute timeframe and the one hour timeframe. And we use the one hour timeframe as a broad trend indicator. So we have two moving averages on the one hour time frame as I did as I applied on this one hour euro-euros dollar chart. You see the blue one it's the faster exponential moving average with

[03:03] eight periods but I think the periods do not really matter. We will make them variable in the programming later. So you can choose either eight periods or 10 or 12 or whatever whatever you prefer. And we have a slow exponential moving average. The standard setting here is 21 periods and whenever the fast moving average is above the slow moving average it indicates an uptrend and if the fast or the blue moving average is below the slow or the red moving average it indicates a a downtrend. So in this case on the first December 2020 at around 15 o'clock, you can see that the blue moving average is above the red moving average and the candles are also above the

[04:03] blue moving average. So this indicates an uptrend. So now we have the trend direction and now we can switch to the 5 minute time frame and we can search in this time interval for entry settings. So for the 5 minute time frame, I have to zoom in a little bit. For the 5 minute time frame we apply three exponential moving averages. You can see it here in the video. We use a eight period moving average, a 13 period moving average and a 21 period exponential moving average. And all these moving averages will be applied to the chart. And you can see that they build a pattern. And if there is a clear trend, you will see that the fastest moving average is below the

[05:10] middle moving average and the middle moving average is below the slow moving average for a down trend and the other way around for an uptrend. So if it is like this or in my chart for an uptrend, the fastest moving average is on the top and then the other ones below that. We would search for long settings. So we wait until the price is above the 8 period exponential moving average. And if it drops back onto the 8 period exponential moving average, then we have an entry trigger And we replace an order at the high of the last five candles, which would be at this point here, because this is the highest of the last five candles. And then we simply wait if this order

[06:16] is filled, which will be the case in this candle. And then we have a trading setup. We also have a stop loss which will be I think he said it is three pips below the lowest of the last five candles 1 2 3 4 5 so it is yeah I think it's below this candle three pips below this candle I don't know if it's three pips here actually yeah it should be even lower like this, I don't know. And the target for the trade is a is one risk above the entry. So we apply this distance and sum it to the entry and we will have a take profit which should be around here and we can see that in this case the trade would be a success

[07:22] and we would hit the take profit. In this video he said some more things about a trading stop and a partial take profit I think. I will listen to it later but for now this should be enough to start off with the programming. So to write an expert advisor. We have to open the MetaQuest language editor and you can do it by either clicking on this little book or you can go to, I think it's view in English and wait, that was not correct. You go to extras and then MetaQuest language editor. If you click it, the MetaQuest language editor will open and it should look like this. On the On the left you have the navigator where you can see different folders, for example the

[08:17] experts folder. And on the bottom you have a terminal which will show warnings and errors. So if you want to write a new expert advisor, you simply click on new on the upper left corner. And then you click on next and you can give it a name, simple and profitable expert. But you can choose every name you want to. And you click on next, next and finish. And then this will auto create a expert advisor for you with three basic functions which I will explain in a second. First of all, I erase all these gray lines. These gray lines are comments which are only for the programmer to understand the source

[09:21] code, but it has no influence on the actual program. So the computer does not look at the gray lines and it does not have any impact on the functionality of the Expert Advisor. You can create your own comments by doing these two slash symbols and then you can write whatever you want in this line. It will not affect the functionality of the program and whenever I click on compile up here it will create the expert advisor and then you will be able to see and use it here in the navigator in your Metatrader 5. You can open the navigator by clicking control N like this. You can open and close it. So if you're in the navigator and you go to experts you can see that we now have

[10:18] an expert called Simple and profitable expert and I can apply it to this chart. And yeah, you can see I can click OK and now it is applied to this chart. So what I wanted to say is whenever I click compile, it will use this source code file and it will compile it to a working and executable expert advisor, which is an EX5 file for the MITREDRAID 5. And you can see that the computer does not care about this line because there is no error. If I do, if I write something like this without using a, without wrapping it in a comment, you can see that there is an error and I will not be able to use this expert advisor.

[11:16] So yeah, there are two ways to write comments. The first one is using these two slash signs. The other one is using a slash sign and then a, this little star. This is used to open a comment. Now I can write whatever I want in as many lines as I want. And then I have to close this comment by using this little star and another slash sign. So afterwards or before this, there will be errors because this is not wrapped by these, yeah, comment signs, but if I write it only between these two markers, there will be no problem at all. So that's it for comments. Yeah, we don't really need it. I mean, it's good if you plan to work on a file

[12:15] with other programmers. So they know what you did, what you thought writing this code, but for us, for this pretty simple program, we don't really need to write many comments. So, yeah, we don't need these two lines either need these two lines either because they only take care of the information displayed here. So for example, if I keep the lines, you can see that it says copyright 2020 Metacors Software Corporation and there is a link below this. But if I erase the two lines, you can see that this line is no-gun. But it doesn't really have any effect on the functionality of the Expert Advisor.

[13:07] So we don't need it. Okay, we could also erase the version number, but I like to keep it, because if I make any changes on the Expert Advisor later, I can increase this number and I always see what kind of, in which state this programming source code is. Okay, so if we want to start with our strategy, we first of all have to, we first of all have to calculate the overall trend situation. So we need these two exponential moving averages, the 21 period moving average and the eight period moving average on the one hour chart and to receive the value of an indicator, we can do it by, we first of all have to create a handle for this specific indicator.

[14:11] So we can do it like this fast. Let me declare two variables really fast. So what I did here is I declared two variables. So I told my computer, hey computer please make space for two variables in your... in your memory. And whenever we start the program, the computer will now go into its random access memory and it will make space for two integer variables. Integer variables are numbers like two, three, four, or like this. And it is always a number without a combata. So it is a number without a combata.

[15:20] And this indicates, or we will use this number to indicate a specific moving average, or exponential moving average in this case. And all this is done in the background. it is the metadata five and the MQL5 framework takes care of it for us. And we don't have to understand in complete detail what is happening in the background. But what we do know is that this will make space for an integer variable, so for a number, somewhere in the random access memory. So we can use this free space that we reserved for this variable and fill it with a specific value. So we do it like this. We can say handle trend ma fast

[16:16] and we can give it a value and we receive this value by using a function which is called ima. The i stands for indicator and ma is for moving average. So there is a predefined function in the MQL5 framework, which will help us defining a specific moving average. So this function has different parameters, which you can see if you open the parentheses after the IMA name. And you can look at all the parameters in more detail more detail if you click on anywhere inside of this function name and you press F1 on your keyboard. So this should open the reference and for me this is in German for you it should be in English

[17:13] or in your local language and here you can see a summary of all the requested parameters for this function. And you will have to fill in a value for every single one of these parameters. And this will help the emitter trader to understand what specific moving average you want to calculate. So in this case, we have to provide a symbol name. For example, in this case, it should be euro US dollar, or there is a better way to do this. simply write underscore symbol because this is a system variable. And if you look it up in the reference, you can see that this system variable will always store the symbol name of the

[18:05] chart to which the expert advisor is applied. So if I apply the expert advisor to this euro, your dollar chart, the underscore symbol variable will contain the value Euro, value, euro, euro's dollar. And because it is a text value or a string value, it is always wrapped in these exclamation marks. I think you can call them. So after the symbol, it requests the period. This should be the period H1 because it is the one hour time frame. And now we need the moving average period. The moving average period is for the fast exponential moving average. The... it is 8. As you can see here, it is an 8 period fast or exponential moving average.

[19:09] And we don't have a shift value. A shift value would be this. If I enter 5, for example, the moving average is shifted to the right for 5 candidates. If I apply a negative number like minus 10, it is shifted to the left. But since we don't need this feature, we simply add 0. So I will do it here and enter a 0. Now it requests the mode. You know, for moving averages there are several standard modes. And you will see them here. Simple exponentials moved and linear weighted. And I want to use the exponential moving average. So there is an identifier for this which is called mode underscore EMA. And if you look it up in the reference

[20:06] you can see all four modes have this identifier and you can simply enter one of these four identifiers here. And for the last I have to tell the IMA function what price I want to apply the moving average to. In most cases it will be the close price of the candidates. This means that this moving, exponential moving average will now calculate its value for the last eight bars. So this 1, 2, 3, 4, 5, 6, 7, 8. And it will use the close prices of the bars to calculate its value. Now we can simply copy this and do the same process for the slow trend moving average with one difference. We use a 21 period moving average.

[21:09] This is pretty good so far. What we did now is we declared these two variables. Whenever we start the Expert Advisor, it will immediately make place or space for these two values somewhere inside the Random Access Memory. And when we start the Expert Advisor, after that the onInit function will be called. You can see it. I add a print statement here. I can write something like onInit. A print statement simply means that whatever I write inside of these parentheses will be printed to the experts log, which you can find here. And you now see if I apply this expert to the chart, you can see that it says onInit

[22:11] because the onInit function is called whenever the expert advisor is initialized or started. And in this onInit function we use the IMA function to create a handle for this specific moving averages. So we do it with an 8 period exponential moving average for the one hour chart and we do it with a 21 period exponential moving average for the one hour chart. We store these handles inside of our newly created variables. So we can now use these variables which hold the value that identifies a specific moving average to receive the values of these moving averages, which we can do by using the copy

[23:15] buffer function. The copy buffer function is another predefined function in the MQL5 framework and we can do it as I will show you now. You can see if I open the parentheses of these functions, it requests a handle. And I can simply add one of my handles, for example the handle for the fast moving average. And I can now tell the copy buffer function what buffer number I want to request. And for the moving average, you can see it in the data window, which you can open by pressing CTRL D. You can see that both of these moving averages only provide one value. So it's only the value of the moving average at this specific candle where my cursor

[24:10] is at the moment. So the first and only value of these moving averages has the index zero. So the buffer number is zero. If there would be more than one values for these moving averages, there would be more buffers I could request. And I could also add a one or a two or whatever. But in this case, we only have one value. And this is the value with a buffer number zero. The counting here starts at zero. So the starting position for the moving average I want to read is let us say 1 and I want to receive three values for example and I want to store it in the moving average array. an array is simply a field of variables somewhere in the random access memory.

[25:18] And I declare it like this. We learned before if you declare a variable, you have to write the identifier for this variable, so the variable type, and then the name for this variable, and you simply end the line with a semicolon. And in this case I want to declare a field of double variables. Double variables are variables or values that are also numbers, but they do contain digits because they have a comma term. Double values are values like three dot, I don't know, this or 23 or any number with a commenter and digits. So because a moving average is made out of price data, which has a commenter, we can't

[26:21] use integer values, but we have to use double values. And we need a field or array of these values because we request not only one value, but we want to request three values. So we need not only one double variable, but we need more of them. And by declaring an array of double variables, which is indicated by these square brackets here, we tell the computer that we need space in the random access memory, but not only for one value, not only for one double value, but for more than one double values. And yeah, this is a way to show the computer that we need more space. So if you use the copy buffer function to store values inside of our moving average array,

[27:14] we can do it like this. The last parameter here is the reference to our array. So it pretty much is a pointer to this exact space in the random access memory where the data will be stored. And we can now use this to use this array to to receive the moving average values that are stored at this point in the memory of the computer. And we can do it like this. I can simply print all these three values on the chart in the upper left corner by using the function comment. You can of course read what it does in the reference. Yeah, it only says that it will write a comment in the upper left corner of the chart and it will look like this.

[28:10] You can see in the upper left corner we now have the last three values for this eight period exponential moving average. So this should be the first value and it says 1.206343 and you can see this is indeed the first number that is printed on the chart here and this should be the second one. Yeah, you can simply compare the second number with the number written here and it should be the same and this is the third value. So we managed to receive all three values of the last three candles for this eight period exponential moving average and this is a formula which you can apply to any predefined indicator

[29:06] in the metadata file like the RSI indicator, all the CCI indicator, all the SIA indicator. So there are many predefined indicators which you can read by creating a handle and then receiving the values. So what we do now is we want to take these lines and we want to move them to the on-tick function because this is a process that we want to do with every single tick that the euro-euros-dollar does. And we want to receive the current moving average value for this fast and for the slow moving average. So if I simply change the start position to zero, we will now receive the value for the candle at the index zero, which is always the current candle.

[30:13] Before this, we did it for the last three candles. Now we want to do it for the current candle because we want to have the current trend. And we only want to receive one value, so I can change this three to a one. And then I can print this moving average value to the chart. I can also say that this is the fast trend MA. And then I can use the double to string function to make it look a little bit better. And I can use it to only print the first five digits of this moving average value. This will look much, much more better. I will show you in a second. But first of all, I want to also print the value for the slow trend moving average by

[31:12] using the same method here. Trend slow. what I have to do now is I have to rename the variables since we have more than one now. And I also have to create another double array for the slow trend MA. And I can now use another copy buffer function to receive the values for the slow moving average. And I will store the values in the MRTrendslow array. Okay, if the error is gone, I can show it to you in the chart. MRTrendslow, ah, this is no array. No, it is an array. And now we can see that in the upper left corner, it prints the current value for the fast and the slow trend moving average. And if you compare the value in the upper left corner with the values in the data window,

[32:21] you can see that this is indeed the same value with the only difference that this is, and this shows six digits and this only shows five digits. Okay, so now we have these two values. What we can do now is, and I forgot to explain one thing, since we printed or since we wrote all these lines in the on tick function, it will be executed with every tick. So the on tick function is another predefined function in the MQL5 framework, just like the on init function, and it is called whenever there is a tick in the market, which the expert advisor is applied to. So you can see here whenever there's a tick, whenever there's a price change, a price movement

[33:15] meant everything will be recalculated and these numbers in the upper left corner may change if the moving average values change. So since we did this now, we can also identify the current trend direction. For example, I can use a integer value to identify the trend direction and I can use a if statement to check whether the MR trend fast value is greater than the MR trend slow And if this is the case, the trend direction is, yeah, we can say it is positive or it is an uptrend. And in the other case, it is a downtrend. And we can now print this trend direction, direction on the chart by extending our parameter

[34:22] in the comment function. If I do it like this, we can now see that it always states the current trend direction in the upper left corner. So in this case, we can see that this is an uptrend because the fast moving average is below, is above the slow moving average. In the video, this person from Forex Signals TV also says that it is not enough to check if the fast moving average is above or below the slow moving average. Because you also have to check if the current price or if the candles are above or below the moving averages. And we can do it by simply receiving the current price for the symbol.

[35:11] There is a pretty fine function for this, of course, to receive the bit price. And the function is symbol info double. name of this function is pretty easy to understand because it gives you a double information for the current symbol and to show the symbol you want to receive a double information for you have to provide the parameter for the current chart symbol and then you have a list of identifiers which you can use to or which you can choose one from to receive a specific value. For example if If I use the symbol underscore bit identifier written like this, it will give me the bit price as a result for this symbol info double function.

[35:58] And I can store this in another bit, in another double variable somewhere in the random access memory. Now I can extend my if statement here and add another condition. I can also check if the bid price is above the MR trend fast, above the fast trend moving average and for the downtrend I can check if the fast moving average is below the slow moving average and the bid price is below the fast trend moving average like this. Okay and now you would see there is no trend direction because the fast moving average is above the slow moving average, but the bid price is not above the fast moving average.

[37:07] So this is no uptrend and no downtrend. It is a neutral trend and we would not make any trades now. Let me explain this if statement real quick before we go on with the five minute timeframe. So a if statement is used to bring structure into your programming. And what it does is the following. You can use if as a keyword and then you open these round parentheses and you can write a condition here. For example, if 4 is greater than 5 or 4, so you add a condition and then you close the round parentheses and open these curly brackets and of course close the curly brackets afterwards. And this is the body of the if statement.

[38:00] And whatever is written in the body of the if statement will be executed if this condition is true. So I can say if 4 is greater than 4, print this is true. And when the computer is at this point in the source code and wants to execute the if statement, it will simply check if this condition is true, if it is indeed true, it will go into the body of the if condition and do whatever is written here. And if it is not true, it will simply skip the body of the if statement and proceed with the further programming code. And I can also extend this if statement by writing another else if afterwards.

[38:49] I can say if if is greater than five. Then I want to write something else. This is no true. And I can do this as long as I want. So I can write if four is smaller than five. This is now true. I swear. And if I write a if statement like this, the computer will check if 4 is greater than 4. It will see that this is not true. So we will check the next condition. If 4 is greater than 5, this is also not true. So we will check the next condition. If 4 is smaller than 5, and this is true, so it will print and it will only print this is no 2 as well and Normally you don't hard-code these conditions So if I write it like this, this will never change and with every tick that

[39:53] That happens it will always print this is no 2, but if you have a Condition that may change like we have here because it checks the condition with different moving average values. In some cases, this might be true. In other cases, this will be true. So, you use if statements to bring structure into your program code and to have different resites for the if statement for every tick. And yeah, we can use this if statement to simply check whether we're in an uptrend or in a downtrend. And they will use this program in the strategy tester, which you can do by pressing control R on your keyboard. It will open the strategy tester. And you can go to visualize and you can choose a

[40:56] expert advisor you want to test. And I can simply choose my simple and profitable expert. I go to the one hour time frame chart and I click on start test. And this will open the strategy tester and it will do a simulation for this expert advisor. And you can see that it applies the experts, the moving averages to the chart. So we have a visual effect. And we can also see that it says it displays the current trend direction in the upper left corner. And in this case, the fast moving average is above the slow moving average and also the bit price is above the fast moving average. So this is a uptrend and if I speed this up a little bit, you can see that this trend

[41:47] indication will change whenever there is a price movement and if there is a change in trend direction. So we can wait until there's a downtrend like it is here and you can see that there's no displays minus one. So the strategy test has a great tool to check if your program is doing the right thing and you should do this from time to time if you're developing an expert to see if if you are still on track, or if you have to change something because you made an error or something. Okay, so after doing this, we can now proceed with the five-minute time frame. And for this purpose, we need handle MRFast,

[42:42] we need three more handles. And I will, I will simply declare them here, slow. Maybe you ask yourself why I sometimes declare variables at the very top of the program outside of any function and sometimes I declare them like these double variables or arrays inside of a function. If I declare them outside of a function, this means that they are global variables. And they will be declared at the very start of the program and they will stay inside of the random access memory until the program is terminated. So if I start the program, the computer will make space for these variables in the memory.

[43:36] And this space will be reserved until I remove the expert advisor from the chart or until I close the meta-trailer. And if I declare variables inside of a function, these variables will only be stored or will only take space in the random access memory until the function is terminated. So whenever there is a tick, this code will be executed, but afterwards, if everything is executed, the function is over, it's terminated, and every variable declared in this function will be freed in the memory. So it will not take as much space as a global variable, pretty much. Okay, that's not correctly. That's not correct.

[44:32] It will take the same space but not for the whole time. So only if it's needed. And if there is like a tick, everything will become executed. And if there are five minutes without a tick, we don't reserve space for all these variables in the on tick function for the five minutes between the ticks. So you should always declare variables on the smallest possible scope. So this is a very wide scope because it's on a global basis for this expert advisor. And this is a smaller scope because this is inside of a function. If I now go inside of the body of a function, and if I declare a variable here, and I

[45:25] I can also put a value inside of this variable. This is an even smaller scope than this function scope because it is only inside of the body of this function. And if we leave the body of this function, this variable will be erased from the memory. So it does not take the space for such a long time. So it's all about programming efficiency here. It would not really make a difference if we declare all the variables on a global scope because computers these days have really high capacities and it would not really slow your program down but it's still good practice to do it like this and always declare variables on the smallest scope.

[46:18] and it will also make it much more easier to read for you or for any other programmer because it is easier if you don't have that many global variables. But we still need some global variables because we need these variables, these handle variables in different functions. For example we need it in the onInit function to initialize the handle variables and then we need the handle variables in the onTick function for our copy buffer function. And if we declare the function in the onInit function it will say in the onTick function that it does not know this variable because it's only declared in the

[47:07] onInit function and whenever the onInit function terminates this variable will will be erased from the random access memory. So we have to declare it on a global scale, scope. So it will stay active for the whole lifetime of this program and the error is gone. So now we have these three other handles and we can do the same process that we did before with the trend moving averages with a difference. We now enter the period M5 for the five minute timeframe. And we now enter the other parameters, which are the same as we used for the trend moving averages. So this is a 8, a 13 and a 21 period moving average here on the five minute time frame

[48:12] I will simply add it at all the parameters. So we can use these handles later in the on tick function to receive the values. Handles, MR, slow, IMA, symbol, period, M5, 21, 0, period M5, 21, zero, mode EMA, price close. Okay, so since we have the handles ready now, we can use the handles to receive the values for moving averages and we can do it here. For example, we write MR, fast, MR, middle, middle middle, middle, and m are slow, we declare three more arrays, which we can now use in the couple buffer function to store the values of the moving averages inside these arrays. So handle m are fast, buffer number is always zero for moving averages because it only

[49:24] has one indicator line. And we start at the position zero. We count one value and we store the value in the MRFAST array. We do the same process three times for the MRMIDL array and the MRSLOW array, middle and MRSLOW. And now we have everything up and running and prepared for the checking of our entry condition. So what I do here is I extend my comment one more time, fast MA, and I print all the values here on the chart. So we can always see if the calculation is correct. Middle and slow moving average. mR middle, mR slow.

[50:31] And if I compile this, there are some errors which I have to take care of. Okay, error is gone. And now we can see that this should indeed print the values of the three moving averages on the chart. We can simply check these values and compare them to the values here in this data window. So the fast one is the lowest, the green one is 1.20479. And that, oh, that's not correct. It's 1.2047 or 48. And it is indeed the value that is written up here in the chart. And you can also compare the other two values, which should be the values written here in the chart. Yeah, I think it's correct. Okay, so since we have the values now, we can check if all these are lined up as they

[51:39] should be. So the fastest is either at the top or at the bottom. The middle one is in the middle and the slowest one is on the other side. So we can check if the trend direction is one, then we check if MR fast is above the middle MR and MR middle is above the slow moving average. If this is true, we wait for an entry trigger. Let me write the other condition two real quick. If we have a downtrend, we check if all this is in reverse. So the fastest one is the lowest moving average and the middle one is below the slowest moving average. if this is true, we check for short scenarios. So what we have to do now is we have to check

[52:46] if the bit price hits the fastest moving average. And we can do it by checking if the bit price is smaller or equal to the MR fast. the MRFAST, I always write this 0 because I am addressing the index 0 of this array, which is the first value and we only have one value stored in this array using the copyBuffer function, but we still have to use an array. We can't use a single variable like this because the copyBuffer function simply requests an array like this. There is no, the copy buffer function has no parameter where it requests a single double variable to store a value in, and we always have to provide an array.

[53:46] Okay, if this is true, then we have a by signal, I think. Let me test this real quick in the tester to see if this is running to the right direction. So I can test this, start. We go to the five minute timeframe. Oh wait, let me change this. So we have the five minute timeframe as our main chart where we will see the chart comment in the upper left corner. So here we have a uptrend and there is a bias signal right now. So this is working, I think. I don't know if this is enough already.

[54:50] What we can do is this prints a bias statement every time the condition is true. So we get several of these bias signals whenever all conditions are met. And this may be true for several ticks. So what we can do here is we can check if the price just fall or just hit the fast moving average. So what we can do is we can use another variable and we can make a static, for example, and we can use a static variable of the type double which we can call last bit. And I will tell you in a second what I did here. Here I declared a variable of the type double. So the computer will look inside of its memory

[55:46] and it will watch out for a spot where it can fit a double variable. And this will be reserved, the space will be reserved for this last bit variable. But this variable is not only a double variable, but it is also a static double variable. Static is a modifier for variables and it says that even though this variable is declared in the on tick function, it is not erased after the termination of the function. So it says pretty much when you first declare this variable in the memory, it will stay there until the program terminates. So it is similar to a global variable, but you can still only address this last bit variable inside

[56:44] of the ontick function. So if I want to use it here, this will not work because it is not available in the on init function. But if I want to do the same if I write the same line inside of the ontick function after I declared the last bit variable, this will work because it is known in the ontick function. So this is what it's about with static variables and I use the static variable to store the last bit value inside of this last bit variable. And since this line is only executed once for the first time when it is processed, it will always store the value of the last bit price for the last tick.

[57:38] And I can now use it to check if the last bit is greater than maFast. And here I can check if the last bit is smaller than MAFAST. And after I did all this, I will now update the last bit variable with the current bit price which I receive here. So if I do this, it should reduce the print statements that I receive. Whenever the signal appears, but I see that there are still several print statements executed because the price may of course tick above and below the fast moving average, more than once in a pretty short time. So yeah, it is like this, but I don't care. We still have the signal figured out.

[58:40] And what we can do now is we can check if we have an open position. And if we do not have an open position, we can open a position in the right direction, of course. So I will simply reduce these statements since we saw it is working. And now I can, I have to include another file, which is trade, trade.mqh. And if I do this, I get access to a file in the include directory, in the trade directory. And there you find the trade.mqh file. And you can see that it contains a class. This is pretty complex if you just started programming for more experienced programmers. This C-Trade class takes care of all the trading mechanisms.

[59:42] It means you can open positions, you can close positions, you can manage open positions, you can place orders, et cetera, et cetera. And for the not experienced programmers, you simply have to know that we can use this file, we can include this file to make trading operations much more easier. So by including this file, we now have access to the C-Trade class and we can create an object or an object variable of this C-Trade class, which we simply name trade. And by doing so, we now have, we are now able to use this trade object object to do trading operations like this buying operation. We can simply buy the current underlying.

[60:40] We are using this expert advisor for. So we now want to use this trade function here to open a buy position whenever there is a signal. But first of all we have to count or open positions. And there is a mechanism in the MetaTrader programming or the framework which uses magic numbers, expert asset, expert magic number and every expert advisor has or can have its own magic number or every position, every order can have its own magic number. The magic number is used to identify a position. For example, if you are using three different expert advisors and they all have different strategies and you don't want them to interfere with each other.

[61:41] So you have to use a different magic number in every expert advisor. So for example the first expert advisor has the magic number 5, the second one has the magic number 100 and the third one has the magic number 68. Then you use this setExpertMagicNumber function to provide your individual magic number for For example, I can use two here. And whenever I open a position using this straight object, and for example the by function, it will, in the background, it will use this magic number and store it with the open position. So the position has a specific magic number. And then I can use this magic number to check if this open position or this open order belongs

[62:40] to this exact expert advisor. Okay, I hope this was not too complicated. All you have to know is that every expert advisor needs its own unique magic number to identify its own traits. And I can now use this magic number to count the open traits. So for example I can go like this. E is like positions total. Let me write a loop with a quick and then I will explain what I did here. with position select by asking a position ticket. Okay, position get string.

[64:33] Okay, what I did here is I first declared a new integer variable. This is pretty normal, we did this before. and I initialize it with a value zero. So we now have a place in the random access memory where we can increment this number for every position of this expert advisor that we have found in the open positions. So we then use a loop like this. This is a for loop and the for loop is always implemented in the same way. We use the keyword form, then we open these round parentheses. We can then use a precondition. For example, I create a integer variable with the name i. And I initialize it with a value of positions total minus 1.

[65:47] Positions total is a predefined function, which returns the amount of open positions. For example, if I have positions, let me open some positions real quick. If I have two open by positions, positions total would return the value of two. And then we subtract one from this amount of positions. So two minus one would be one. And our integer variable I would contain the value one in this specific case. So let's do it like this real quick. And then I have a condition which says i is greater or equal to zero. And then we have another piece of code which will be executed after every run of this for loop.

[66:44] So you can read it like this. For indicates that there will be a loop. Then we do the precondition only one time when we enter this for loop. Then we check this condition before every execution of this for loop. And after checking this, we execute the body of the for loop. So if this condition is true, we enter the body. For example, we can add a print statement and we can print the value of i. And after every execution, after every body of the for statement, we do this piece of code. So this would say that I will be initialized with a value of one, then it checks if one is greater or equal than the euro, this is true, so it will print one.

[67:39] Then the body of the Fawc statement is executed and then we would do this part of the for loop. It says that we decrement the value of I by one so it is the same as this. i is equal to i minus one so we just subtract one from the value of i so i is now zero then we check the condition again and it will now say is zero greater or equal than zero and this is also true so we would print zero then we subtract one again and now i is minus one and if we check the next time if i is greater or equal zero then we would see that this is not true and the body of the for loop would not be executed and then the for loop is over and yeah we will go on with the further code and of

[68:44] Of course, we don't want to use this simple example in our program, but instead we want to count all the open positions of our expert advisor. So we use this to loop over every position and inside of the body of this for loop, we simply receive the position ticket at the index of the value zero. So positions are indexed like this. If we have two positions, this will be the indexed zero. I think this would be the index one or the other way around, but it doesn't really matter because we simply loop over every single position. By using this index, we can now receive the position ticket by using the position

[69:37] get ticket function which demands a index as a parameter and then we can use the position select by ticket function which is also a predefined function which will select a position if you provide the ticket as a parameter and since we have the position ticket for the current index we can use this position ticket to select a position. If this If this was successful, it will return a Boolean value, so either true or false. And if it is true, we will enter the body of this if statement. And then we can check if the position symbol is equal to the symbol of the chart. if the position magic number is equal to our magic number that we used here to, we can use

[70:35] a variable for this. So we don't have to change it at many different places if we change the magic number. So we can simply check if the magic number is equal to the expert advisor magic number. And if this is true, then we increment the value of our positions variable variable by one. So this is again the same as this. Okay, so there is one error which I should take care of. Okay, there was only one equal sign, but this is the wrong method because if I am comparing two values, I always have to use two equal signs. Okay, now the error is gone And we have some warnings. These are not errors, but warnings,

[71:26] which pretty much say that the magic variable, which I declared here is also used in the trade MQH file. So we use the same variable name at different places in the source code, which will lead to a warning. And this is never good. So just change the name to, for example, EA magic and the error is gone or the warning is gone. So now this will give us the information about all open positions. And we can, for example, print this in our chart comment. Positions, positions like this. And now I can do the same for open orders. Because, but I don't really have to check for open orders. Yeah, I have to check for open orders.

[72:25] Okay, I have to check for open orders like this. The process is pretty much the same, but we change some functions. We now check the orders, the total amount of orders in our loop. We of course want to receive a order ticket, order get ticket, and then we select a order using the order ticket, not the position ticket, and then we can check order get string like this, and order get integer. The rest should be the same here. And we have to change the identifier to order symbol and order magic. And then we increment the value of order variable by one. And we can also add another line for our comment where we print the number or the amount of

[73:36] orders. Okay, if we did this, we can now check before we open a position if positions plus orders is orders orders orders orders is zero. And then we can execute a position. Let us for now open a by position with a volume of 0.01 by using the trade object. I will explain this later. And of course we don't want to open a position right away, but instead we want to place a order. Okay, let's try this in the tester. We should now open a position when there is a signal. And here we have our first signal and we can now see there is one position in the chart

[74:47] right now and this is, yeah, it was opened when the price hit the fastest moving average. This is working right now and there should not be any more action in this expert advisor because this position will stay open for the whole time and there is no condition where it would be closed. So this is working so far, but we have to make some adjustments because we don't want to open a biposition array, but instead we want to open a bystop order at the highest of the last five candidates. So first of all, we have to receive the highest of the last five candidates. And for this purpose, we have a function which is called i highest.

[75:42] is also a predefined function and it returns the index of the highest candle in a specific time frame for the last x-candles. So we want to take the last five candles and we started index one. So high price, we can now calculate the high price using the high function, which will now return the price of the candle at a specific index. So at this index highest, for example, and we can now use this high price to provide it to a buy by stop function and high price and we can set we can place a order at this specific price level. We do of course the same process for the for the for the sale function and

[76:53] Here we don't want to use the the highest and I lowest and I high function But instead we want to use the I lowest and I low function and we of course want to receive the lowest value And we open a position at the lowest price and it should be a Sales stop. So let's see if this is working correct. We can simply do another test run and wait until it places an order. This placed an order, but it is kind of far away, I think. This doesn't really look correct. Wait, no, it is correct. I think I just, the speed was just too high. Let's wait. Okay, so I think, ah, come on, pause. I think the order was executed here and we have one, two, three, four, five.

[77:57] This is the highest of the last five candles. So this is indeed correct and working. Okay, great. So now we have to take care of the exit. Oh, and have a look at the chart comment. It says we have one current open order for our expert advisor. And can I show you the magic number? Yeah, you can see if I hover over this, this order, you can see there is a number two written in this little comment popping up and it says number two and two is the magic number that we use for this for our expert advisor. If I change this to 22, for example, and do another test run, you will see that this order then is has the number 22 instead of 2. So this is the magic number which helps to

[78:53] identify the order. So oh wait it does not say 22 so this is not the magic number. How can I show you the magic number? I think I can't really show you the magic number. not good. I can show it like this but it is stored in the background and just trust me here because in this loops here we check if the position magic number or the order magic number is equal to our EA magic number so this is the same number that we stored in this EA magic variable. Okay, after doing all this, we now have to provide a stop loss. And the stop loss is, I think he says, it is at the high or the low of the last X candles.

[80:01] And here we can simply do the same that we did for the entry price and we do index lowest. I can just copy it from here. Like this and copy this and put it here. So we now get the lowest and highest price in both, or before both transactions here. We can now provide the symbol and the stop loss, which would here be the lowest price and for the sell order the highest price. If I do it like this, we should now see that the order has a stop loss price attached to it. Okay, here you can see there is the opening of the order, the open price, and there is the stop loss price.

[81:07] So this is working, I think. I think this is correct. Okay. Perfect. to calculate the take profit. And we can simply do it by using the entry price or the high price. And add the distance between the highest price minus minus the lowest price. So the difference between the entry and the stop loss, if we do it like this, then we should have the take profit calculated. and lowest price minus highest price, wait, no, that's not correct. Lowest price minus high minus lowest price for the cell order.

[82:15] And if we put the TP here, we should now have an order with a TP and a stop loss. Okay, this is good so far. Whenever you work with prices, it is always good practice to normalize the values because whenever you work with a computer, there are sometimes some strange behaviors like adding a digit like on the eighth on the eighth or tenth position or something and this will cause a problem because then you don't have a valid price. So you have to round or normalize the value and there is a predefined function for this. It is called normalize double and you simply provide the value and the amount of digits you want to use.

[83:09] And you can use underscore digits to get the amount of digits for the underlying symbol. So if I do it like this, I can now round or normalize all three prices here to prevent any errors. The lowest price, normalize double, lowest price digits. And here I do the same for the high price, normalize high price digits. Okay, now we can open our order with a take profit and a stop loss. So whenever the order is executed, it should be closed later on. And let's wait for the execution of this order. Okay, this executed and we have a, I think a winning trade here. Yeah, we made a profit of zero.

[84:12] So, a dot to five dollar and now we wait for another signal which we somehow don't have here. Okay, I think it has something to do with this if statement. I think if I simply erase the second part of the condition, it should be fine. Let me try this. We're correct. We can see if this works. So I start the test again and we will now see if this is working. So first order is good as usual. We have to check if the second order is executed. And it is not executed, I think.

[85:23] That's just not correct. You have to dig deeper into it in a second. But first of all, I just watched a part of the video. And I saw that my stop loss calculation was not correct. It is not the highest or lowest price of the last five candles. but instead it is the high or low of the trigger bar plus three pips. So for the stop loss, we calculate it as follows. We write double SL is equal to I low of the current bar. And shift value is zero plus an amount of pips. He said three pips, which is 30 points. I can write it like this. Okay, now I have to still have to find my mistake here.

[86:43] Okay, I think I found the problem. I am pretty sure that when the price was still running here, the low of this bar did not hit the moving average. And the moving average is an indicator that is a little bit slower than the actual price. So I think that when the price was down here, the moving average was even lower. And when the price recovered to this level, then it looked like the candle hit the moving average but at the moment of the trade it didn't. So yeah I just realized there is another problem with the stop loss. Something is not working correct here. Let me check this a quick. Okay it places the stop loss at the entry level pretty much.

[87:53] Hello. Okay, so this should be minus. This should be plus. Let me do this again. Okay, now it is working, I think. By the way, you don't have to use 30 points. You can choose whatever you want to choose. You can choose 50 points, 10 points. You can just play around by changing this number here. Okay, so the entry is working, I think. Now we have to take care of the signals like this signal and the very first one where it takes a decent amount of time for the order to become filled. I think this is not the requested behavior by the expert advisor and we should provide a mechanism to cancel the order after like five minutes or so.

[88:59] So we can simply go inside of our loop where we search through all the orders and we can check if order get integer order time. I think it's time setup. Is it time setup? I think it's time setup. If the time is less than the current time minus 10 minutes, for example, we simply want to trade order, delete. We want to delete the order like this. If we do this, it should be cancelled after 10 minutes. And it is in this case indeed cancelled after 10 minutes.

[90:05] 10 minutes is maybe a little bit... It's not enough time. Maybe we can say after 30 minutes, if there is no execution, we want to cancel the order. If we do it like this, it should work fine. You can see it places orders. Most of them become canceled. Some of them will be executed. Okay, so this is working. Now let me watch some more of the video. I think he's talking about a trading stop loss, which we can add here for the system. And this should be the whole tutorial then. So let me see what he says about the stop loss. and then I will talk to you later. Okay, so after watching some more minutes of the video,

[90:59] I realized that we don't provide a tech profit like we did here. Instead we have a partial close. And then we go on with a trading stop. So here, we will simply erase the TP from the order. order. And then we go to the position loop and check if we have a position. And if we have a position, which is a by position, then we want to do something. And if we have a position, get integer that is a cell position, then we want to do something else. Type cell. So for the buy position, we will have a first TP, which is the position get double, the position open price.

[92:04] Come on. Price open plus the open price. and the bid price is above this, or equal to this TP level, then we want to close half of the position. So, this is the first take profit. If the price reaches this mark, if the bid price is above this, or equal to this TP level, Then we want to close half of the position. We do it by using the trade position close partial function and we provide the position ticket.

[93:11] We provide the volume, which would be position get double position volume divided by two. And we should round this to digits. So we don't get any problems here. And then we check if this ROS was a success. Ah, come on, like this. And then we move the stop loss to the entry price. So we say we calculate a new SM, which is position get double, Position price open. Then we use the normalized double function to round this value of course. And then we want to trade position modify. We want to modify the position

[94:17] by providing the position ticket, the new SL and the O-Wild TP, which is zero, because we don't use a TP here. Okay, so compile. We have several errors here because I think I forgot to close the parentheses. I don't know, I did this really fast so I'm pretty sure I made a mistake somewhere. But let me try this real quick. I will provide the same mechanism for the cell position. We check if the bit is smaller or equal to the Tp. Then we close half of the position, blah, blah, blah, position break even. Okay, and now we have to increase the lot size because we can't really close something if we only use 0.01 lots.

[95:13] So if we increase it to 0.05, we should be able to have a partial close. And now we have to wait for an order or a position that will make a profit. Okay, here we have a by position. Wait a second. Do we have a by position? We have a by position, I think, but it is not. Not closed somehow, 0.0. It's placed. I really get this out, out, out. Disclosed. Okay, the position is closed too often. Okay, okay, because the condition is fulfilled more than once in a very short period of time. So we have to check if the bit is above the TP and if there was no partial close before.

[96:32] So we can use a global variable called lots and we can simply provide it to the bystop and sellstop function. And then we can check before we do another partial close. If position get double position volume, volume is equal or above the lots that we provided to the bystop and sellstop function. And if this is true, then we want to check if we have to do a partial close and otherwise we don't want to check this. Okay, compile. Okay, the trade function uses the same name for a variable. So we have to change this to EALots, which we will do very quick. EALots, EALots, compile. Another test run.

[97:35] go go go and whoa let us check this there is a partial close we pull we close 0.3 lots of the 0.5 which is 50% rounded of course and this is working and the rest is the losses at the entry level and now we only have to provide the trading stop which says that we will put the stop loss at the lowest of the last three bars for a buy scenario and at the highest of the last three bars at a sell scenario. So we want to check if we did this partial close here. Then we want to calculate the new sl. It is the lowest of the last three bars. So we use the iLowest function again in the M5 chart,

[98:54] mode low, hold array, blah, blah, blah, three bars. And then we can use the iLO function in the M5 chart and provide the shift value here. We normalize double the value and then we can use the trade position modify. Oh, first of all, we have to check if the new SL is greater than the position stop loss. Get double position SL, modify position ticket SL and now TP. And we do the same for the sell order, of course. higher, highest, highest, high, high, high, highest, check if the new SL is smaller than

[100:16] the old one. Okay, by the way, I forgot to explain the position get functions. I think it's really easy to understand. After selecting a position by using the position select by ticket function, you can use position get string, position get double, position get integer to receive the parameters of the currently selected position. So you have several identifiers like position symbol, position tile, position volume that identify what kind of value you want to receive and then the function simply returns the requested value. As you can read in the reference if you press F1. And yeah that's pretty much all there is about the position get functions.

[101:06] So let's see if the trading stop is working. We go to the order this one again. Now this one, okay, there is the partial close and we see the stop loss is at the, is it at the low, the last three bars? One, two, three, yeah it is. It is working I think, maybe we can check this one more time. Yeah, I think it is working, last three bars. You can check this at your PC by using the tester and using a much slower speed so you can see everything. So this is a short explanation of how you can code an Expert Advisor like the one explained in this video and you can you see the advantage of doing so because now you can use this Expert

[102:09] an advisor to test every market you want. And you can adjust the expert advisor to change the periods, for example, of the moving averages or the lot size and everything else. And you can even change the trade management if you make adjustments here. So this is really, really cool. And, yeah, just play around with it. I hope I was able to explain most of it in a very easy to understand stand way and yeah, I realized I had to struggle more than expected with with speaking in English Since I had a long break making videos in English and I don't really speak English in my normal life So I hope you were able to follow my explanations. Just play around with this

[103:01] expert advisor and Yeah, make adjustments if you want to and you can of course apply it to any different symbol. And you can also use it in the demo or even live account if you found a setup that fits your trading style. Please take care even if this expert advisor is a fully automated program. There might be any unexpected situations where you experience unexpected behavior. So please always have a look at your chart, at your trading, while using automated programs. I did this from scratch without great preparation. So I cannot guarantee that there are no unexpected situations where the expert advisor might get into trouble.

[104:00] So yeah, just take care. if you use it in a life account but have fun and yeah wish you a good time testing and working with this program. Okay yeah I will now cut this video put it online and wish you good luck trading with this expert advisor. See you in the next video. Until then have a good time and good trades. Bye bye.
