# 100% Automated Heiken Ashi Forex Trading Expert Advisor Programming Tutorial For MT5

- URL : https://www.youtube.com/watch?v=XU3FXFg3anY
- Date de publication : -
- Vues : 30 000
- Duree : 45:24
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is René. Welcome back to another video on this channel. And today we will have a look at the Heiken Ashi indicator. So first of all, let's see where we can find this indicator. If you are in the MetaTrader 5 and you have any chart open, for example, here the euro US dollar, you can go to the navigator on the left side. If you do not see it, click on View Navigator. And there you can find the indicators. You click on this button here and then you click on examples and then you will search for Hiken Ashi and there it is and you can just drag and drop it onto any chart. And if you click on OK, you can see that the previous candles will be hidden behind

[00:51] new Hiken Ashi candles. So if we, for example, go to properties and we can change the colors of the bars here, so we may make them white, then we only see the Heiken Ashi indicator, I think. Oh, wait, this only changes like the border, I think. I will have to change this to white as well, like this. So you can see, now we do not see like the original URU Soda chart, but we only see the Heiken Ashi candles. And you can see these candles can either be red or blue, which means for a red candle, the open of the Heiken Ashi bar is above the close. And for a blue candle, the open is below the close.

[01:49] And what you can also see is that the Haiken Ashi trends or a sequence of blue or red candles is often really stable. So there is a well-known trading strategy which trades these trends. So we could say once the color changes from blue to red, we will start a sale trade after the first red bar for example here at this point and then we will close this trade after the first blue candle here and then we start a buy trade and yeah we go on with this strategy as the bars continue. So this This is a great strategy for trending markets because in trend markets you will generate huge profits with this indicator.

[02:50] Still, it is not perfect for markets without a clear trend. But let's have a look at the programming part now. So click on Tools and MetaQuartz Language Editor to open the MetaQuartz Language Editor, which is the code editor in the MetaTrader 5. Then on the left side click into the navigator on expert and if you do not see the navigator click on view navigator and then you click in the upper left corner on new. Then you click on next, then you can provide a name, for example, Haiken Ashi program. The name is not really important for the functionality of the program. Then you click on next, next and finish and this will create a brand new expert advisor

[03:46] for you. So you can see on the left side we can now see this Hikinashi program in the experts folder. And now I remove all the properties at the top and the comments because they do not really affect the functionality. And we still do not see this program in the Metatrader under Expert Advisors. But once we click on Compile, we can take this source code file and make it an executable file. And this file will then automatically appear in the Expert Advisors folder here. So we could already take this and drag and drop it to any chart. So this is the program that we just created. And if I click OK, it is now running on this chart.

[04:39] And this is really, really cool, I think. So what we have to do now is we will have to get the Heiken Ashi indicator data first to generate our trading signals. And this indicator is, as we already learned, available in the MetaTrader 5. If you click on indicators, you will find the Heiken Ashi here. So it is a program that we can then use. So whenever you want to receive data from an indicator in MetaTrader 5 programming, you will need a handle for this. So we can go at the very top of our program outside of any functions and we can create a integer variable which will be the handle for our Heiken-Ashi indicator.

[05:29] You can just write int, which says that it is an integer variable, which is pretty much just any number like 23, 243, 423, yeah, I mean, you get the idea. In any integer variable, you can store numbers without a decimal point. And this is just the name of the variable. We could choose any other name, for example, Peter or Mary, doesn't really matter, but I would always suggest choosing a name that already tells you what this variable holds. And then we end the line with a semicolon. We click on compile and then we have to put a value or a handle value pretty much inside of this variable because right now there is no defined value inside of this variable.

[06:24] So what we can do is we go inside of the body of this onInit function. the body is in between of these two curly brackets and the onInit function is a system function which is automatically called by the Metatrader once the program is initialized on the chart. And here we say Haken-Ashi or handleHaken-Ashi is equal to the return value of iCustom and iCustom is a function. We can read about this in the MQL5 reference. So if you click with your cursor into this word, I custom with a small letter i and a capital letter c and then custom, you can click F1 on your keyboard and this should open the

[07:09] mql5 reference. There you can read about this function. This is a function which is part of the mql5 framework so we can use it without having to do anything for it. So you can see this function returns the handle of a specified custom indicator. This is great because we need a handle to be stored inside of this variable. And we can read even more about this function. You can see this function returns an integer value. This is why we created a integer variable. And this function needs at least three parameters. So we will have to say what specific Haykann-Ashi indicator we want to use. So we open the round brackets of this function and add some parameters.

[08:00] The first one is the symbol. And we can use this underscore symbol system variable to automatically use the symbol of the chart where the expert device is attached to. In this case it would be Euro-USD. Then as a second parameter, we have to provide the time frame. So there are different time frames and you know them if you worked with the MetaTrader before. So you can see you can, for example, switch the chart time frame and the Heiken Ashi indicator also changes. So it is important for the iCustom function to know what specific time frame you are looking at. So we can say for example, we want to use the current chart time frame.

[08:44] So we can write this period underscore current. And then we will have to provide an indicator name because at this point, the custom function does not know what indicator we want to use. So we will have to say, and you can go to the indicators folder here in the navigator. You can search for Hikinashi here. And then you can just pretty much copy the name. It is hikin underscore ashi and you write dot ex5 because this is the file that we want to use for this iCustom function. So the iCustom function will have a look at this specific program and will use this program and create an indicator handle for this indicator.

[09:32] So if we click on compile, this works. Now once the program is executed or attached to the chart or activated, we will then automatically create a handle. This handle is just any number. It usually is a number like 0, 1, 2, 3, something like this. So it is not really saying anything. But in the MQL5 framework, working with indicators is based on these handles. So the program or the meta trader knows that behind the handle ID 0, for example, there is this specific Haken Ashi indicator. And this is why we can define a indicator handle like this. So we always create a handle first, and then we can go ahead and receive values for this

[10:25] indicator. In this case, Haken Ashi indicator. So what we will do is we create some arrays to store the indicator data inside. So we can say, I can actually open for example, and I can actually close. So what we did in this line is we created two arrays. The type is double. So it is not an integer number in this case to be stored inside of these variables, but it is a double number. And double numbers are any numbers pretty much that have a decimal point. So these are some examples. And we are choosing double now because we want to store price data inside of these arrays. And you can see price data always has decimal points and some numbers after the decimal

[11:13] point. So we have to use double. And we now not only create one variable or array in this case, but we choose to create two. So we can do this by separating them with a commata. We could also do it like this. This would be exactly the same, but this is the operation in one line. And we are not only creating a single variable where we can put one value inside, but because of these squared brackets, we will create an array. So the computer now knows that we want the possibility or the option to store multiple values inside of these arrays. So there is an indexing mechanism for arrays. So we could say HA open at index 0 and we want to store some data inside.

[12:09] And we can say at index 1 we want to store another value. This will cause a problem, I think, if we run it. Yeah, there's a critical error, and this is because the array has the wrong size. So before we put data inside, we will have to define the size. And we did not do this yet. But this is not really necessary for us, because we want to use the copyBuffer function to store values inside of these arrays and the copy buffer function takes care of the resizing of the arrays. So the copy buffer function can be used to provide an indicator handle and it can be any indicator handle pretty much. You can choose the Heiken Ashi, the moving average, the

[12:57] bolling events, whatever handle you have and you can provide it to the copy buffer function as a first parameter. You can see the parameters here for the parameter list in the signature of the copy buffer function. So we can say we want the handle HikinAscii. Now we have to provide a buffer number. And this is a little bit tricky because the HikinAscii has many buffers. In total there are four buffers and you can read about them here in the data window. So I would suggest to open the data window. You can click on view data window to open it. And then if you move your cursor over the chart, you would see the values change

[13:36] because we always receive the values for the chart where our cursor is currently located. So in this case, we can see this icon actually open is at 1.0004 and you can read about this here in the data window. And you can see the close price is also displayed here. So these are the two values that we need to check if it is a buy or a sell candle. So what we do here is we We need the first buffer and the fourth. And this is a little bit tricky because the accounting starts at zero for the buffers. So this will be buffer zero, this will be buffer three. So we can say, how can I actually open, shall be stored, shall be received from buffer

[14:22] number zero from this specific indicator. Now we have to define a starting position which will be one and a count. position means that we start at the very right side of the chart and we go one bar to the left. So this is like the starting position. If we would choose starting position zero, we would choose this bar. And the count means that we want exactly one value. If we would increase the count to three, we would receive values for these three bars here. Because we start at bar one and we go one, two, three bars in total. And this is why we need an array because we have the option to store multiple values inside of these arrays.

[15:07] But in this case we only need one value. And the copy buffer function will take care of everything and will resize the array and it will then read the data from the from the from the indicator and then store it inside of this array. So we will have to provide the array as the last parameter And we do the same thing pretty much for the buffer number 3, which will be the close price. So like this. And I saw already that there is a problem with our Hikin-Ashi indicator. I think I made a mistake there. So if we attach it to the chart again, I think we do not need this .ex5. We can just choose the name.

[15:50] And this should hopefully work now. Okay, so what we did now is we received the open and the close price or the open and close, yeah, price for the last Hikinashi bar. And if we use the comment function, just copy what I do here, we can print it on the chart. So, Hikinashi close, this is just a information on the chart. We can print something on the chart using this comment function. And if you want to say to the PC that you are talking about texts, you will have to put it in these quotation marks. So the PC knows it is a text. And yeah, there's a problem because I tried to print the I can actually open data.

[16:42] But since this is an array, the PC or the MetaTrader, whatever has to know what index I want to address here. So I will have to provide in squared brackets the index here. And this is always index zero because we only have one value inside of our arrays. So in the upper left corner, we should now see the, oh, we cannot load this indicator. I'm not sure what the problem is. Maybe we need MQ5 to make it work. Yeah, no, MQ5. Why cannot load this indicator? I don't really get this. Oh, okay. I got it. I think I found the problem and this is a path to the indicator that you want to use and the path starts in the

[17:36] Indicators folder, but since the Hikinashi is in the examples sub folder, I think we will have to add examples here with two backslash and then hack an ashi and if we try it now it works You can see that it works because we do not see an error here in the experts tab And we will see the values in the upper left corner and these are the values for the last Bar here. So if we have a look at these the open and close value here These are the values for this bar displayed in the upper left corner in the chart comment here. So we know that this is working. So what we can do now is we can figure out if it is a blue or red candle. So we can check if Haikenashi open is

[18:26] smaller than Haikenashi close, then it is a blue candle. Blue candle or bi So this if statement can be used to check if a condition is true or false. So we have this word if and then in round brackets we have a condition. Here we check if the Heiken Ashi open value in the Heiken Ashi array at index 0 is smaller than the Heiken Ashi value in the Heiken Ashi close array at index 0. And if this is true, then we enter the body of this if statement. The body is always in these curly brackets. And then we can add an else if, because if this is not true, we might want to check if it is a red candle, candle or cell

[19:28] candle, like this. So this is how we can check the conditions. So this should be already enough. So there's one more thing that we can do, which makes our program a little bit cleaner. Since this operation only has to be done once per bar, we can add something to make sure that this is not processed with every tick. Because we added the code in the body of the on tick function and this on tick function is called whenever there's a tick in the market. And we only want to process this if there is a new bar. So what we can do is we can create another global variable and we can say bar's total like this.

[20:16] And we can initialize it here and we can say the value or the return value of this I bar's function shall be stored in the bar's total variable. And the I bar's function, it returns the number of bars in a specified chart. So we have to specify the symbol and chart period, which is again the period current, so the current chart period. And this will return the number of bars. So for example, 10,335 or something like this. And it will be stored in this variable. And now we go to the on tick function and here we create another variable. And this variable is completely different than this one. And in this variable,

[21:00] we will store the iBars return value again. But there's a difference between this variable and this variable. This busTotal variable is declared outside of any function body on a global scale. This means that this variable will stay alive as long as the program is alive. And this variable in the ontick function, this will only stay alive as long as we are in the body of the ontick function. So once the tick is processed, This variable will be deleted from the memory of your PC and on the next tick it will be created again, pretty much, but as a completely new variable. And the BAS total variable will stay alive for the whole runtime of your program and

[21:50] therefore the value in this variable will not be resetted. So we can check now if the value of this BAS total is different than the value in the BAS variable. So if this is true, this is another if statement here, we go into the body of this if statement and then we want to process this stuff. But we only want to process it once per bar. And we can make sure that it is only processed once per bar by saying we only do this if If bus total is not equal to bus, and afterwards we update this bus total variable and put the value in the bus variable inside of the bus total variable. So this is a way to check if there's a new bar in the chart.

[22:45] So what we can do here is we can now open positions. So the next step is we want to open positions if there's a buy signal. So what we can do is we can include a file into our program and you will want to include this trade.mqh file. So what you do is you write this hashtag include order and this and then in these greater than smaller than sign bracket thing you will add trade slash trade.mqh and this will include a file. And you can find this file in the navigator if you click on include. There you will find a trade and there you will find trade dot mqh. And this is a huge file which holds a class and the class is called Ctrade.

[23:40] This is a little bit complex if you want to learn more about this and object oriented programming and if you want to learn more about programming in Metadata 5 in general, check out the link below this video and you will find a complete course on mql5 programming. short form is the short explanation, this Ctrade class will help us to open trades. So we can now create an object variable of this class Ctrade. And therefore it is important to include this file because if I do not include it and try to compile this, there's an error because Ctrade is not known. But if I include it, everything works fine and I'm able to create this object variable

[24:20] of type Ctrade. And I can now use this, so I write trade dot, and then I can use the function by, which is part of this C trade class, and I can use this to open by positions. So I can say this shall open a by position. And for example, I want to open 0.1 lots. So the first parameter is the lot size of the position that shall be opened. And I could also say I want to sell if there's a sale signal also zero dot one lots. And now we can have a look at this program in the strategy tester. So go to the Metatrader and go to view strategy tester and there you can find the strategy tester. If you click on overview visualize then you can choose an expert which will be the

[25:18] the Hikinachi program, of course, and you can define the symbol, the time frame, and everything here and the period that you want to test. But we will just click on start in the lower right corner and you will see this will now simulate our program on historical data. So this is what would happen if we would trade this program pretty much. So we can see that hopefully we will open a position once this bar is closed and it should be a sale position. So let's wait until the bar is finished. I do not want to speed it up too much here because then it goes insane. But we can see there's a sale signal and we open a sale trade.

[26:10] And we see that after every bar, we open a trade node, which is completely not useful, but it shows us that the program, the mechanisms work. So there were several problems. We open too many trades and we open, yeah, we never close trades. So first of all, we should take care of the first problem. We only want to open one position. And there's an easy way of doing some. So what we can do is we can say that, what can we do here is we can say that we store the ticket of this position in a variable. So we can say position ticket like this, compile, and this is a global variable. So it will stay live for the whole runtime of the program.

[27:11] And this is of type unsigned long. values are similar to integer values, but they can be even bigger. So they can be greater numbers. And this U or unsigned means that this cannot be negative. It is always a positive number. And this is because we want to store that position ticket inside of this variable. So we can say if trade buy, and this works because trade buy will return a Boolean value, which is either true or false. So this is a complete condition. So if this works, we want to store, whoops, we want to store inside of our position ticket variable the return value of trade, result, result order.

[28:06] And I will show to you what this does in a second. And I will do the same for the sales signal. So I will say if this worked, I want to store the position ticket in this variable. And we can now also show this in the chart comment. So we can say position ticket and we say position ticket. And also before we open any position, we check if, or here we can check before we open a position if position ticket is smaller or equal to zero. Because in this case, there is no open position. if position ticket is smaller or equal to zero. And we will now see that here we will only open one position. So let's have a look at the test again.

[29:02] Now I can speed it up because we will only open one position after the first bar. And we will see in the upper left corner. Right now you can see here the position ticket number is zero. But after the first bar is finished and we open a position, there is a position and you can see this position has the ticket number 2. And this is now what is displayed in the upper left corner. And by receiving the result order ticket and storing it in the position ticket variable here, we will get the position ticket. Because if we have a look at the history here and we can have a look at the orders, You can see the order number is 2.

[29:45] And for a hedging account in Metatrader 5, and this is a hedging account, the position ticket, we can see it here, the position ticket of the open position will always be the same as the order ticket of the order that opened this position. So we can make sure that we store the position ticket of the position that is opened after this operation in this position ticket variable. And what we have to take care of now is closing these positions if there is a cell signal. So what we can do is, if we have a bycander, we want to check if position ticket is greater than zero, because in this case, we will

[30:31] select a position. We say position is select by ticket and this is a function that selects a position and you will have to provide the position ticket as a parameter. You can see here, position ticket is requested as a unsigned long value. So we just provide our position ticket variable, which holds this value for this function. And this will then select a position. And you can read about it in the reference, click F1 again. And here you can see, once we selected a position, we can then use the position get double, position get integer and position get string function to receive information for this currently selected position.

[31:16] So what we can do here is we can check if position get integer and then we say in the brackets as a parameter, we provide a identifier and you can write POS S and then you can already see recommended all the valid identifiers. And we want to choose position type because this will give us the position type and we can compare it to position type cell. This is always a defined value in Metatrader 5 in the MQL5 framework. So this is a way to check if the open position is a say position because in this case, we want to close it. So we say if trade position close, which is another function of the C trade class, and

[32:14] then we can provide the position ticket. And if we were able to close this position, we can say position ticket will be resetted to zero. And now we can select and copy this whole block and add it here for the red bar or cell signal. So we check for a red bar if there is a position or first we check if position ticket is greater than zero. Then we check if we were able to select a position. Then we check if this is a by position. So we write position type by. And then we close it. it is a bi-position and then we click on compile and this should be enough code already to run this program completely automated because now we can see if it loads what it doesn't do.

[33:13] Okay, there it is. So we see a cell signal, a cell position is opened and it is closed once There is a bi signal so you can see this was the cell position then there's a blue bar There's a bi trade. There's a red bar. There's a cell trade. There is another blue bar. There's a bi trade and Here we should see a cell trade. I think it is not loaded yet, but it should be there. Yeah, there is another Cell trade there's another bi trade cell by and so on and so on so you can see we are now trading the the Hikinashi signals. And yeah, as I said, this may work in trendy markets. In this case, it does not work for this specific situation,

[34:05] but you could also go ahead and add some stop losses, trading stops and stuff like that. But this is not, oh yeah, maybe I want to do this. Maybe I want to add a stop loss And then we can add some variable percentage lot calculation because this is what we did in the last video and we can use this function. So this is a great learning experience, I think. So what we want to do here is maybe we can go ahead and create some input variables. So go to the very top of your program and write input double lots. And then you can write zero dot one, for example, and input int sl points, for example, 200.

[34:47] or no, lots per cent or risk per cent like this, 0.5 for example. So what this does is it creates two input variables and input variables are the kind of variables that can be defined by or changed by the user of the program. So if you go to the settings you can see they are now here. So for your program go to right click expert list and and then hack and ask your program. And wait a second. Yeah, there it will open the inputs or just double click the little head in the corner here. And this will open the inputs. And you can see the user can now change these values. And I can also change this value.

[35:35] And yeah, this is because it gives flexibility to the user of the program. And in the test that you will find the inputs tab down here. So what I want to do here is I want to provide a stop loss for our trades. So before we open the trades here, we will calculate the entry price first. And for a buy position, this is usually the ask price. So you can receive the ask price using the symbol info double function, then provide a symbol. And the identifier symbol ask is a seven parameter. And then we can say entry, normalize double, entry digits. This is pretty much just to round the value. So if you have some decimal, some numbers

[36:24] after the decimal points that you do not want, then you can use this normalize double function to cut them off. So we say that we want maximum these underscore digits amount of numbers after the decimal point. underscore digit, you can read about it in the documentation, it always holds the number of digits after a decimal point for the currently selected chart. So in this case it would be five because there are five numbers after the decimal point. So now we can use this entry value which is just the ask price and calculate a SL. So we can say SL shall be entry minus SL points. These are are the points that we defined here,

[37:10] multiplied with underscore point. And underscore point holds the value of the smallest possible price change. In this case, it would be 0.0001. So 200 multiplied with 0,0,0,0,1 is 0.00200. And this is why we can calculate the SL like this. So we should also round this here to make sure there are no unwanted numbers after the decimal point. And then we can provide these things as parameters for the buy function. So you can see if I write a comma here, I can provide a symbol and then a price, which is the entry price and ASL like this. And this will now add ASL to all the trades. And I want to do the same thing for the sale trades.

[38:04] There's just one difference or two things we have to change. We will have to use the symbol bit as an entry price because we open cell trace always at the bit price and then for ASL we add the SL points to the entry and we do not subtract them. The rest is the same. We can use the chart symbol, we can say we want to open at the entry price and we provide ASL. So if we run the program again, everything stays the same pretty much, but now we have this as a and this will also cause a problem that we will have to handle. So we will have a look at this. And yeah, there you can already see it. So we opened this first position and this position now has a stop loss.

[38:58] And I think this stop loss was reached. Wait a second. This did this even work? Yeah, I think it worked. So let's have a look at the deals. Yeah, the SL was reached. So when the program tried to open the next by position here, it tried to find a position since position ticker is greater than zero. But this position was already closed. And therefore, we are not able to find this position. So we will have to add some more code. We will have to say, if we are not able to find a position, then we automatically reset the position number to zero. Because then this will be true again. We can open another position.

[39:53] So let me copy this and add this for the cell block here. So if we are not able to open a position, we paste it here. And make sure that you do not mess up like the curly bracket stuff here. So you should go and place a cursor after this opening bracket, because then it will highlight the closing bracket of this if statement. So move your cursor here. It will highlight this curly bracket. and you can add the else part here. So the program checks if it is able to select a position and if it is not able, it will reset the position ticket number. So if we run the program again, you will see this works now.

[40:46] There is this little bug in Metatrader 5 and I don't know why they can't fix it. I'm not able to start a test until some, I don't know, some time passed. super annoying. I don't know why they can't fix it. Okay, so now we can start it and we should see that even if the trades hit the stop loss level and they are closed, we are still able to open more positions. So here you can see we open more positions and many of them hit the stop loss because we now added a stop loss limit. But this is not only to have a stop loss. This is something that I want to use to calculate the variable lot size so right now you can see we always lose a

[41:35] Yeah some amount of money, but if we say we want to lose 1% of your or we want to risk 1% of our account With every trade we can do so by adding a function to a program. So in the last video, and if you did not watch it, you should go back to the last video now and watch it and program this function together with me. You will find it on the channel if you are searching for I think percentage, risk, calculation, something like this. And you can find this calculate lots function there. We write it there and I can now copy this and paste it into my Hiken Ashi program at the very bottom outside of any other function.

[42:22] So if I just paste it here, we can now compile it, there's no problem and we can use it before we open a position. For example, here before we open the by position, we can say the lots shall be the return value of this calculator, lots function, and we provide the risk percent and the SA distance, which is entry minus SL, like this. And this will give us the lot size. And yeah. And we can do the same thing for the, oh, and then of course we will have to say that we want to open this lot size. So as a first parameter for the by function, we say lots. Because this is the name of the variable that we just created and where we start the return value

[43:08] of this calculate lots function inside. So we do the same thing for the sell side. So here we only have to say S L minus entry because the S L is higher than the entry price for sell positions. And then we also have to exchange this zero dot one with the lots here. And once we did this, we can run the test again. And now we should see that we do not open the same lot size with every trade But we risk this like, I don't know what is the setting, we risk 0.5% of our account with every single trade. So you can see once we lose a trade and once it hits the stop loss, we lose around 0.5% of our account.

[43:57] It is always a little bit more or less because this is rounded And there are sometimes issues with the exchange ratio for euro-euros dollar and stuff like this, but you can see it pretty much works. So we risked roughly 0.5% of our account. And yeah, this is really cool. So this is a way to limit your losses and to still be able to make big wins like we just saw here somewhere 100 euro in profit. And still this does not seem to work with these settings for this chart and the symbol, but you can use the tester and just play around with these inputs, play around with the symbols and maybe you can figure out some situations where the strategy works or maybe you just

[44:59] learn something and that's all I wanted to achieve with this programming tutorial. So if you are still watching make sure to give this video a thumbs up and recommend it to your friends, subscribe to the channel and yeah I hope you liked it and if you are subscribed we will see each other next time in the next video until then have a great time and good trades. Bye bye.
