# Automate any Trailing Stop in MT5 Using This Simple Program | mql5 Programming Tutorial

- URL : https://www.youtube.com/watch?v=3l8RyeQNmNo
- Date de publication : -
- Vues : 33 000
- Duree : 52:59
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is René and in this video I want to show you how to write a trailing stop program in MetaQrida5 so this will be a raw skeleton pretty much for any trailing stop expert advisor that you Yeah, can imagine and want to program in MetaQrida5 because the general structure can be always the same so a trailing stop is pretty much just a system or a algorithm that takes the stop loss of your trades and trades it or drags it pretty much into the trade direction if the if the price moves into the right direction. So for example here we have a downtrend and a good trading stop would take the stop loss and trail it

[00:48] in a specific distance to the current price, so you won't get stopped out on small retracement here, but yeah, you will be stopped out if the market turns completely and goes into the other direction. So whenever we want to write a MetaTrader 5 program ref to use, or we can use the MetaQuotes language editor, you can find it if you click on IDE or if you click on tools, MetaQuotes language editor. This will open the editor that you can use to write automated training programs or programs like a trading stop. Then on the left side you will find the navigator. If you do not find it click on view navigator. You can highlight experts because in

[01:34] this case we want to write an expert advisor and click on new in the upper left corner. Then you can just select expert advisor template as it is already selected and you can provide a name after clicking on next here. So we can say trading stop system whatever, doesn't really matter how you name it here. And then we have some more forms, form fields here that we can fill but we do not have to. So we click on next, next and finish. And this will create a raw skeleton for an expert advisor, which just covers the three basic functions here. I will delete everything else because we do not need it.

[02:19] These gray lines are just comments. So what we have left is the onInit, the onDinit, and the onTick function. These three functions are system functions and they are called by the Metatrader if we attach this ExpertAdvisor to a chart. So for example, the onInit function is called whenever the program is initialized. The onDinit is if it is de-initialized. For example, the onInit would be called if we attach the Expert Advisor to a chart, and the onInit will be called if we remove it from a chart. And the onTick function is the most important because this is the one that is called with every single tick.

[02:58] You can try it out by just writing this print and then in parentheses a text and end the line with a semicolon inside of the curly brackets of this on tick function. And if you then click on compile, afterwards it will take your MQ5 file that you can see here and that you just edit it and transform it into a EX5 file. The EX5 file will then automatically appear in the navigator in your Metatrader 5. Here you should find in the navigator under expert advisors, you should find a trading system now or whatever your name for your program is. So I could open any chart and attach the program to this chart window.

[03:47] So, plan chart. So I can just choose training stop system, drag it into any chart and I click on okay and it appears in the upper right corner and it is now active. If this would be a weekday, Right now it is Sunday, so we do not see any ticks. But if it would be a weekday, there we would see the print statement in the journal. But what I can do here is I can open the strategy tester. If I click on view strategy tester, and let me close the normal terminal here because it doesn't really make sense on weekdays. And I click on visualize, and there I can also select the expert that I want to test.

[04:33] For example, trading subsystem. And if I now click on start, this will visualize the tester for a chosen period of historical data. And there in the journal, I will now see the text output with every single tick. And yeah, you can see whenever there is a tick, the on tick function is automatically called and processed. So whatever is written in the on tick function will be executed with every single And this is why in 99% of all cases you want to put your code that manages your trailing stop inside of this on take function. And what we want to do here is a trailing stop has to modify the open positions.

[05:23] Positions in MetaTrailer 5 are the kind of trades that are already executed and that are currently in the market. So you see a profit and loss for them. So you want to loop all the positions and you can do this by using a loop like I will now do here. So let me just write the first line and then I will explain it real quick. So print i. So this is a for loop. A loop is a control structure that you can use if you have a piece of code that has to be executed several times. In this case, we want to execute the code that manages the trailing stop for every single position. So first of all, we have to make clear

[06:14] that we have the right amount of positions because, yeah, as I said, we want to loop or we want to use this loop for every single position. So a for loop is always structured like this. First we have this keyword pretty much which is four. Then you open the brackets here, the round brackets and then we define a variable. This can be for example, or this is in most cases the variable i which is an integer variable and which has a starting value. And if you see this variable i in four loops, this is not because it has be named I, you could choose any other name. You can say fox or dog or whatever you want

[07:00] to call it. But this is just a programming convention. And yeah, most programmers in any programming language start with the letter I if they initialize a for loop. So this is just a good habit. And a integer variable means that this I variable can hold any number pretty much that doesn't have a decimal point. So numbers like this can be stored inside of this i variable because they are values without a decimal point. And we then initialize the value or the variable i with a starting value. So for my example here, this will be zero now. And then as a second information for the for loop, we have to declare a condition.

[07:59] So we can say I is smaller than two, for example. And this is the condition that is checked before every run of the loop, pretty much. And then we have another piece of code that is executed after every loop of this for loop. So this could be a concept or a skeleton for a for loop. And what this would do is the PC, if it reaches line 13 pretty much, it would first of all go into this precondition here and it would create this variable I inside of your memory of the PC and it will store the value zero inside of it. And yeah, that's pretty much the first step the PC does as soon as line 13 is reached.

[08:52] And afterwards it would automatically check this condition. So it would check if there is a variable named i in the memory, which is true of course, and it has the value zero. So the PC checks if zero is smaller than two, which is indeed true. So afterwards, after checking this condition, we would go inside of the body of this for loop and the body is always inside of these curly brackets here. And then everything in the body is executed directly after checking the condition. So if we, for example, write print and in the parentheses i and end the line with a semicolon we would print zero on the first run of the loop and after processing everything

[09:44] that is inside of this body the PC would not go on with line 18 but it would go on with this piece of code which is executed every time we processed the body of the loop. So in this case, this is just a way of saying that i has to be incremented and increased by one. So we would add one to our i variable. So right now inside of this i variable one as a number is stored. And after increasing the value i, we would again check the condition. So in this case, we check if one is smaller than two. This is true. So we enter the body. So we print one in this case because now i is equal to one and after processing the

[10:35] body again we would increase the value of i by one. So in this case the value will be two. Afterwards we check the condition again. Two is not smaller than two. So we would not do anything else and we would go on with the following code. So this for loop would have the following output zero and one. And I can of course demonstrate this to you. If I for example take this piece of code, put it in the onInit function and click on compile and if we now activate this program you will see that this is indeed working. So again I have to open the terminal for this and click in the tool box and click in the experts journal down here to see all the outputs of my program.

[11:23] if I now activate the trading stop system, you will see the output is zero and one, as I explained before. So this is how you can use a for loop. And the structure is pretty much always the same. And this is why I explained this so slow and so detailed. Because a for loop always has this precondition, then a main condition, which is checked before every run of the body of the loop. loop and then a piece of code that is executed after every execution of the body of a loop. And once the condition is no longer true, the loop will be left and the next piece of code after the loop will be executed.

[12:08] So this was just an example. But in this case we want to loop all the open positions. So this is why I built the for loop like this. The general structure is completely the same. So we have this keyword for, then we initialize a variable i, but in this case we do not choose value zero, but we choose the value or the return value of the positions total function and subtract one from this value. So position total, or positions total, as you can read in the documentation, which you can find if you click on help, mql5 reference, and then you can just type And in the function name that you want to search for, you will see this returns the number

[12:54] of open positions. And this is a system function, so we can just use it like it is. So if there are three open positions, positions totally would return three. If there are five open positions, it would return five. So this is great, because we always receive the amount of currently open positions. Then we subtract one from this value. So for example, if we have three open positions, we would initialize the i variable with two because we have three open positions, we subtract one. So what is left is two. And then we check if this value inside of the i variable is greater or equal to zero. While we do this, I would explain it in a second.

[13:35] And then we go into the body, we can print this. Yeah, we will do something else, of course, later. And afterwards we will not increase i by one, but we want to subtract one from i. So we loop, for example, if we have three open positions, we would initialize i with a value of two. Then we would see two is greater or equal to zero. So we would process the body, then we would subtract one from two. So one is now stored inside of i. We would check the condition, it is true. We would process the body, we would subtract one from i, now i is zero, which is still greater or equal to zero. So we would process the body again,

[14:17] then we subtract one again, now i is minus one, which is not greater or equal to zero, and we leave the loop. So if we just open some positions now, just for testing purposes, because I make this video on a Sunday, so I cannot open positions in the normal market here, or maybe I can do it. Can I do it for Bitcoin? I think I can do it for Bitcoin, because Bitcoin should be, I should be able to trade Bitcoin. Yes, this works. So just opened some positions here. Now I have three open Bitcoin positions, and I can now activate my trendline trader on this, no, my trading stop system on this chart. And as you can see now, if there is a new tick,

[15:07] we will see the output two, one, and zero here in the experts journal, I think. So we see two, one, zero. As you can see here with every tick. And this is because we have three open positions. So this is exactly what I demonstrated before. Positions total will be three in this case. We subtract one, we print everything and everything works fine. So now the question in your mind is, why do we do this? We do this because of course we do not want to print I, what we want to have is the ticket numbers of these open positions. So we can use the position getTicket function, and we have to provide a index.

[15:46] So as you can see in the documentation, the function returns the ticket of a position with a specified index. And this is of course a system function, so we do not have to write it on our own, we can just use it straight ahead. So you use this position getTicket function, we provide a index which is i because i will be two for the first one, one for the second one and zero for the third one. And then we store the return value of this position getTicket function because the return value is of type unsigned long inside a unsigned long variable which is named positionTicket. So what we can do here is we can say, for example,

[16:33] we want to print the result. Or we can do it like this. Just copy everything as I write it. And now we can see, if we look in the Express Journal again, we will see that position with index two has this position ID has this position ID with 765 in the end. And we can see 765 in the end it is this position ticket ID which is now at position two. And the same is for position one and zero. So there is an indexing of open positions and it always starts with position index zero which is the very first position. The second position here is index one, the third is index two, the fourth will be index three, the fifth will be index four. And you can see this is just a

[17:35] indexing which always starts at zero and then goes on. And if I for example delete the position at position index zero like this, this would automatically rearrange. So right now this Currently highlighted position is at index zero and this is at index one So if I just clear this you will see we only Get position zero and one now in the expert journal because position at index two is no longer available With only two open positions. So this is a great way. I can delete this print statement I think now this is a great way of of looping all the open positions using just the position index and the position getTicket function.

[18:23] And since we now have a position ticket, we can use the positionSelect function like this, selectByTicket function, and we have to provide the position ticket here. We can now select a position. So we can click on compile. And what we did here is we used the position select by ticket function. This is a function which returns bool, so either true or false, because a boolean value can only be true or false. Only these two states can be stored inside of a boolean value. And then as a function parameter here, we have to provide a position ticket of type unsigned long, and this is great because obviously we have a position ticket

[19:07] stored inside of our position ticket variable here. So we can just provide it as a parameter for the position select by ticket function here. This if is just a if-else or if statement or if control structure, however you want to call it. And it, yeah, you can read it in the documentation pretty much, but I will explain it real quick. You have this code word if, and then in parentheses here you have a condition and the condition can be either true or false and this is great because our position select by ticket function it returns a Boolean value which can be either true or false. So obviously if

[19:46] this function call succeeds we will have true as a result value so the whole condition for this if statement is true and we would enter the body of this if statement which is again in curly brackets. So if we were able to open a position, we select a position. We, it would be a great idea to check if this is a position of the same symbol then the chart symbol because this would help us to not just process this trading stop for any position in the account. So we can say position get string which is a function that will return a string value which is pretty much, wait, why doesn't it open the documentation?

[20:37] Whoops. Okay. I think I don't know what happened here. Now I have the documentation in the upper left corner here which I do not want. So, yeah. No, this is not cool. Okay, so let me open the, ah, there it is, the documentation. So position get string is a function which can give us information about any, it can give us string information from a currently selected position. And it is important, Before you use the position get string function and there is also a position get integer and a position get double function. Before you use any of these functions, you have to select a position successfully

[21:34] because otherwise this will not work correctly. So always make sure that you use the position select by ticket function before you try to get any string integer or double information from a position. But the position get string function is really easy. it returns a string value of course, and then it wants to know a identifier. So for example, in the brackets here, I can say I want to have the position symbol of this currently selected position. For example, for any position here, it would return BTCUSD, because this is the position symbol. And I want to check then if this is the same as the current chart symbol, which we can always receive

[22:24] using the underscore symbol system variable, or predefined variable, because this will always return the name of the current chart. So for example, if I add my print statement here, I can say for i, we have position ticket, blah, blah, blah. And we can, for example, also say position get string, get string position symbol because we want to print the position symbol and again we can also add The underscore symbol variable here. So you can see everything on the In the in the journal here So you can see we still have these two positions with index zero and index one We still have the position IDs and we have Bitcoin US dollar as a

[23:10] a position symbol and as a chart symbol. For example if I would... yeah no this is enough for now. So now we have all these informations so we know if we reach the body of this inner if statement here. We have selected a position, we know the position ticket and we know that the position has the same symbol as the chart symbol. So now we can start calculating a stop loss. But before we do this, it would be a good idea to check if it is a buy or a sell position. So we can say position get integer because this is now a integer value and we can say position type. We can check if it is position type buy like this. Because in this case it is a buy position and we

[24:04] We can do the same thing, position type by for cell positions. And as you saw here, I combined these two if statements here using this else if formula pretty much. So the PC first of all checks this if statement or this condition in detail. And if it is true, we enter the body of this first if statement, but if it is false, Then we check the second if statement here and check if this condition is true. And if it is true, we enter the second body of the second if statement here. So here we can now calculate our new SL price. So we can say, yeah, we want to have a specified amount of points from the current price.

[24:57] So we can say, as TSA points shall be 10,000 like this. And we can now use this input variable here to define the stop loss. So what we can do here is we can take for any by position, we can check if a symbol info double, underscore symbol underscore comma symbol bit, minus TSA points multiplied with point. So this would be a calculation for a simple stop loss level. So what we do here is we declare a double variable, which is just like any other variable, some space inside of your memory to hold a value. And a double value is a value that has a decimal point. For example, this would be a double value.

[25:50] This would be a double value. This would be a double value. Yeah, this works fine. So every number pretty much with a decimal point is a double value. So now we have this SL variable and we initialize it with the reset of this calculation. What we do here is first of all, we use the symbol info double function, which is pretty similar to the position get string or position get integer function. But this works for a symbol. So we write the name of the function symbol info double and then in parentheses, we have to provide at least two parameters. The first one is the symbol that we want to receive the information for.

[26:34] In this case, we again can use the underscore symbol variable here to provide the name of the current chart symbol. And as a second parameter, we have to provide one of the allowed identifier zero. identifiers here and you can read about these identifiers in the reference if you click on this symbol info double link here so you will see all the identifiers that are valid identifiers for the symbol info double function and we chose symbol bit here because it will return the bit price so the current price for the chart symbol then we take this price we subtract the TSA points that we defined up here from this bit value and we multiply the TSL points with a point value

[27:22] because TSL points is 10,000 and if we would subtract 10,000 from the current bit price which is 42,000 the SL would be placed at 30,000 which is pretty large distance. So we do not want to do this, but instead we want to multiply this 10,000 with the value of point for the current chart. And the value of point is always the smallest price change of the current chart symbols. For example, in Bitcoin it would be 0.01. And if we multiply 10,000 with 0.01, it would be 100 points, pretty much 100 euros or dollars for this current... chart symbol which is Bitcoin US dollar. So now we have a new SL and what we want to do now is we want to check if this new SL is

[28:22] greater than the current position SL. So what we want to do here is we want to or we can do it up here. We want to calculate the position SL. We can do this. Maybe you already expected but we can use the position get double function for this and we can say position SLs or identifier here, because this way we turn the position stop loss. And if we are here already, I can copy this line once, I think, for the position TP price, because we will need it later on. So we can just say position TP should be stored inside of this post TP variable. And now we can check if this SL that we just created here

[29:06] is greater than the previous or the position SL. And if this is true, then we want to modify our position here. So we can modify the position by using the Ctrade class. So at the very top of your program, you can write this hashtag symbol here. You can write include. And then in these brackets, you want to write trade slash trade dot MQH because this will include a file to a program. And this allows us to now create objects of the Ctrade class. This is kind of complex stuff if you are new to programming. This is object-oriented programming, which I will not explain in detail in this short tutorial.

[29:53] But you can check the link below this video if you want to have a full course in MQL5 programming. There you will learn everything about object-oriented programming and about the Ctrade class. But for now we just have to know that we can use this trade object here to have access to the position modify function. And this function it has three parameters. First of all the position ticket which we already have stored inside of our position ticket variable, then the position SL and this will be the new SL that we want to use now and the position TP and this will be the old TP because we do not want

[30:28] to change the TP. And we can now also provide a print statement here like position number, position ticket was modified like this. Make sure that you copy everything as I write it here, oh and I forgot to make the code a little bit bigger so you can read it properly on the PC. So make sure to copy everything like I do here so do not forget any comma or any semicolon or any bracket because otherwise your program will not compile. Okay, what I can now do is I can take this whole code here and I can copy it pretty much for the for the sale positions. So the calculation is a little bit different because in this case of

[31:15] course we want to have the ask price and add the TSL points to it because the the SL for a sale position is always above the current price. Then we check if the new SL is below the position SL. And we also have to check one more thing, but I will do this later on. And the rest is pretty much the same. If we click on compile now, you can see the positions are already modified. And you can see the buy position already has a stop loss, which is, or it should be 100, Let me change the time frame 100, yeah, 10,000 points below the highest price here. And this is, yeah, I think this should work. I mean, you can check it on your PC, it should work.

[32:05] And the sale position is not modified. And this is because the if condition is not true. So here we check if SA, if the newly calculated SA is smaller than the position SL. And this is false because right now the sale position doesn't have ASL, so the value for this is zero. So right now we are checking if this calculated SL price is smaller than zero, which is of course not true. So for every sale position, you should also always check if the position SL is equal to zero. Because in this case, the sale positions can be modified and you can see there is a SL for the sale position. And yeah, as the market moves, the SA is also direct,

[32:52] always 100 points after the current bid or ask price. And this can be used in any market. So you can not only use this in Bitcoin, US dollar, you can use this in Euro, US dollar in DE, 40, US 30, whatever you want to trade. So this is, can be used in any chart pretty much. So this is how you can trade in a specific distance. I want to add some more stuff to this. For example, you can have a TSA trigger, maybe also in points, because it might be the case that you want to say, I want to trail in a distance of 10,000 meter trader points, but only after the price is 5,000 points above my position open price.

[33:39] So what we can do here is we can say, before we do this whole calculation stuff, we want to check if the price is above the position open price. So first of all, we want the position open price here. So we use the position get double function again, position price open, like this. And then we can, yeah, I would also recommend to store the bit and ask price in a variable here like this. Symbol info double, symbol symbol bit and double ask symbol info double symbol symbol ask like this. And now we can exchange these function codes with a bit and for the same position with the ask variable. And now we can check before we do all the things here, we can check for the buy position

[34:45] if the bid price is greater than the position open price plus TSL trigger points multiplied with points. And only if this is true, so if the trigger is reached, we want to do the rest of the calculation and modification of the SL. The same for the sale position, we check if the ask price is below the position open price minus this trigger points multiplied with point. And only if this is true, then we want to do the rest. Make sure to always like move your code to the right a little bit. If you put it inside of a if statement because it makes them a lot more easier to read. So like this, this will now also only modify the position.

[35:26] If it is 5000 points in profit already. So as you can see here, even if the price changes, the SA will not be modified because the TSA trigger is not reached. And yeah, we can see this if there is a new tick. I don't know, we have to wait for a new tick now. Okay, there we saw a new tick and the SL is not modified. But if we change and you can always double click the trading stop system in the upper right corner to see the settings. If we change the TSA trigger points to zero, then it should modify the buy position because we are currently above the buy entry price. So this is how you can use a trading stop trigger. And there is one more one last thing that I want

[36:13] to demonstrate here because I want to show you how you can use a, wait let me change this again. And then I want to show you how you can use any indicator to use it as a trading stop. So what we can do here is, for example, we can use a moving average indicator to use it as a trading stop. So a moving average indicator always has to be created or can be, the values of a moving average can be received using a handle. You typically declare this handle inside of a onInit function. If you do not know the process of declaring moving average or indicator handles and everything, make sure to check out the other tutorials on this channel.

[36:56] I explained this multiple times. And if you, again, if you want to have everything in a structured way and a complete course, check out the link below this video. But we can create now, we can now create a handle in the, on Indian function using the IMA function. There we have to provide a symbol, a timeframe. And I would always suggest to TSLMA time frame to make this a input variable so the user will be able to change this. And of course we also need periods for this moving average, for example, 20 periods. And we need, whoops, this has to be of type integer. And we need the enum MA method input here,

[37:41] MA, TSLMA method, mode SMA. and we need the applied price. This is also an input variable that I always declare here. So the user is able to change this really quick. And then we can choose the TSMA time frame, TSMA period, shift value, most of the time choose the zero, the method and the TSMA applied price. So again, if you do not understand anything here, check out the other video tutorials first and then come back to this video. And yeah, this is a way of declaring any indicator handle. You can do the same for the I bands, for the bollinger bands indicator, for the fractals indicator, for the stochastic indicator.

[38:36] So you can pretty much choose most of the basic indicators using the I and then indicator name function to create a indicator handle. And then we can go back to our on tick function. And here we can now choose, or we can now use this handle here to receive the current moving average price. So what we can do here is we can say, we use the copy buffer function, and then we provide our handle MA. We have the buffer number zero, or we can also choose mainline as a identifier for this. We are starting position one, count one, and we store the values inside of our MA array here. If we compile this, yeah, we can now see

[39:23] that everything works and we now have a value stored inside of our MA array. So for a by position, we can now check if, or we can calculate the SL here, which is, No, we do not have to do this. We just have to check if the MA value, or we first of all check if the array size of this MA array is greater than zero. And then we check if the MA value at index zero is greater than the position SL. And then in this case, then we want to modify the trade. or what I saw here is we can of course declare this Ctrade object variable up here, so we do not have to do it here multiple times. So we can now choose or copy this code pretty much because this is always the same.

[40:27] So we put it here. We can say by M-A-T-S-A, we can modify these print statements some more so we see what kind of trading stop modified the positions. So let me do it like this. If it's double SL is equal to MA, if SL is equal to... Yeah, let's just do it like this. Makes it a little bit clearer because now we know that the MA value is stored inside of this SA variable, which we then use to modify our position. Then we can copy this whole block here and put it at the right place for the cell positions like this. And in this case, of course, we have to check if the new SL is below the position SL or

[41:28] if the position SL is equal to zero and then we want to modify the SA. And there's one more thing, because this may cause problems, because, yeah, you can see here it tries to modify a position, but it doesn't work. And this is, I think this is because it tries to modify the by position, but the current moving average indicator, and I can just demonstrate this by showing the moving average indicator in the chart. You can see the 20-period simple moving average is currently above the current price. So we cannot put the SL for a buy position above the current market price. So what we have to check here is before we modify the buy position, we want to check

[42:31] If this SL is below the current bid price and for the sale position, of course, we have to check if the new SL is above the current ask price. Only if we do it like this, the error messages will no longer appear here in the, in the journal. Yeah. So as you can see, there are new ticks, but there is no error message. And of course we can open a sale position again. And you can see the SL is always placed on the last moving average value. So for the last bar, you can of course also choose the current moving average value. You just have to change the copy buffer function here. And then you have to say starting position is zero instead of one.

[43:19] And this would change the SL to the current moving average price. And yeah, you can see it will be then stopped out if this price is reached. And if you are wondering why the execution is above the current price, this is because sale positions are of course always closed at the current ask price. So you can show the ask price here. If you right click the chart and click on properties and then you can see the current ask price is this red line it is a lot higher than the current bid price and it always changes so sale positions are closed above the current bid price of course. So let me explain this

[44:02] program one more time. So at the very top of our program we first of all include the trade.mqh file so if access to the Ctrade class then we later need to modify our positions. Then we have several input variables here and input variables are the type of variables that the user can find in the inputs tab here if he opens the Expert Advisor settings. We have input variables for the distance of our normal TSL in points and we have variables so we can define the moving average indicator that we want to use for our trading stock. Then we have another global variable and make sure that you realize that this is not an input

[44:48] variable because the input modifier is missing before the int or before the type declaration here. This is named handle MA. We use this handle MA variable to create an indicator handle, in this case a moving average indicator handle and the onInit function. Another indicator handle function which is IMA, we have several parameters. You can read about them of course in the documentation if you search for the IMA entry here. And you can see all the parameters here listed and explained. And this will then create a handle for a moving average indicator and the handle is pretty much just a description which is stored somewhere in the background of the

[45:36] MetaTrader and which the MetaTrader program can then use to look up what exact moving average indicator you want to calculate. Then we go to the onInit function because this is the function that we want to place most of our code inside. And there is only one for loop inside of this onTick function. This is one big for loop because we use this for loop to loop over all the open positions. And we can do this using this formula. I explained this very detailed before, so I will not do it again. After selecting a position and checking if this position has the same position symbol as the chart symbol, we then create several variables here.

[46:19] First of all, the object variable named trade of type C trade because we will use it later on to use the position modify function to modify the open positions and to update the SL. Then we receive some properties of the currently selected position. We can use the position get double function for this. Make sure every time you use this position get double function you have to select a position before otherwise this might not work. Okay, yeah. So we store the position open price, the position SL, and the position TP in three separate variables of type double. Then we have two more double variables, which is the bit and ask variable.

[47:03] We store the current bit and the current ask price inside of it. We receive these prices using the symbol info double function, providing the current chart symbol and the symbol bit or symbol asked identifier. Then we calculate the moving average and we do this using the copy buffer function. This is a function that you can use to receive values from any indicator handle pretty much because this is the first parameter for this copy buffer function. Then you have to provide what line or what indicator buffer you want to extract the values from. In this case the moving average only has one line.

[47:42] So it is always main underscore line or zero. I mean you can choose it is both the same. Then we have to provide the starting index. In this case we chose zero which is the current bar. So the last bar is the very right side. If we choose one we would start at the previous bar. And two at this bar and three at this bar and so on and so on. And then we have to provide the amount of values that we want to store inside of our moving average array. an array is always declared using these squared brackets and it can hold multiple values of the same type. In this case we only want to store one value inside of this moving average

[48:25] array. After preparing all these values and all this data we then check if we currently selected a bi-position. If this is true we want to check if the bit price is greater than the position open price plus or a TSA trigger for the normal TSA. If this is true, we calculate the SL price. And what is always a good idea is to normalize this price. We can do this by writing SL is equal to normalize double SL and then comma underscore digits because this will round it to the precision of the current charts and with digits. this, yeah, just makes sure that you do not have problems with rounding issues or something like

[49:08] this. So this is always a good habit to round prices. Now we check if this currently calculated SL is greater than the position SL. If this is true, we use the position modify function, which is part of the C trade class, which we can access using the C trade object, or this trade object variable, and we modify the position. If this was successful, we have a print statement that shows a text in the experts journal so we can see what the program does. Okay so this is everything for the normal TSA. Then we also have the moving average TSA and again I can place this code here so we round this as a value it's always a good habit as I said

[49:50] before. So what we do here is we check if the array size of the moving average arrays greater than zero so this is just to make sure that the copy buffer function was executed successful and that a value was stored inside of this array. We choose the first, the zeroth index of this moving average array to receive the current moving average price and we store it inside of this SL variable. We then round this value and then we do the same as we did before. We check if the price is currently, if the new SL is greater than the position SL and for this specific case because the moving average price can be above the

[50:28] current bid price. We check if this SM, which is the moving average price, is below the bid price and everything else is the same. Modify the position and we have a print statement to say what exactly we did here. And of course we do the exact same thing for this part here where we check if we selected a sale position. Yeah, the calculations are a little bit different, but the process is completely the same. And this is what you can do for every trailing stop. So as you see here, I demonstrated two trailing stops using a normal calculation in points and one using an indicator. And for any other trailing stop that you want to implement, you can always use this

[51:12] program as a base program because the raw skeleton can always stay the same. The one thing that changes is the calculation. And this in the end comes down to mathematical algorithms and calculations. And I cannot tell you every trading stop because there are 1000 different ways of trailing your stops. And I only want to give you an example. And you can now modify this however you want to modify it. So you can add one or two trading stops. You can modify these trading stops. You can exchange the indicator, you can add more indicators. So this is why I always say it is important to learn the basics.

[51:59] And once you have the basics figured out, you can go on from there. And we will be so much easier for you to adapt new styles of moving your SL, for example, if you already know the basic concept. So I hope that you were able to understand some mechanisms that I wanted to explain in this tutorial and if you did I would be super happy if you could like this video and make sure to write a comment just for the YouTube algorithm and it would really help me, it would help YouTube to suggest this tutorial to more people, so more people can benefit from it. Okay, this is it. I hope you liked the tutorial.

[52:42] Let me leave a comment what you want to see on YouTube. I should check out the complete course below this video. If you want to learn this in much more detail and with a better structure. So yeah, see you next time. Until then have a good time. Great trades, bye bye.
