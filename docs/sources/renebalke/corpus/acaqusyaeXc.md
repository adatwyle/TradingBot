# MT5 Supertrend Indicator Strategie MQL5 Programming Tutorial - Part 1 - Indicator Values

- URL : https://www.youtube.com/watch?v=acaqusyaeXc
- Date de publication : -
- Vues : 29 000
- Duree : 21:51
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, this is René and in this video I want to start a new programming tutorial where we will implement a supertrend expert advisor. So for everyone who does not know the supertrend indicator, it is a pretty famous indicator which is not part of the standard set of indicators in the MetaTrader 5. So if you do not have a supertrend indicator, you can check out the link in the description. I will post a download link there for the super trend indicator. And I already have the file here or the files. I have two files here, which is the first one is the source code. The second one is the executable.

[00:42] I think I will only upload the source code. So let me just demonstrate you how to install it in your omitted trader. So you copy the source code and you go to your metadata file and you simply click on file, open data folder, then you click on MQL file and indicators and you simply paste the file here. So I now have the supertrend.mq5 file here and I can close this folder again. If I try to find this indicator here, it is not there. And after refreshing, I cannot find it. And this is because I only have the source code right now. And what I have to do is I have to go to the meta editor, which I can find by clicking

[01:32] on meta quotes language editor or tools and then meta quotes language editor. It will automatically open the editor for me. And then on the left side, you can see the navigator. And if you click on a view and navigator, you can open and close it. And there you will find a folder which is called indicators and there we can find the supertrend.mq5 file. So I double click it and this will open the source code. And if I now compile the source code which means that I create an executable, I will now automatically find it in the metadata file. So now in the indicators folder in the navigator tab here, I can find the supertrend

[02:15] indicator and I can now drag and drop it on any chart that I want to use it with. So as you can see here we only have two settings which is the period and the multiplier and yeah you can choose the standard settings for example like this and you will now see there is a line which is drawn above or below the current candle. And whenever this line is hit by the price and the price moves, for example, above the line, then this super trend changes. It changes the color and the position of the line. So it moves from above the candidates to below the candidates. And there is a pretty common strategy, which is simply trading the super trend

[03:05] signals. Let us have a look at the signal here. You can see the price falls below the super trend. So we would have an entry somewhere here, for example, and we would trade until the trend changes again. And of course, you can have ATP, you can have an SA, you can have a trading stop, and we will cover all this in the upcoming videos. But yeah, first of all, let's start with this. Yeah, really basic strategy. So I can remove this RSI window here, I think, because we do not need it. And what we want to do now is we want to go back to our editor here, to the meta quotes language editor. So we can open this again and we can create a new expert advisor.

[03:48] Expert advisors are the kind of files that are used to other the programs that that are used to automatically trade a strategy. So we can create for example the supertrend system. You can choose whatever name you want to choose for this program. And then we can click on next, next and finish. And you can see there is a new file created which is automatically placed inside of this experts folder here. You will find the supertrend system somewhere here. And this is like the base skeleton for an expert advisor. This is like a template. And what we have here is we have many of these gray lines. I can simply remove all of them because they do not impact the program at all.

[04:39] They only provide information for the programmer, but this is no real, really complex program, so we do not really need comments. And I usually rearrange the brackets here like I do right now because this is the way that I used to program in this way that I always place the brackets. We will have a look at these three functions, Wilkwijk. But first of all, we can also delete all the three lines here at the top because we do not need them. And I always use to recompile the program quite often because whenever I compile the program it will now take the source code and create an executable. So you can see here in the navigator again I already have the super-trans system here

[05:33] and I can already put it on any chart and you can see this program can be executed. And it will already work. There is no code inside of these three functions, so of course we do not see any output on the chart or something like this. But if we for example add a print statement here, we can say this is like the onyllid function and a print statement is a function which then prints a text in the expert's journal here. So you can see in your MetaTrader 5 in the toolbox, you can click on experts and And you can see there is a text output, which is, this is the onInit function, because whenever I activate this program, let me clear everything, whenever I activate the program, the onInit

[06:27] function is automatically called. And you can say this means that the code, which is inside of the body of this onInit function is then executed. And the body is indicated by this opening bracket and this closing bracket. So everything in between of these two brackets is the body of this onInit function and it will be executed whenever the program is initialized. So the program is initialized for example when I activate the program or when I start the Metatrader or when I simply change the time frame as you can see here. There are more functions, for example the onDNet function. Yeah I can add another text for this saying this is the onDNet function and you can see

[07:19] whenever I remove the program from the chart like this the onDNet function is called just a split second before the program is removed. So whatever code is inside of the body of the onDNet function it is executed once the program is de-initialized. And if we activate it again, you can see this is also executed whenever or before we change the time frame. So it first calls the onDNit and then the inIt. And there's one more function left, which is the onTick function. And we can of course add a output here. And you can see whenever there's a tick or a price movement in the chart, this onTick function is called.

[08:03] So most of the time you want to place your main part of the code in the antique function because this is the function which is called whenever there is a price movement. So this is the place where you want to do all the calculations. So let me remove these print statements here because we do not need them. We know what these functions do and we can simply go ahead and calculate the indicator values. So yeah, and what I didn't mention is here, there is some more stuff when it comes to functions like parameters or return values. And we do not have to cover this right now if it will become necessary for us in the upcoming videos.

[08:46] I will of course explain it, but right now it is simply important that these functions are automatically called by the Metatrader and whenever they are called the code inside of the body of the function will be processed. So when we want to receive information from this super trend indicator here, we first of all have to create a handle. So we can say for example, ST handle, which is super trend handle, and we can then update the value in this ST handle variable. This is a integer variable. A integer variable is a, any number pretty much like two, three, four, oops, four thousand and something like this.

[09:33] So it is any number without digits. And a handle is pretty much just a predefined set of settings for this specific indicator. So the program automatically knows what specific indicator you want to work with. So if we have a custom indicator like the super trend indicator. And as I said before, this is no part of the standard set of indicators of the mid-25. We have to use the iCustom function. And you can always, if you have a question about any function, click inside of the name of this function and you can click F1 on your keyboard and it should automatically open the reference. If it does not automatically open the reference,

[10:17] you can click on help and Q5 reference. So, and while we are looking at this function, we can see this function has a return value and parameters. So this is already the point where I have to explain it. So whenever you have a function, a function can have parameters. And if there are parameters, you have to provide these parameters whenever you call the function. So for example, we see these three parameters here. And it is first of all a string parameter, which is the symbol that we want to calculate this this indicator for. And if you use underscore symbol, it is always the name of the current chart symbol.

[11:04] So for example, if I print this, I can show it to you real quick. The program is still active. And you can see this is Euro-US dollar right now because the program is running on the Euro-US dollar chart. If I activate the program on the US dollar Japanese yen chart, it will output US dollar Japanese yen. So this is a great way how you can use this underscore symbol system variable because it will always hold the value of the current chart. So as another parameter here for the super trend handle, we then have to provide a time period. So these parameters are always comma separated. So we can provide a time period.

[11:45] And the time period can be any value of this enumeration here. So if you click on this enum timeframes, you can see all the possible IDs here. So for example, we can say period M1, which would be the one minute chart. And you can provide it here as pretty much as a hard coded parameter for this variable, but there's a much more smarter way. You can create a input variable of type enum timeframes. So this is pretty much timeframe, I will explain it to you in a second. So we can create a variable of this enumeration type here. So instead of writing integer like we did here, we write the identifier of this enumeration as a type

[12:33] in front of the name of the variable. And before the type, we also add a input. And what this does is, oh, and we provide a default value, which is a period M1. And what this does is it allows the user to have a look in the settings here and then provide a time frame from a drop down menu. And this is a really cool way to make it easy for the user to choose the time frame. And you can of course always choose the current chart time frame if you choose current here. Yeah, for example, period current, we can set it as a standard value here. And we can now use this time frame variable that we created here as a parameter for the iCustom function.

[13:18] So whatever the user chooses here as a time frame will then be provided for the iCustom function. We then have to provide a indicator name and this is the file pretty much here, supertrend.mq5. So we can say for example, supertrend.ex4, five in this case five in this case because we want to have the executable. And then we can add some more parameters which is then a list. Let's go back to the custom, which is then a list of the indicator input parameters. And as we already realized, the indicator has two parameters, which is the periods and the multiplier. And we can say, we want this as input variables also.

[14:10] So we can say periods, for example, 10 and input double multi player 3.0 is the standard setting. So we can say periods, multi player like this. And there's a typo somewhere, periods like this. Okay, if we compile this, this works. Make sure that you don't forget the semicolon to close this line here. So we already learned or used a new variable type here, which is double. And double is not only like a number like 3 or 4 or 24, but it can have digits like we have here. So this is for example a number like 24.2. This would be a double variable and you can double number pretty much a double and you

[15:14] cannot store it in an integer type variable because it has a different type. So you need a double variable for this. Okay, since we now created this handle, which is pretty much only a pointer to a specific address in the memory which holds this indicator specifications. But what we can do now is we can go to our on tick function. We can say we want to have a double array which holds the supertrend values. And we can say, oh yeah, a double array is indicated by these squared brackets here behind the variable name. And this variable can now hold not only one value, but it can hold several values.

[16:07] And we will fill values inside of this array using the copyBuffer function. And this copyBuffer function has several parameters. For example, we can provide the stHender here. And then we can provide a bufferNumber. And as you can see, the super trend only has one value. You can see it in the data window. So this value will have the buffer number zero. And then we have a starting position. So we can say, for example, we want to have like the current value of the super trend, which would be starting position zero. And then we want to have a count. And the count may be three, for example. And we have to provide an array

[16:54] where we want to store the values inside. So what this does is it takes this starting position here, which is starting position zero, which means the last bar in this chart. Starting position one would be the previous last bar, starting position two the bar before this one and three would be the bar before that one and so on and so on. So if we have starting position zero, that means that we want to start at the very last bar in this chart. And then we have a count of three, which means that we want to have three values starting from the very last bar here stored inside of our array. So we will receive this value, this value,

[17:35] and this value. And we can demonstrate this by simply, for example, printing the values. Print ST0, ST1, and ST2. ST0, ST1 and ST2. And by using these squared brackets and a index, we can then say what specific value we want to receive from this double array. And I forgot the semicolon. And if we do it like this, you can see the value here is now printed. And this is not really working. I think it is because... Are there different settings? No, it's 10 and 3. Oh, I have the one minute time frame here. I have to choose current, of course, to receive the values from the current chart. And you can see if you compare the values in the data window here for the super

[18:35] trend. exactly the same which is printed in the expert journal here. So you can see this is a way to receive values from any indicator by using the icustom function and the copy buffer function. And we can of course also place a comment in the upper left corner of the chart by using the the comment function for this. And we can say, for example, super trend, super trend value zero like this. And then we can print the super trend value zero. Then we can go, for example, to a new line using this backslash n operator thing here. We can say super trend value at position one, which will then be super trend,

[19:30] the value one of this array. And we can say super trend value at array position two, which will then be super trend two. And you can, which is also important, you cannot access index three here. If I try this, this will produce an error. As you can see here, this is because we only stored three values inside of this array. and the indexing starts at zero. So we have index zero, one and two. And index three is not accessible because we only have three values. And so if I activate the program again, you can see the values are now printed in the upper left corner and this is super handy. And you can see the value for the last

[20:17] or the current bar now changes because yeah, you can see there is a drop pretty much in the super trend line. So this is a way to receive all the values from the super trend. We will use these values in the next or in the upcoming video to create, whoops, I don't want to see news here, to create signals for this trading program. Oh, and there's one last thing that I want to explain here for the iCustom function. I said this has a return value. If a function has a return value, it simply means that after the PC processes the function it will then return a value which we can use to for example store the value

[20:57] inside of a variable. And since we created this variable here we can store the return value of this icustom function inside of the st-hander integer variable. And we can then use this variable in the ontick function because it is a global variable which is declared outside of any function body and we can then use it here for copy buffer function to show the PC what exact specifications we chose for this supertrend indicator and what exact indicator values we want to receive. So what we learned here is how to receive values from a custom indicator. So make sure to subscribe to this channel so you don't miss out the upcoming videos

[21:40] where we will finish this program and where we will maybe even test it. So make sure to subscribe and I will see you next time. Goodbye
