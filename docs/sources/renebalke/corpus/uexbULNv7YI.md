# How to Create an RSI Trading Bot for MT5 using AI (Zero Experience Needed)

- URL : https://www.youtube.com/watch?v=uexbULNv7YI
- Date de publication : -
- Vues : 57 000
- Duree : 18:48
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey, listen in this video, I will show you how to create a RSI expert advisor fully with Gemini AI, absolutely no coding needed. And this expert advisor works in a way that it is selling if the RSI is overbought or signaling overbought. And it is buying if the RSI is signaling over sold. You can see here we do see a bunch of signals. For example, here, if the RSI goes above the 70, we see sale positions here and here again. Then also we do see already for the last position, there is a trailing stop in place. So whenever the market moves into the direction of a trade, a trailing stop is used to secure the profits.

[00:42] The expert advisor also has an additional moving average filter. So we only open by positions above the moving average and sale positions below the moving average. If you want to learn how to create this program, then watch this coding or not coding, but AI Vibe Coding Tutorial. And you can see this strategy can be used to create a very high hit rate expert advisor that can be used in any symbol with multiple settings combinations. And yeah, basically you can play around with it, test it and even use it in your account. So let's now create this exec strategy for the RSI indicator using Gemini AI.

[01:21] And I will always paste the parts of the prompt into Gemini AI. And I will explain why exactly we use these sentences and this logic. So to start off, whenever you try to create anything with chatbots, you should set a persona for the chatbots. Here I write act as an expert algorithmic trader and MQL5 developer, write strictly in MQL5, do not use a depreciated MQL4 functions. This is to avoid that the AI is mixing up MQL4 and MQL5. This was especially a problem with previous large language models. Recently it became a little bit better but still it doesn't hurt to add this function. Also you need to make sure that the AI understands

[02:13] that it is a professional in writing MQL five code. It can just save you from some like not professional written code. And then the next block is write a complete error free. Oops, I'll write a complete error free expert advisor for the Meteor Shredder five. The code should be efficient and without unnecessary repetitions or calculations. It should be safe and restore its state in case of a MQL MTFIVE restart. it should be able to run in an account with other EAs without interfering. The EA should have its own magic number and only open modifying clause trades with this magic number. The magic number should be adjustable in the inputs.

[02:54] The EA should work in any symbol. The program should be mainly used in the strategy for testing and optimization, make sure that the input parameters can be used in the MTFive optimizer. This is a little bit optional. I will exclude this sentence maybe, but I want to keep that the inputs should be usable in the MT5 optimizers. This can be helpful for testing. So this block basically is something you can use for any expert advisor that you want to create with any AI chat agent. So whenever you use a Gemini, Claude, doesn't matter if it's the free or the paid version, you should add this and it will just make sure that the quality of the code you receive is a lot better.

[03:39] So basically you can write this down and save it somewhere on your PC. That's also what I did. And whenever I want to create a new expert advisor, I simply copy and paste it. So now let's get to the actual strategy. First of all there will be the description. So I, I'm writing the EA should use Oopala. The EA should use the RSI indicator to find trading signals. Actually, it's a bit better like this, I think. So the EA should use the RSI indicator to find trading signals, the RSI period. And here I started to use in brackets. Whenever I explain an input parameter, I started to give like the default values.

[04:25] This makes sure that you will have the default values that you want. So I've write the RSI period default 14 and the time frame default default H1 should be set up in the EA inputs. In the inputs there should be two additional parameters for the RSI buy and sell threshold. Here we can also say default 30 maybe and sell default 70 threshold. Once the RSI goes below the RSI a threshold, it should open a by position. Once the RSI goes above the RSI threshold, it should open a sale position. Maybe here I should write buy and sell threshold. After a by position, the RSI must go above 51st in order to open a new another by position.

[05:16] For sale positions, it must go below 51st in order to open another sale position. So I'm trying to give the logic as detailed and as precise as possible. So I'm kind of preventing that there are multiple positions open whenever the RSI stays above 70 or just drops below 70 to 69 and then goes above 70 again because that's usually not what we want. So whenever you create strategies with chatbots like this, you should try to be as precise as possible and describe the strategy as detailed as possible and leave less room for error. Even though if there is room for error or you do not provide a perfect explanation,

[05:59] the AI will create the bot for you, but it will assume some things that you might want differently. So it's better to just provide a little bit more of an explanation. Then I also want to use a filter. So I'll write the EA should have an additional, an optional moving average filter. It can be turned on or off in the inputs. The moving average period default 50, Time Friend default D1 and type default simple should be set up in the inputs. If the filter is active, then the EA should only open by positions if the position open in price would be above the moving average and only open set positions if the position

[06:36] opening price would be below the moving average. So this is simple moving average filter and that's how you can edit. This brings us to stop loss, take profit, trailing stop and risk. I will just add these blocks here quickly and then we discuss. So for stop loss, I've right the stop loss for every order should be defined in a percentage default five of the position open price. Uh, wait. Yeah. Below by trades or above sale trades, the position open price. The percentage should be settled in the inputs. If zero is chosen, then there will be no as a for take profit. I do exactly the same, just the other way round.

[07:17] The take profit for every order should be defined in a percentage default one of the position open price above by trades or below cell trades. The position open price percentage should be set up in the inputs. If zero is chosen, then there will be no TP. So basically here you can add any logic you want. You could also ask for a SL or TP in points or in pips or whatever you want. Just make sure to use a description that is as precise as possible. Also, I personally like to use something like this to disable the parameter or to not use a TP. This just gives the user a little bit more flexibility because maybe he wants to close

[07:55] trades with the trailing stop. And that's the next section here trailing stop. There should be three inputs for the trailing stop, the trigger default 0.5, the distance default 0.1 and the step default 0.05. All inputs should be a percentage based on the position open price. trade is at least the trigger percentage and profit, it will activate the trailing stop, it will then trade the stop loss in the distance defined in the inputs. The stop is only adjusted if it is at least the step percentage further in profit than the previous stop. If the trigger points are set to zero again here, then the trailing stop is not used. And the last part,

[08:34] risk, the risk for each trade should be definable in the settings either as a monetary amount or as a percentage of the count capital. So this is the full prompt. It is quite a bit of text, but as I said, try to be more precise rather than write a little bit more. This will ensure that the AI will give you exactly the result that you want. So again, a quick summary first describe or define the persona or like the perspective from which the AI should fulfill this task, then then give a little bit of context, like what kind of program you want and what the AI should look for, for example, like write efficient code. And it might sound funny, but it really

[09:16] makes a difference. If you, for example, skip this sentence here, it can be that the program runs a lot slower in the tester. So just edit and it will make you a lot more happy with the reset. And then you can start describing the strategy. Usually I go ahead and describe the strategy first and then in different blocks, for example, ask for filters, stop loss, TP, trading stops. So just try to make a logical description that you would also give to a human programmer if you're asking for a freelance job, for example. It's kind of the same. And then just go and send the prompt. Doesn't matter if you use the free version of Gemini, Claude,

[10:00] chat GPT, the prompt can actually always be like this. And it should produce a reasonable and good resite. So there we go. Just a few seconds later, Gemini gave me the resite and this is the full code. What I like a little bit more to be honest about the Claude, I is that it's usually explaining the code, even if you don't ask for it, but I'm sure if you ask Gemini to explain the code, it would also explain it for you. or you can ask for comments inside of the code itself. If you don't know a lot about programming and if you wanna get a rough idea of what's going on. But what I will do is I will just click on copy code here,

[10:38] then go to the MetaTrader, then in the upper left corner, I click on Tools, MetaQuest Language Editor, and here in the Language Editor, let me close some of the programs here, I will go to the upper left corner, click on New, Expert Advisor Template, but it doesn't really matter. They not choose any name like Gemini, RSI trader, doesn't really matter. And then I click on next, next and finish. And this is a template for expert advisor, but actually we can delete all of the code here. And we will just paste whatever I got from the Gemini AI. And then in the upper toolbar here, Click on compile and actually I'm surprised that there are some

[11:27] some errors here because usually you do not see errors with the AI these days, but even if you see errors, you can just get these errors here. For example, copy them and paste them back to the AI. I get these errors, fix them and then you just, provide the errors here and then it should be able to fix the errors. Also with cloud AI or cloud code or the pro version of cloud code, I usually didn't have these problems. So I assume it's still a little bit better actually than Gemini, but you would see that it usually is able to quickly, to quickly fix the errors. If you just like provide, um, like the, the errors like this,

[12:18] So provide a screenshot of the errors and then I can just exchange the code snippet here for the lot size calculation. So I will just take it here. I will go to the function here, the dynamic lot size function. And I will just exchange this. So I will delete the previous one and paste the one that I got from the AI. And you can see now the error is fixed and now it should work better. So let's have a quick look at the parameters here because I will not go into detail with the code. So actually we can also do it in the meter trailer five. So once you copy the code here, click on compile again in the upper toolbar.

[13:02] And once there is no error, you are good to go. Then go back to your meter trader. And now you should be able to test it in the strategy test. So in the meter trader go to view strategy test up. And here you should see now a Gemini RSI trader, your program, like whatever name you chose, then you can choose any symbol you want to test in the time period, but this should not be about testing, but rather about expert advisor, I have other videos to explain the strategy test if you're interested in that. And then here, let's have a look at the inputs. And here it's really cool because since I asked

[13:35] for these inputs specifically, it's giving me exactly what I asked for. So we have the RSI settings, We have all of the default values that are asked for. We have the moving average with the switch so we can turn it on or off. We have the exit settings. We can disable them, which is really cool. We have the trailing stop settings. Yeah, also like with the default values that are asked for. And then we have a percentage of the account balance. Yeah, and if we just started tests with these settings with this expert advisor, we should see that it is exactly doing what we want. So you can see here, let me zoom in a little bit.

[14:14] We can see now I do not use this filter. So you can see once the moving average drops below these threshold levels, it will open the positions. Also, I think currently it's not working on a on close basis or is it? I think it is. What's the RSI value here? Let's have a look at the data window to get the RSI value. So yeah, I think it's working on an on close basis, which is good. That's usually what you want. And you can also see for fast forward, it's very efficient. It's very quick actually. So now let's see, for example, here we have a buy position. You can see the TP is usually a lot closer

[15:00] than the stop loss, which will give us a good win rate. And then we can see there's another TP position, but between two, there's another by position, but between two by positions, we should see that the RSI was, not below, was above 50, which happened here. So this is totally fine. Actually, this was a previous by position, which is still open, but yeah, you can see it can open multiple by positions if the TPE is just above the 50 again. And yeah, if I fast forward, I wanted to do it a little bit slower, So we can maybe see the, the, the trailing stop once and you can see once the position is going to profit.

[15:45] Okay. It's really quick, but yeah, we can see that in the operations, the stop is adjusted. So that's basically how it works. Also we can use now different settings here. For example, if we have a little bit of a better view on the trailing stop, we can say, for example, the trailing stops should go in distance of 0.5, which is a lot more and also the step could be 0.1 only. And then we could say we want to use also the trailing, uh, the, the moving average filter here. Then you can see I can adjust the settings and start the same test and the result will be different. So it's really cool. Once you call it this with AI, which just takes a few seconds, actually,

[16:24] you just need to write the prompt and maybe correct some errors. And then you have a program that you can test a lot of different strategies with. So you can see now there is no TP here, which is really cool. So all of the trades will be closed once the trading stop is activated, which we see here, for example, here for the sale trade, no, the trading stop is activated and it should trail in the distance of 0.5%, which it is doing here. So you can see it's really cool. And yeah, the step also is being kept here for the trades. So whatever is your preference, like using the trading stop or using a hard

[17:02] TP, using aggressive or not aggressive trading stop, you can do all of this with this expert advise and especially you can test all of this. So you can see, of course, this will naturally create a very high wind rate because we do have the very distance stop loss. So I think, I mean, even if I don't want to recommend anything really, ah, by the way, I also want to show you one more thing here, you can see we are only creating sales rates because we are currently below this moving average, which is kind of the filter that we use. And once we are above the moving average, we only open by trades. So yeah, what I wanted to say is we do have of course a very high

[17:40] hit rate. Actually we saw like one, two, three, three losses here, but still there's a profit. And if we go to the back test, we do see a very high win rate. And also of course we could increase this win rate even more if we use for example a very aggressive trading stop, or if we use a stop loss that is even further away. So I hope you like this little insight. I hope you want to play around with this program. And basically the most important thing is that you take away from this video is that how to prompt efficiently for the chatbots, because this I think is really the skill that you will need as an automated trader now already and also in

[18:19] the next month and years. If you want to be ahead of others, or if you just want to like write code or test different strategies very efficiently with minimum effort, no coding knowledge needed at all. And you can basically test and trade any strategy that you want. So use this wisely. Use it now to be ahead of other traders. And I hope you like this insight. I hope you like to play around with this. And I will see you in the next video. Have a great time and good trades. Bye bye.
