# CHANGELOG — TradingBot

## Non publié

- **Analyse des trades (parité Trade Buddy) — SPEC_analytics-trades** (1.1.2) : page `/analytics` (4ᵉ shell, lien « analyse » dans les 4 `<nav>`, lien « analyse détaillée » pré-filtré sur la page stratégie) ; routes `GET /api/analytics`, `/api/analytics/trades` (journal paginé serveur 200/1 000, tri `close_time desc`), `/api/analytics/kpi` (bandeau PAPER / LIVE de la vue d'ensemble) — filtre hors domaine = seule erreur (400), sinon jamais un 500 (sous-ensemble vide + `warnings[]`), méthodes ≠ GET = 405. Moteur `server/analytics.py` (calcul à la requête, scindé par mode × devise) : filtres AND/OR à listes dépendantes, 9 tuiles KPI, courbe solde + drawdown (série unique, base capital connue ou zéro), heatmap mensuelle + barres, répartitions instance / symbole / stratégie (groupe `magic divergent`) / jour / heure, distribution en R ou devise, synthèse 5 colonnes (Sharpe/Sortino sur R, streaks, durées, solde & coûts), positions ouvertes hors statistiques. Adaptateur **lecture seule** `server/journal_adapter.py` : les 5 journaux forward (`gold_forward`, `s13_forward`, `s20_forward`, `alexg_paper`, `macd_ai_paper`) lus comme des trades ledger (chaîne SHA-256 vérifiée, offset broker → UTC, `dedup` sur `(run_id, source_ref)` quand le ledger portera la projection). `state.py` : `LEGACY_STUDIES` dérivé du catalogue de l'adaptateur (+ `s20_forward` sur la carte S020), `study_state()` lit le format à bras (`arms{}`, s13/s20 — compteurs sommés + détail par bras). Front : SVG maison (`lineChart`, `areaChart`, `barChart`, `heatmapTable`), filtres dans l'URL, polling 10 s sans re-rendu inutile, feuille d'impression. Tests : +~120 (adaptateur, moteur, routes, état).
- **Analyse des trades — relecture tour 1** (1.1.3) : front, le garde « re-rendu seulement si le JSON diffère » (AN-3 / D-AN-14) comparait le corps entier, `generated` compris (horodatage recalculé à chaque requête) — il ne bloquait donc jamais et le DOM (tuiles, SVG, options, journal, bandeau KPI de `/`) était reconstruit à chaque poll de 10 s (`<details>` « tableau par instance » refermé, options réécrites sous le curseur) ; la comparaison porte désormais sur le corps sans `generated` (`payloadText`, garde statique dans les tests de routes). Courbe : la décimation > 2 000 points (AN-17) émet, par jour local, le solde final ET le creux du jour quand il en diffère — la courbe décimée porte de nouveau le max drawdown du KPI (D-AN-9, test à 2 001 points). Adaptateur : un journal réduit à des sauts de ligne (`
`) levait `StopIteration` hors de `read_study` et faisait perdre toutes les études — lu comme « journal vide sans en-tête » ; journal absent alors que `state.json` en mémorise la taille = « suppression détectée ». Mineurs : départage de tri numérique sur `id` (ordre du ledger), tris des répartitions sur le net brut, repli (3) de la base par ordre `open_time` (§3.3), symboles broker suffixés (`XAUUSD+`) filtrables, 405 rendu en JSON, `generated` UTC `Z` aussi sur les réponses dégradées et 400 des routes analytics, panneau d'options des filtres fermé au clic extérieur (AN-27), libellé « 0 » de la ligne de base en base zéro (AN-15), « n trades/an » affiché sous Sharpe (AN-26). Tests : +11.
- **Bascules études (CUTOVER)** : `macd_ai_paper` puis `s13_forward` migrées robinbot → tBot factory (GO Adrian, journaux intacts, verify 0/0). Restent : `gold_forward`, `alexg_paper`, `s14_sentiment`.
- **Schéma manifest unifié** : manifest S017 conformé au schéma plateforme (`display_name`/`magic_number`/`symbols`, SPEC_ui-dynamique §3) ; registre `tbot-factory` tolérant aux deux schémas (canonique prioritaire) ; gabarit `strategies/_TEMPLATE/manifest.yaml` créé.

## 1.1.0 — 2026-08-26

Run /apex-autonomous « GO E3 + E4, terminer l'application » (E3, E4, E5 + tBot factory + préparation bascule études). Détail des tâches : `SHARED_TASK_NOTES.md`.

- **E3** : code des 19 stratégies migré du prototype (verbatim + imports adaptés, verdicts préservés) + corpus de sources (7 chaînes).
- **Specs** : 6 spécifications d'implémentation (`spec/specification-app/`) par cc-spec, zéro TBD.
- **CI/CD (E4)** : pipeline GitHub Actions — push `dev` → pytest complet → publication auto `main` (ff-only) + tag `v<VERSION>`. Validé en production (premier tag v1.0.0 auto-créé).
- **Watcher PC prod (E4)** : wrapper englobant `tbot-prod-watcher` — poll `origin/main`, gate `update_safe` par stratégie (directive Adrian), `.stop` propre, pytest local post-pull, rollback anti-boucle, pull `db-backup/` sans redémarrage.
- **Ledger (E5)** : `core/ledger` sur le schéma hérité (WAL, migration v2 `instance_id`, agrégats jour/semaine ISO/mois/12 mois/année par stratégie et instance, décomposition fiscale brut/commission/swap/net).
- **UI (E5)** : serveur de supervision réécrit — découverte dynamique des stratégies, contrat `status.json` par instance, vues dev/paper/prod + courbes SVG, drill-down par stratégie, vue services communs, divergences déclaré/réel. Maquette et injection regex supprimées.
- **Telegram (E5)** : canal TradingBot — `tbot-notify` (ligne par trade + récaps jour/semaine/mois/12 mois/année aux formats Adrian) + `tbot-gateway` (bot dédié, sessions headless lecture seule, menu skills, `/etat`). Inertes sans tokens (TCK-007).
- **tBot factory** (directive Adrian 2026-08-26, TCK-005) : console 24/7 du projet — collecteur GEX S017, workers CC (files de tickets, S017), gateway/notify, backup, supervision ; panneau à chaud `C:\db\tradingBot\tbot-panel.txt` ; assertion anti-live R4.
- **Préparation bascule études (TCK-009)** : code des 5 études forward migré (scellés octet pour octet), workers off, `verify-journal`, runbook `CUTOVER.md` — bascule par GO Adrian, étude par étude.
- **Backup GitHub** : worker `tbot-backup` — allowlist fail-closed, commit `[skip ci]` scoped `db-backup/`, inerte hors branche `dev`.
- **Phase X** : revue finale du cumul (12 findings, tous traités ou actés), passe de-sloppify, 497+ tests.

## 1.0.0 — 2026-08-25/26

- Bootstrap E1 : structure projet, acteurs CC, corpus `input-adrian/` (9 chapitres), 19 dossiers stratégies documentés, ticketting.
- E2 : socle G3 RobinBot migré dans `app/` (factory, orchestrateur, serveur, core, ~180 tests), chemins unifiés `core/paths.py`, divergence de panneau corrigée.
- Fondation S017 ireland_gex (session parallèle).
