# Automate ANY MT5 Indicator With One Free EA (Full Code)

- URL : https://www.youtube.com/watch?v=DMCYVVNKC4M
- Date de publication : -
- Vues : 31 000
- Duree : 42:48
- Categorie : video
- Transcription : whisper (langue : en)

---

[00:00] Hey guys, this is René. Welcome back to another video on this channel. And today I will show you something new for the MetaTrader 5. And this is like the only video that you will ever need to trade any indicator in the MetaTrader 5 completely automated. I showed this several times on this channel for specific indicators, but today I want to show you how to trade any chart indicator, which is already attached to the chart, like really easily and really quickly. So to understand the process and like how to build this EA watch the complete video please and do not just copy and paste the code really try to understand

[00:40] what I'm saying because from my experience this is the only way that you really learn and understand programming for the Metatrader. But yeah today I will show you how to trade or first of all receive data from any chart indicator for example like this bowling events indicator or also from indicators that are displayed in a separate chart. You will find a lot of oscillators like the RSI that are displayed in a separate sub window pretty much below the main chart with the price. So we will write an automated program to recognize these EAs and to get to receive the data from them and then to be able to trade

[01:22] the signals. Okay, so first of all we will have to open the IDE which is the MetaQuotes integrated development environment and you can go to tools MetaQuotes language editor or you click on this IDE button and this should automatically open the text editor that you can use to write code and this also has a compiler in it which you can use to turn your text files into executable files that you can then run in the Metatrader. And this is what we do right now. Whenever we want to create a program that can trade in the Metatrader we will need an expert advisor for this purpose. So in the very upper left

[02:03] corner you can click on new, select expert advisor should be pre-selected, click on new and then choose any name for your program. For example trade any indicator but you can choose any name you like. Then you click on next next and finish and this will create a brand new expert advisor for you. The first thing that I will do is to delete all of these gray lines because the gray lines are just comments for the programmer and we will not need this in the tutorial. Also I like to rearrange these curly brackets a little bit doesn't really have any influence on the functionality of the program but

[02:43] this is like the style that I used to set my brackets. Also like these three lines in the very top these are just properties. So if I compile the program and you can do so by clicking compile in this toolbar here. So click compile and if you then switch back to your Metatrader you can go to the navigator, if you do not see it, click on view navigator. And then you can open the expert advisors folder here. And here you can find the program that you just created. For me, it is trade any indicator for you. It's whatever name you chose. So I can drag and drop this on any chart. And then you can see it will automatically open this pop up window.

[03:26] And it says trade any indicator 1.0, copyright, blah, blah, bloop, and there's a link behind this. And this is because we set these three property lines here. But if we delete them, we can still run the program, but the information here is gone. So this is what the properties do. It's not really necessary for the program, just another information for the user of the program. So I can now click on OK. And if I do this, you can see this program is already running in the upper right corner of the chart. And now what we want to do is we want to have access to these indicators that are already attached to the chart.

[04:04] So first of all, we will have to figure out how these indicators are named. So we can click control D, or you can go to view data window, and then you will see the data window. And here you can see the name pretty much of the indicator, because, oh no, I think the better idea is to right click the chart and then click indicator list, because here you will see the actual name of these indicators. Like the first one, the bolling events indicator is named BB only, and the second one, the RSI indicator is named relative strength index. So these are the names. The names will be really important for us to find these indicators.

[04:49] Because once we are in our code, we can go into this onInit function, which is a function that is one of the main like three event handling functions. Whenever you create a new EA you will have these three functions the on in it on in it and on tick the on in it function is automatically called when the program is activated or initialized. So in this function we will have to put some code. So the code that we will use is chart indicator get and this chart indicator get is a function that we can use right away because it is part of the MQL5 framework. And this function has three parameters. But first of all let's have a look at the documentation.

[05:36] So if you place your cursor inside of the name of this function and press F1 on your keyboard, it should open the documentation here or the reference. And we can read about this. And you can see this function returns a handle of a indicator with a specified short name. So what we can do here is we can provide a chart, a sub window and a indicator short name. So what we will do is we will choose our chart zero because this is always the chart where the EA is attached to. And then we choose a sub window. This is zero as well for the like for this part of the chart, like where the price is the price action is visible.

[06:18] and the RSI window would be sub-window 1 because there is this RSI indicator and it's in a separate window. So this would be window 1. If you would have multiple oscillators, it would be window 1, 2, 3, 4 and so on and so on. And now we need the short name and this is really easy because it is BB for the Bollinger bands. So if we compile this, there's no error, no warning and what we can do now is we can create another variable here on the global scale of this program, which will be named handleBB. Because as we read in the documentation here, this one or this variable, it will return a indicator handle

[06:58] if the function was successfully executed. So it will return, you can see it here in the so-called signature of the function, it will return a integer value. So we will create a integer variable here, which is named handleBB. And then make sure to not forget this semicolon to end the line. And in this integer variable, we can store any value. For example, we can store this return value of the function in this variable. And integer variables can always hold any value that is pretty much a number without a decimal point. So these are all integer values. And this handle will also be a number like this.

[07:43] It will be most likely the number 10 now for the first indicator handle created by this program. So this is how we can get like the handle for this indicator. And now we can go to our tick function. And now it's really easy because since we have this handle, and this handle pretty much now is the definition for this specific indicator. So now we can use this handle to receive data from this indicator. And since we are using MQL5, we will find a predefined function for this. And this function is copyBuffer. So we can use this copyBuffer function. And of course you can read about this in the documentation,

[08:24] but the entry for this copyBuffer function is a little bit confusing if you never worked with it. So I will explain it real quick. The copyBuffer function, it will need a indicator or handle as the first parameter because you will have to define what specific indicator you want to read from. So here we provide our handle that we just created because as I said before, this handle, it will hold all the information about this specific Bollinger Bands indicator that the EA found in the chart. And then there's the buffer number requested. So the buffer number can be taken from the data window in most cases.

[09:07] So we can see like this indicator has three buffers. So it has the middle line, the upper line, the lower line. And we can choose any of these lines. For example, if we want to choose the upper line, we will have to provide index one. The middle line is zero and lower would be two. Also, yeah, I think this should be correct. So we can choose one, for example, and this should be the upper value. And then we can go ahead and choose a starting position. The starting position is always counted like from the very right side of the chart. For example, this would be starting position zero, the last bar, this would be one,

[09:47] this would be two, this would be three, and then it just goes on like this. So if we want to add the value for the last bar, for example, we will have to choose index one as a start. And then there's the count. The count is pretty similar to the starting point. it starts at the starting point pretty much. For example, now we chose index one here as a starting point. And if we have a count of one, we say that we want to have the value of this bar. If we have a count of three, we would have this bar, this bar, and this bar, so three bars in total. If we would say four as a count, we will have these four bars.

[10:26] This would be five, six, seven, eight. So it just goes like this. So you always define like your starting point which bar you want to start, and then how many bars you want to receive the data for. So we only need like one bar here, so we can choose the count of one. And then the last parameter here is an array, and this is an array where we want to store the data in. So we will have to create this array first, and this area has to be of type double, because the double data type can hold values like this. So any value pretty much that has a decimal point and digits after the decimal point, important because we want to store price data in it. And as you can see here like on this

[11:09] epsilon axis, the price data usually has a decimal point. This is why we cannot use a integer data type, but we need a double data type because these variables can hold double values. So now we create a bb upper array and to make sure it's in array we have to add these squared brackets. So if we would create the variable like this, this would be somehow okay, but this variable would be only a single variable and this could only hold one value. But if you make it an array like this with these squared brackets, it is then an array and it can can hold multiple values. So we could, for example, choose to add

[12:02] values at different index points of this array, like this, for example, and like this. So we could store like multiple values inside of this array. This will not really work. At runtime it will throw an error because it doesn't throw an error. Okay, this is weird, but yeah, okay. We don't want to talk too much about arrays. I just made a video in the last few days or weeks where I talk about the array out of range problem that can happen if you're using arrays. You should watch the video to understand arrays better. But now we just have to know that we can use this BB-APPA array as a last parameter for the function

[12:51] Of course we want to store the data inside of this array. And what we will do now is we will create a chart comment which we can do by just writing comment, this is another function. Then we can say something like BB upper like this and we print the value in this BB upper array in the chart. So if we compile the program now, we should see in the upper left corner that there is some data displayed. And this is not really happening for USD-Japanisian because I'm recording on Saturday and there's no tick and we placed all of our code in the tick function. So this function is only executed if there is a tick, so a price movement in the

[13:34] chart. So what I will do is I will switch to Bitcoin because Bitcoin is traded on weekends. And now we should see as soon as there is a price movement here, we should see in the upper left corner that there's the value displayed. And there is a value displayed. So this seems to work, but the problem is that we do not really see the right value. So it looks like I messed up at some point in the code. So let me find the error here real quick. So the first one that we can do is the first thing that we can do is we can check if this handle was received successfully. So we can do something like if handle is equal to handle invalid or invalid handle

[14:16] Then we want to print something we can print for example Handle is invalid Check the name something like this So if we now compile again, we will probably see this message in the in the journal because Yeah, this seems to be invalid. So it looks like the the name that we added here is not correct. So this might be, I think, we should use the name from the data window as I said before, but I wasn't really sure about this. But let's try with the name bands now. So if we compile this we can see the name is still invalid so it's also not a bands so I will have to think about this maybe read in the documentation what kind of name is

[15:14] requested here so the short name and show no property using okay we can get this name using the chart indicator name function. I think this is what we should do because I somehow wasn't able to guess the name correctly here. So what we do is we will use a string variable and a string variable can always hold values that are text values. For example the name of a variable of an indicator. So So we can say something like indy name and then we can say chart chart indicator name because this is the function that was recommended in the documentation of the chart indicator get function. And this is for example how you can work with the documentation.

[16:06] You can just read it and often you would see a explanation of the parameter and you will get a fix or a way to get the requested value. And as you can see, I would consider myself as a somewhat experienced programmer for MQL5 programs, but still I don't know everything about the programming language or the framework. So I always have to read in the documentation and search for the right way to use a function, for example. So this is a function that I do not really use frequently, so I have to read about this. And I think it's totally fine. So this chart indicator name function, it requests a chart ID again, this again zero, a sub window

[16:48] again, which is zero, because yeah, we will choose the main chart pretty much. And then a index. And the principle of this is pretty similar to everything else. Indexing starts at zero. So we will have to look at the indicators. We will see the main chart, which is the chart with the indicator, with the chart ID zero. And then we will see that BB is the first index. So what we do here is we choose index zero because counting starts at zero here. And now we can for example print the name here if there was any error. And otherwise we will just choose this indicator name here for the chart indicator get function.

[17:37] And this should be correct now. So you can see there is no error so far. And if we see the next tick, we can see in the upper left corner that the value is now updated. So all we have to do is to check if this is the value for this specific bar. And you can see like in the data window, you will see the bands upper value. And if we compare this to the value that is displayed in the upper left corner of the chart, you can see this is correct. Also what we can do is we can change or chart comment here and we can say something like or we can make this in the name or name BB. We can make this a global variable so we can access it in any function and we can say

[18:21] name BB like this. We always use this variable now and now we can also choose this here in the in the chart comment like this. So if we do it like this, it should now display the name in the upper left corner once there is a new tick. And then we can see the short name that we would have to use. Okay, so you can see the short name for this indicator is bollinger bands. And this is something I think we will have to figure out using this chart indicator name function because it's hard to guess the name of the indicator if you do not use the function because it's not really displayed here anywhere.

[19:07] So yeah, this is how you can get the name or guess the name. So we can apply the same principle for our RSI indicator, right? So we will pretty much just copy everything. We will say name RSI, we will say handle RSI. And now we can go ahead and say the name RSI variable should hold the return value of the chart indicator name function for chart zero subwindow one. This is important because this is not in the main window, it's in the first subwindow. And then we have the index zero again because it's the first indicator in this specific subwindow. Then we have the handle rsi and here we say chart indicator get.

[19:58] So you can see once you figured it out it's really easy. You just have to get your main, your chart IDs and your sub-window IDs in line. I mean, the chart ID is pretty much always zero because you usually want to use the chart where the EAS attached to. And the sub-window is just in which window the indicator is visible. And then the chart name can be received using this chart indicator name function. And then we say name RSI like this. And of course we can have a print message here if this is invalid and then we can also add a new line here for the chart comment something like this. So if we compile this this will throw an error of course because this RSI array is not known

[20:56] already. have to create this first, but it's always the same now. We create in double array which we can call RSI for example and then we can use the copy buffer function, provide the handle for the RSI indicator, we choose the buffer number one, we start at position one, we have a count of one and we store the values in the RSI array like this. You can play around with these values if you want to receive other indicator values but for now I will just do it like this. Okay, this is a little bit ugly because I think it would be good to move this to a new line. If we compile this, we can now see this.

[21:34] Okay, this doesn't seem to work. I think it is because the index now, I probably wrote one here for the buffer number. Yeah, you can see the buffer number. I took this from the Bollinger bands, but this of course zero for the RSI because it only has one main line, one buffer line. Now with the next tick, the value here should update and it should display the value of the RSI for the last bar. So there are some more things that we can do. For example, it's now really easy to get the BB lower value, like the value for the lower line of this bowling events indicator. We just have to change the buffer number because now it is two and we can store the

[22:15] values in this BB lower array. So I can copy the line and just name, B, B print the lower value here of this bollinger bands indicator like this. And if we compile this, it should now give us the upper value for the bollinger bands indicator and also the lower value for the last bar. So you can see this is now working. So this is like the easiest way with just five lines of code, I think, for every indicator to receive the indicator values. So you just create the handle in the onInit function. You do not have to do this with every single tick because it doesn't really make sense. Or also what you...

[23:08] Yeah, I mean, yeah, you just do this in the onInit because you define the indicator once and whenever for example like the time frame changes it will automatically change all the values because it will then run the program again automatically. So you can see, yeah. Now it seems to have a problem with the indicator name of the bollinger bin's indicator. Yeah, but now it works. I don't know why there was a problem when I changed the time frame before. Maybe not everything was loaded correctly, but yeah, you can just check if everything works and if the right values are displayed in the upper left corner.

[24:00] So what I can do now is I can use the values that I have now for my RSI and bowling events indicator and I can now make my program trade these signals. And this is what I want to do next. So first of all, make sure to include this line into your file where you write trade trade dot mqh because this will include another file to your program. So just write this at the very top of your program and you will then be able to use the so-called C-trade class and you can use the C-trade class to create object variables. I will not explain everything about this in this video because it's object-oriented programming is

[24:48] a whole another topic. If you are interested in this, check out the links below this video and you will find a link to a complete course that I made about MT5 programming where I explain, for example, object-oriented programming details. But for now, we just have to know that we can use this trade object variable to open or close or modify positions. In this In this video we just want to open positions if there is a signal. So what we want to do, we can say something like, we want to trade, for example, if the price closes below the upper, below the lower line or the upper line of the bollinger

[25:32] bands, and if the RSI has a minimum or maximum value, this would be like a really easy program that we could do. So the easiest way to do so is to first of all make sure that you store two values in your BB upper and lower array. You can just change the count value here. So if you do this and compile, you will now store two values in your array. And also we can exchange the indexes indices here. So we still print the value for the last bar because now we will store like these two bars inside of our BB upper and BB lower array. And this bar here will be at index zero now in the BB upper and lower array

[26:19] and this bar will be at index one. And we will need these values to figure out if there was a signal. So what we do now is, the next step is that we receive the close of the bar at index one at index one in the current chart. And we can use the iClose function for this, period current like this. And the iClose function, you might guess it by the name, but it just returns the close price of a specific bar. And we define the bar in the function parameter. So we can choose the symbol, the time frame, and the shift value. And the easiest way to get the current chart is to use underscore symbol for the current chart symbol,

[27:07] then period current for the current chart timeframe, and then the index of the bar that you want to receive the values for. I will just choose one and two to get the close price for this bar and for the bar before that. So now I can check if the close at the last bar is above the BB upper value at the last bar, the last bar and also I want to check if the close at the bar before this bar is below the BB upper value at this specific bar because this means that the bar just closed the bar for the BB upper line for the first time. Also we can check if the RSI value at index zero because we only have one value in this

[27:55] array if this is for example greater than I don't know 60 something like this and in this case we open a cell position so we can use the trade object for this now it is a set so we can say trade cell and then we can say 0.1 for example for the lot size and then I can just go ahead and copy all of these lines and just turn around these mathematical operators pretty much and and exchange these arrays. So now it's the BB lower array because we want to check if like, the last bar just closed below the BB lower line. And yeah, like the RSI has to be below 40, let's say, and then we want to open a byte rate.

[28:42] So if we compile this, this should be a working program. So we can now just wait until there is a signal. I mean, this is kind of boring, of course. So what I will do here is I will just, wait, let me open a new chart, Bitcoin USD, and then I will go ahead and apply my template, plain chart, and now I will add these indicators again. So wait, let me search for the indicator so we can say oscillators, relative strength index, and then I want to add the Bollinger bands indicator as well, bowling events. And now I go right click, and then I say, save templates, save template. And I would save this template as tester.tpa.

[29:33] This is important if you want to test your program, because otherwise you will not see the indicators in the tester. So now I can, whoops, go ahead and open the strategy tester. You can do so by clicking view strategy tester. And then I can close my toolbox here. And in the strategy test side, it should look something like this. You can click on overview, then click on visualize. And you can just search for your program here. My program is named trade any indicator. So you just choose this, you choose a symbol here. Doesn't really matter. You choose a timeframe here. I will go with the five minute timeframe, for example.

[30:15] And then you choose the time period that you want to test. I would just test the last year. And then it is important like to see what the program does to take this visual mode box here. And then you can see we do not have any inputs and we can just start the program in the lower right corner by clicking on this start button. And this should open a new window where we can then see hopefully that the EAs working here. Also we should see the, oops, that's not really perfect. Also we should see that there is, there are the indicators displayed because we saved the template as test.tpl. And you can see there's a problem

[30:58] because we are opening too many positions. And this is something we will have to fix. Like you can see like the signal was correct at this point, right? So we can see that there, wait, wait, the signal you can see like here the bar closed above the BB upper line and the previous bar was below the line and also the RSI is 63 so above 60 so the signal is valid the problem is that the signal is valid for the for every tick in this bar here so we will open a position with every tick in this bar and this is of course not correct so what we will have to do we will you have to make sure that this does not happen.

[31:40] So the easiest way to do so is to create another global variable, which is bus total. And then you will just go to your antique function and create another integer variable, which is bus. And you will store the return value of the bus function inside of this. You will provide the symbol, the chart symbol, and the chart period as parameters. And then in the next step, we will check if bus total is unequal bars. And in this case, we will update the bus total variable. You can see we open the brackets for this if statement here and we will have to close the brackets and we will do this at the very bottom of the program

[32:27] just before the on tick function is finished. Then I will highlight everything here and use my tabulator button to move this a little bit to the right because this block here is now in the body of this if statement. So it will only be executed if this condition here in the if statement is true. So only if the value in the bus total variable, which is this one, if the value is different than the bus variable, this one, in this case we will update the bus total variable and then we will process all the code. This makes sure that for every other tick in this current bar, these values are the same.

[33:08] So we do not get into the body again because this condition is not true. And it's only true once for every bar. So if we run the program again now in the strategy tester, just close the previous window, run it again, then you can see that now we only open one position if this signal is there. So yeah, you can see that we had a signal, sell signal. There we had a signal and there we had a signal. Maybe we can make this even a little bit. We can filter out more signals. For example, if we change the RSI values to 80 and 20. So we will not see as many trades as we saw before because now the RSI has to be above 80

[33:54] or below 70, 20 sorry, for the signals to, so we see them. So we should not see a lot of signals here now I think. Wait, do we see any signal? Like here there was a signal I think. No, yeah let me just fast forward a little bit if we can see any signal here. Yeah, there was a signal. So yeah, maybe 80 and 20 is a little bit too much. So we should change maybe to 70 and 30 to see more signals. But yeah, you can see, you can just play around with this. And you can do this with any indicator you like. Just create like, or just search for the name of this indicator and then create the handle. And then you will be able to use the copy buffer function

[34:48] to get the actual value of this specific indicator. So this is a really easy and quick way to test any indicator you like in the chart for the current chart settings. Of course, you could also add ASL and take profit for all of your trades. I will not do it in this video, but yeah, you can watch some of the other tutorials on this channel. I showed this several times already and you will learn how to calculate a TP and ASL for your trades. So let's have a look at the program one more time. It's a really short program. It's kind of easy. So what we did here in the beginning is we included this trade.mqh file

[35:33] to have access to the C28 class. So if I remove this line, you can see the code doesn't work because this is not okay. but if we include the line, it's okay. So it's important to put this at the very top of your program. Then we have some variables, oops, then we created some variables that we will then need later on to store the name of the variables inside of them. This is string variable, which can hold values like this, like string values that are wrapped by these rotation marks. And we always create a integer value. this value should hold the indicator handle. So we know what specific indicator we want to calculate.

[36:21] And then there is this C trade class that we use to create a trade object. And this is important because we will later on use this here and here to open buy and sell positions. This is the easiest way to do so in Metatrader 5 programming. And we also have this bar's total variable. This one is needed later on to make sure that our code in the on tick function is only executed once per bar, not with every single tick. Then in the on tick on ended function, this is the most important part of the program to get the right indicator, pretty much. So we will, first of all, have to get the indicator name.

[37:02] And we can do so by using the chart indicator name function. This function has three parameters where you have to define like the chart ID. And again, this is, in most cases, zero. Then the sub window, this is zero for the main chart here. And every window below this is like zero or one or two. It depends on the order of them pretty much. And then you have to get like the index of your indicator. For example, if we have multiple indicators in our chart, I could add another indicator here. For example, let's say I want to add another moving average indicator. In this case, you would see that if we open the indicator list,

[37:41] there are two indicators in the main chart. And the bolling events would be index zero, and the moving average would be index one, and then the so on and so on. So index zero, one, two, three, four, five, and so on and so on. So once you did this, you would get the chart indicator name, and we store this in this variable. And then we calculate the handle. This is really important for us because we needed later on for the copy buffer function to receive the actual values. So we choose the chart indicator get function for this and we again, we just define the chart ID, the sub window and then the name of the indicator.

[38:17] This information was received before in this slide and then we check if we were able to calculate the handler otherwise we will print that it was invalid. And then we do the same thing for the RSI indicator but everything is pretty much the same just that we choose the sub window one now because this RSI is in a sub window. Then we have the onDinit function. This one is called whenever the program is de-initialized. And a lot of people already commented below my videos that you could do something like indicator release here where you release the indicators that you created. This will, I think it will free some resources on your PC

[39:00] like some, yeah, some space in your RAM, but I think in most cases it doesn't really make a difference. So you can add these lines, you cannot do so, it is up to you. And then in the on tick function, we will check first of all if there's a new bar, and this is what I explained before, we will get the number of bars in the current chart, we define the chart in the parameters of this I-bar's function. And then we just check if the value is no different than the value in the bar's total function. Because in this case, we want to process the code here. The first thing we do is that we update the bar's total variable,

[39:44] and then we process the rest of the code here. And the rest of the code is that we first of all receive the values for the BB upper line, the BB lower line, and the RSI indicator. We can do so using the copyBuffer function where we just provide the handle that we created in the onInit function. And then we just, in the copyBuffer function, we have to define the buffer number and the easiest way to get it is to use the data window. And you can see here in the data window, the first value for the indicators, usually index zero, then index one, and then index two. Might be different in some cases,

[40:20] but you can just check if you receive the right value if we print it in the chart or in the journal. So we receive the value for the BB upper, lower, and the RSI. We store it in the array for all of them. We just create an array. And then we get the close price of the last and the bar before this. And then we have some more if statements where we can check specific conditions that have to be true to open trade. So we just check if the close above the BB upper price and the close before the spies below the BB upper price. And then we just check the RSI. But I mean, this code is of course different

[41:04] for every indicator that you use. You will have to do, yeah, you will have to write whatever condition you want to check. For example, for a moving average, you might want to check if the close is above or below the moving average or whatever you like. And if all of your conditions are true, you just open it by your cell position. And again, if you want to learn more about position opening and calculating TP and SL prices, just check out some of the previous tutorials on my channel. I showed this like 10 times already. So just make sure to check out some of the previous videos. And then we have this chart comment.

[41:36] And this is the closing bracket for this if statement. So the body of this if statement wraps this complete block here. And it is important that you like move your code a little bit to the right to make sure you always, that you do not lose track of your structure in the code. So you can see if everything's moved a little bit to the right, it's easy to see that everything here is like inside of the body of this if statement. So this is why I would recommend to always do so. Yeah, so you can see that like this is a really easy and simple program. Copy the code if you like. And the most important thing is that I wanted to explain like this chart indicator get function

[42:21] that you can use to like receive data from any indicator in the chart. And you can play around with this, try out different indicators. And it's a really easy way once you set up the structure like this to test a lot of different indicators in the strategy test, of course. So I hope that you liked it. I hope that you understood most of it. So thanks for watching. And again, if you want to learn more about automated trading, check out the link in the video description. Anyways, thanks for watching, have a great time and good trades. Bye bye.
